---
phase: 01-motion-foundation-architecture-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - lib/gsap.ts
  - lib/motion-preferences.ts
  - hooks/use-legacy-parallax.ts
  - components/providers/smooth-scroll-provider.tsx
autonomous: false
requirements: [FOUND-01, FOUND-02, FOUND-03]
user_setup: []
must_haves:
  truths:
    - "gsap, lenis, and @gsap/react are installed at pinned versions and imported only from client-boundary modules (never from a server component)"
    - "A single SmoothScrollProvider client component owns one Lenis instance driven by GSAP's ticker (autoRaf:false), with no second requestAnimationFrame loop inside it"
    - "Under prefers-reduced-motion the Lenis lerp is 0.15 (snappier, still smoothed — not native scroll); under no preference it is 0.07; the switch is live-updating via gsap.matchMedia()"
    - "The legacy [data-parallax] rAF loop is ported byte-for-byte into a single reusable hook called exactly once from inside SmoothScrollProvider"
  artifacts:
    - path: "lib/gsap.ts"
      provides: "Module-scope gsap.registerPlugin(ScrollTrigger, useGSAP); re-exports gsap, ScrollTrigger, useGSAP"
      exports: ["gsap", "ScrollTrigger", "useGSAP"]
    - path: "lib/motion-preferences.ts"
      provides: "Locked Lenis lerp pair + reduced-motion resolver"
      exports: ["LENIS_LERP", "getLenisLerp"]
      contains: "0.07"
    - path: "hooks/use-legacy-parallax.ts"
      provides: "Verbatim port of the existing [data-parallax] + --scroll-y rAF loop"
      exports: ["useLegacyParallax"]
    - path: "components/providers/smooth-scroll-provider.tsx"
      provides: "Single Lenis+GSAP ticker-synced scroll engine wrapping {children}"
      exports: ["SmoothScrollProvider"]
  key_links:
    - from: "components/providers/smooth-scroll-provider.tsx"
      to: "lib/motion-preferences.ts"
      via: "getLenisLerp() inside a gsap.matchMedia() branch"
      pattern: "getLenisLerp|matchMedia"
    - from: "components/providers/smooth-scroll-provider.tsx"
      to: "hooks/use-legacy-parallax.ts"
      via: "useLegacyParallax() called once"
      pattern: "useLegacyParallax"
    - from: "components/providers/smooth-scroll-provider.tsx"
      to: "gsap.ticker"
      via: "gsap.ticker.add drives lenis.raf"
      pattern: "ticker.add"
---

<objective>
Install the GSAP + Lenis stack and build the unified scroll/motion engine as isolated new modules: `lib/gsap.ts` (plugin registration), `lib/motion-preferences.ts` (reduced-motion lerp gate), `hooks/use-legacy-parallax.ts` (ported legacy parallax), and `components/providers/smooth-scroll-provider.tsx` (single Lenis instance driven by GSAP's ticker). Satisfies FOUND-01/02/03.

Purpose: Establish the one true frame loop (FOUND-01), the "softened, not binary-off" reduced-motion contract (FOUND-02), and client-boundary-only initialization so the build stays SSR-clean (FOUND-03). The provider is created here but NOT mounted in `app/layout.tsx` yet — mounting happens in plan 06's switchover, in the same step that deletes `experience.tsx`'s private rAF loop, to avoid the transient dual-rAF window flagged in 01-RESEARCH.md.

Output: three `lib`/`hooks` modules and one provider component; three npm dependencies at pinned versions. No file currently imports these, so the running app and the build are unchanged after this plan.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-motion-foundation-architecture-cleanup/01-RESEARCH.md
@.planning/phases/01-motion-foundation-architecture-cleanup/01-PATTERNS.md
@.planning/phases/01-motion-foundation-architecture-cleanup/01-UI-SPEC.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@components/experience.tsx
</context>

<artifacts_this_phase_produces>
This plan creates the following new symbols/files:
- `lib/gsap.ts` — re-exports `gsap`, `ScrollTrigger`, `useGSAP` after `gsap.registerPlugin(ScrollTrigger, useGSAP)`
- `lib/motion-preferences.ts` — exports `LENIS_LERP` (`{ normal: 0.07, reduced: 0.15 }`), `getLenisLerp`
- `hooks/use-legacy-parallax.ts` — exports `useLegacyParallax`
- `components/providers/smooth-scroll-provider.tsx` — exports `SmoothScrollProvider`
- New npm dependencies: `gsap@3.15.0`, `lenis@1.3.25`, `@gsap/react@2.1.2`
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate for lenis (blocking, not auto-approvable)</name>
  <files>package.json</files>
  <what-built>
    Nothing built yet — this is the mandatory supply-chain gate before installing dependencies. `gsd-tools query package-legitimacy check` flagged `lenis` as SUS with reason "too-new" (latest version 1.3.25 published ~3 weeks before research). Cross-checked signals in 01-RESEARCH.md (Package Legitimacy Audit) are reassuring: 1,017,336 weekly downloads, official `github.com/darkroomengineering/lenis` repo, not deprecated, no suspicious postinstall — almost certainly a false positive from the freshness heuristic. `gsap` and `@gsap/react` passed (OK). The gate is procedural and blocking; it cannot be auto-approved.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/lenis and confirm the package repository points to `github.com/darkroomengineering/lenis` (the maintainer org cited throughout the project research) and the latest version is `1.3.25`.
    2. Confirm the weekly download count is in the ~1M range (not a low-adoption typosquat).
    3. Confirm there is no unexpected `postinstall`/`preinstall` script listed.
    4. Approve to allow `npm install gsap lenis @gsap/react`, or reject with the specific concern.
  </how-to-verify>
  <action>
    Run the read-only GSD package-legitimacy audit and npm metadata checks. Present the repository, download, lifecycle-script, deprecation, and exact-version evidence to the user. Do not install any package until the user approval already recorded in project context is confirmed as the active authorization for this execution.
  </action>
  <verify>
    <automated>cd f:/ClaudeCode/Pagina_Web_Mayra/local-2 &amp;&amp; node C:/Users/JHOAN/.claude/gsd-core/bin/gsd-tools.cjs package-legitimacy check --ecosystem npm lenis gsap @gsap/react</automated>
  </verify>
  <done>The legitimacy evidence is recorded and explicit user approval authorizes installation of the three pinned packages.</done>
  <resume-signal>Type "approved" to proceed with the install, or describe the concern to halt.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install stack and create lib/gsap.ts + lib/motion-preferences.ts</name>
  <files>package.json, package-lock.json, lib/gsap.ts, lib/motion-preferences.ts</files>
  <read_first>
    - .planning/research/STACK.md (Integration Pattern — exact lib/gsap.ts shape, pinned versions, ticker-sync wiring)
    - .planning/phases/01-motion-foundation-architecture-cleanup/01-PATTERNS.md (lib/gsap.ts + lib/motion-preferences.ts analog sections, verbatim shapes)
    - .planning/phases/01-motion-foundation-architecture-cleanup/01-UI-SPEC.md (Scroll Engine + Reduced-Motion Contract: lerp 0.07 normal / 0.15 reduced, locked)
    - components/experience.tsx (existing `window.matchMedia("(prefers-reduced-motion: reduce)")` string to reuse verbatim, line 11)
  </read_first>
  <action>
    Run `npm install gsap@3.15.0 lenis@1.3.25 @gsap/react@2.1.2` (only after Task 1 approval). Do NOT add `transpilePackages` to `next.config.ts` preemptively — only if a real build failure demands it (per STACK.md).

    Create `lib/gsap.ts` per STACK.md's Integration Pattern: import `gsap`, `ScrollTrigger` (from `gsap/ScrollTrigger`), `useGSAP` (from `@gsap/react`); call `gsap.registerPlugin(ScrollTrigger, useGSAP)` once at module scope; re-export `gsap`, `ScrollTrigger`, `useGSAP`. No `typeof window` guard is needed as long as this file is only ever imported transitively from `"use client"` files (Import Hygiene Constraint, FOUND-03). No `"use client"` directive in this module itself.

    Create `lib/motion-preferences.ts`: export `LENIS_LERP = { normal: 0.07, reduced: 0.15 } as const` (locked values from 01-UI-SPEC.md) and `getLenisLerp()` which returns `LENIS_LERP.reduced` when `window.matchMedia("(prefers-reduced-motion: reduce)").matches` is true, else `LENIS_LERP.normal` — reuse the exact media-query string from `experience.tsx` line 11.
  </action>
  <verify>
    <automated>cd f:/ClaudeCode/Pagina_Web_Mayra/local-2 && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` dependencies include `gsap`, `lenis`, `@gsap/react` at the pinned versions (3.15.0 / 1.3.25 / 2.1.2).
    - `lib/gsap.ts` contains `gsap.registerPlugin(ScrollTrigger, useGSAP)` and re-exports all three symbols.
    - `grep -n "0.07" lib/motion-preferences.ts` and `grep -n "0.15" lib/motion-preferences.ts` both match; `getLenisLerp` is exported.
    - `lib/gsap.ts` does NOT contain `"use client"` and `next.config.ts` was not modified.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>Stack installed at pinned versions; plugin-registration and reduced-motion-lerp modules exist and typecheck.</done>
</task>

<task type="auto">
  <name>Task 3: Port legacy parallax hook and build SmoothScrollProvider (unmounted)</name>
  <files>hooks/use-legacy-parallax.ts, components/providers/smooth-scroll-provider.tsx</files>
  <read_first>
    - components/experience.tsx (parallax rAF loop lines 34-52 + cleanup lines 79, 81 — the exact code to port unchanged)
    - .planning/phases/01-motion-foundation-architecture-cleanup/01-PATTERNS.md (use-legacy-parallax.ts + smooth-scroll-provider.tsx analog sections)
    - .planning/research/ARCHITECTURE.md (Pattern 1: <ReactLenis root>, gsap.ticker.add((t) => lenis.raf(t * 1000)), lagSmoothing(0), lenis.on('scroll', ScrollTrigger.update))
    - .planning/research/PITFALLS.md (Pitfall 4/7: competing rAF loops; Pitfall 1: StrictMode cleanup)
    - node_modules/lenis/dist/types/lenis.d.ts (confirm the installed types; the resolved plan deliberately avoids mutating internal options)
  </read_first>
  <action>
    Create `hooks/use-legacy-parallax.ts` exporting `useLegacyParallax()`. Port `experience.tsx` lines 34-52 byte-for-byte into a `useEffect`: `--scroll-y` write on `document.documentElement`, the `[data-parallax]` iteration with `getBoundingClientRect()`, the `reducedMotion` early-return guard (read via the same `window.matchMedia("(prefers-reduced-motion: reduce)").matches` string), the passive `scroll` listener, and the `updateScroll()` priming call. Provide the exact cleanup (remove the scroll listener, `cancelAnimationFrame(frame)`). Do NOT convert to ScrollTrigger and do NOT wrap in `gsap.matchMedia()` — this stays vanilla until Phase 2 (MOTION-02).

    Create `components/providers/smooth-scroll-provider.tsx` as a `"use client"` component `SmoothScrollProvider({ children })`. Render `<ReactLenis root>` (from `lenis/react`) with options `{ autoRaf: false, syncTouch: true, anchors: true, lerp: getLenisLerp() }`. `anchors: true` is required by the official Lenis documentation because this page relies on real in-page anchor links. Use a small child `LenisGsapBridge` rendered inside `<ReactLenis>` so `useLenis(ScrollTrigger.update)` can access the active instance; in that bridge register `gsap.ticker.add((time) => lenis.raf(time * 1000))`, `gsap.ticker.lagSmoothing(0)`, and the exact matching `ticker.remove` cleanup. Import `gsap`/`ScrollTrigger` from `@/lib/gsap` and `getLenisLerp` from `@/lib/motion-preferences`.

    Resolve Assumption A3 without mutating Lenis internals: maintain a reduced-motion boolean in `SmoothScrollProvider`, set it from a `gsap.matchMedia()` branch, and give `<ReactLenis>` a stable mode key (`normal`/`reduced`) plus the matching lerp. A live preference change therefore destroys/recreates the React-owned Lenis instance through normal React lifecycle; `LenisGsapBridge` cleans up the old ticker callback and registers the new instance. Do not write to `lenis.options` and do not rely on an undocumented `updateOptions` method. Call `useLegacyParallax()` exactly once inside this provider (never per-section). This provider is NOT mounted anywhere yet — plan 06 mounts it in `app/layout.tsx`.
  </action>
  <verify>
    <automated>cd f:/ClaudeCode/Pagina_Web_Mayra/local-2 && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `hooks/use-legacy-parallax.ts` exports `useLegacyParallax`; `grep -n "requestAnimationFrame" hooks/use-legacy-parallax.ts` matches (loop ported); `grep -n "ScrollTrigger\|gsap.matchMedia" hooks/use-legacy-parallax.ts` returns zero (stays vanilla).
    - `components/providers/smooth-scroll-provider.tsx` starts with `"use client"`, exports `SmoothScrollProvider`, and contains `ticker.add`, `ticker.remove`, `autoRaf`, `anchors: true`, `getLenisLerp`, `useLegacyParallax`, and a `matchMedia` call.
    - `grep -rn "smooth-scroll-provider\|SmoothScrollProvider" app/` returns zero (provider intentionally not yet mounted).
    - `npm run typecheck` and `npm run lint` exit 0.
  </acceptance_criteria>
  <done>Single-loop Lenis+GSAP provider and the ported parallax hook exist, typecheck, and lint clean; provider not yet mounted (dual-rAF avoided).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local build | Third-party package code is pulled into the build/runtime bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering (supply chain) | `npm install gsap lenis @gsap/react` | mitigate | Install pinned registry versions (not CDN `<script>`). `lenis` is flagged SUS ("too-new") and gated behind the blocking `checkpoint:human-verify` in Task 1 (npmjs.com repo + downloads + no-postinstall confirmation) before any install runs. `gsap`/`@gsap/react` verified OK in the Package Legitimacy Audit. Kept as T-01-SC per the STRIDE register in 01-VALIDATION.md. |
</threat_model>

<verification>
Per D-02 no formal phase gate here; `npm run typecheck` and `npm run lint` run per-task as cheap smoke checks to localize errors before the multi-file switchover in plan 06. The full lint+typecheck+build+visual gate runs once at phase end (plan 07).
</verification>

<success_criteria>
- Stack installed at pinned versions behind the lenis legitimacy gate (FOUND-01 supply chain, T-01-SC).
- Single-loop provider owns one Lenis instance driven by gsap.ticker with autoRaf:false (FOUND-01).
- Reduced-motion lerp 0.07→0.15, live-updating via gsap.matchMedia() (FOUND-02, softened-not-off per UI-SPEC).
- All new client-boundary modules; nothing imported by a server component; typecheck/lint clean (FOUND-03).
</success_criteria>

<output>
Create `.planning/phases/01-motion-foundation-architecture-cleanup/01-02-SUMMARY.md` when done.
</output>
