---
phase: 01-motion-foundation-architecture-cleanup
plan: 04
type: execute
wave: 2
depends_on: [01-01, 01-02]
files_modified:
  - components/sections/hero-section.tsx
  - components/sections/manifesto-section.tsx
  - components/sections/capabilities-section.tsx
autonomous: true
requirements: [ARCH-02]
must_haves:
  truths:
    - "Hero, Manifesto (perspectiva/statement), and Capabilities are each their own \"use client\" section component with markup lifted verbatim from app/page.tsx"
    - "Each section owns a scoped useGSAP({ scope }) call whose body is the existing IntersectionObserver [data-reveal] setup (byte-identical threshold 0.12 / rootMargin '0px 0px -8% 0px'), scoped to the section root — NOT a new gsap.from()/ScrollTrigger animation"
    - "HeroSection owns the video via useRef<HTMLVideoElement> (no document.querySelector('#hero-video')), and the video-toggle button renders as a JSX sibling of <section className='hero'>, not nested inside it"
  artifacts:
    - path: "components/sections/hero-section.tsx"
      provides: "Hero markup + video state/toggle + video-toggle button (sibling), scoped reveal"
      exports: ["HeroSection"]
    - path: "components/sections/manifesto-section.tsx"
      provides: "Perspectiva/statement (#nosotros) markup, scoped reveal"
      exports: ["ManifestoSection"]
    - path: "components/sections/capabilities-section.tsx"
      provides: "Capabilities (#capacidades) markup + moving-band, scoped reveal"
      exports: ["CapabilitiesSection"]
  key_links:
    - from: "components/sections/hero-section.tsx"
      to: "videoRef (useRef<HTMLVideoElement>)"
      via: "toggleVideo reads videoRef.current instead of querySelector"
      pattern: "useRef<HTMLVideoElement>"
    - from: "components/sections/capabilities-section.tsx"
      to: "lib/site-content services"
      via: "services.map for service-row list"
      pattern: "services.map"
    - from: "components/sections/*"
      to: "lib/gsap useGSAP"
      via: "scoped useGSAP wrapping the reveal IntersectionObserver"
      pattern: "useGSAP"
---

<objective>
Extract the first three page sections — `HeroSection`, `ManifestoSection`, `CapabilitiesSection` — from `app/page.tsx` into per-section `"use client"` components, each with its own scoped `useGSAP` reveal wrapper. Part of ARCH-02.

Purpose: Decompose the page's inline section markup into owned components so animation work in later phases is section-scoped. The critical correctness constraint from 01-RESEARCH.md: `useGSAP` here is used as a StrictMode-safe `useEffect` replacement wrapping the EXISTING vanilla `[data-reveal]` IntersectionObserver — reveal/parallax stay byte-identical this phase (Phase 2 / MOTION-01 does the GSAP-native migration). HeroSection additionally fixes the `document.querySelector("#hero-video")` coupling via a ref while preserving the video-toggle button's exact DOM position.

Output: `components/sections/{hero,manifesto,capabilities}-section.tsx`. Not mounted yet — plan 06 composes them into `app/page.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-motion-foundation-architecture-cleanup/01-RESEARCH.md
@.planning/phases/01-motion-foundation-architecture-cleanup/01-PATTERNS.md
@.planning/phases/01-motion-foundation-architecture-cleanup/01-UI-SPEC.md
@app/page.tsx
@components/experience.tsx
</context>

<artifacts_this_phase_produces>
This plan creates the following new symbols/files:
- `components/sections/hero-section.tsx` — exports `HeroSection`
- `components/sections/manifesto-section.tsx` — exports `ManifestoSection`
- `components/sections/capabilities-section.tsx` — exports `CapabilitiesSection`
Consumes (from earlier waves): `Arrow` (`@/components/arrow`), `services` (`@/lib/site-content`), `useGSAP` (`@/lib/gsap`).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Extract HeroSection with video ref (no querySelector) and sibling video-toggle</name>
  <files>components/sections/hero-section.tsx</files>
  <read_first>
    - app/page.tsx (hero markup lines 55-110, including the hero-media parallax wrapper with data-parallax="0.16", the `<video id="hero-video">`, and the circle-link with data-cursor)
    - components/experience.tsx (videoPlaying state line 8; reduced-motion video pause lines 13-18; toggleVideo lines 96-110; video-toggle button JSX lines 189-197)
    - .planning/phases/01-motion-foundation-architecture-cleanup/01-RESEARCH.md (Video-toggle DOM-positioning pitfall — button must be a JSX sibling of <section className="hero">, not nested)
    - .planning/phases/01-motion-foundation-architecture-cleanup/01-PATTERNS.md (hero-section.tsx analog + Shared Patterns: scoped reveal useGSAP)
  </read_first>
  <action>
    Create `components/sections/hero-section.tsx` as a `"use client"` component `HeroSection`. Return a `<>` fragment containing BOTH `<section className="hero" id="inicio" ...>` (markup verbatim from `app/page.tsx` lines 55-110) AND the `<button className="video-toggle">` (from `experience.tsx` lines 189-197) as siblings — do NOT nest the button inside `<section className="hero">` (preserves the current `.video-toggle { position: absolute }` containing-block behavior). Hold `const [videoPlaying, setVideoPlaying] = useState(true)` and a `const videoRef = useRef<HTMLVideoElement>(null)`; attach `ref={videoRef}` to the `<video>` element (keep `id="hero-video"` attribute for CSS parity but read the element via `videoRef.current`, never `document.querySelector`). Port `toggleVideo` (lines 96-110) to read `videoRef.current`. Add the reduced-motion pause-on-mount behavior (experience.tsx lines 13-18) inside an effect using `videoRef.current` (`heroVideo?.pause()` + `setVideoPlaying(false)` when `prefers-reduced-motion` matches). Import `Arrow` from `@/components/arrow` for the circle-link. Add a scoped reveal: `const root = useRef<HTMLElement>(null)` on the `<section>`, wrap the existing IntersectionObserver `[data-reveal]` setup (experience.tsx lines 20-32) inside `useGSAP(() => { ...scoped to root.current.querySelectorAll("[data-reveal]") ... return () => revealObserver.disconnect() }, { scope: root })`. Do NOT introduce any `gsap.from()`/ScrollTrigger animation — keep reveal byte-identical.
  </action>
  <verify>
    <automated>cd f:/ClaudeCode/Pagina_Web_Mayra/local-2 && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `components/sections/hero-section.tsx` starts with `"use client"`, exports `HeroSection`, contains `useRef<HTMLVideoElement>`, `useGSAP`, and `data-parallax="0.16"`.
    - `grep -n "document.querySelector" components/sections/hero-section.tsx` returns zero matches.
    - `grep -n "video-toggle" components/sections/hero-section.tsx` matches, and the button is a sibling of `<section className="hero"` (not inside it) — confirm by structure review.
    - `grep -n "gsap.from\|gsap.to\|ScrollTrigger.create" components/sections/hero-section.tsx` returns zero matches (reveal stays vanilla).
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>Hero section owns its video via ref, keeps the video-toggle as a sibling, and uses a scoped useGSAP wrapping the unchanged reveal observer.</done>
</task>

<task type="auto">
  <name>Task 2: Extract ManifestoSection (#nosotros)</name>
  <files>components/sections/manifesto-section.tsx</files>
  <read_first>
    - app/page.tsx (statement/#nosotros markup lines 112-142, including the statement-visual media-frame with data-cursor and the metric block)
    - components/experience.tsx (reveal observer core lines 20-32 — to reuse inside the scoped useGSAP)
    - .planning/phases/01-motion-foundation-architecture-cleanup/01-PATTERNS.md (sections analog + scoped reveal pattern)
  </read_first>
  <action>
    Create `components/sections/manifesto-section.tsx` as a `"use client"` component `ManifestoSection`. Copy the `<section className="statement section-pad" id="nosotros">...</section>` markup verbatim from `app/page.tsx` lines 112-142 (no logic/text change). Add `const root = useRef<HTMLElement>(null)` on the `<section>` and wrap the existing IntersectionObserver `[data-reveal]` setup in `useGSAP(() => { ... scoped to root ... return () => revealObserver.disconnect() }, { scope: root })`, same `threshold: 0.12`, `rootMargin: "0px 0px -8% 0px"`, same `.is-visible` toggle + `unobserve`. No new GSAP animation. This section uses no `services`/`process`/`Arrow` imports.
  </action>
  <verify>
    <automated>cd f:/ClaudeCode/Pagina_Web_Mayra/local-2 && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `components/sections/manifesto-section.tsx` starts with `"use client"`, exports `ManifestoSection`, contains `id="nosotros"`, `useGSAP`, and `rootMargin: "0px 0px -8% 0px"`.
    - `grep -n "gsap.from\|gsap.to\|ScrollTrigger.create" components/sections/manifesto-section.tsx` returns zero matches.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>Manifesto section extracted verbatim with a scoped, behavior-identical reveal.</done>
</task>

<task type="auto">
  <name>Task 3: Extract CapabilitiesSection (#capacidades) + moving-band</name>
  <files>components/sections/capabilities-section.tsx</files>
  <read_first>
    - app/page.tsx (capabilities/#capacidades markup lines 144-165, service-list `services.map` lines 154-163; moving-band block lines 167-172)
    - lib/site-content.ts (services array, created in plan 01)
    - components/arrow.tsx (Arrow, created in plan 01)
    - components/experience.tsx (reveal observer core lines 20-32)
    - .planning/phases/01-motion-foundation-architecture-cleanup/01-PATTERNS.md (sections analog; capabilities owns the moving-band)
  </read_first>
  <action>
    Create `components/sections/capabilities-section.tsx` as a `"use client"` component `CapabilitiesSection`. Copy the `<section className="capabilities section-pad" id="capacidades">...</section>` markup verbatim from `app/page.tsx` lines 144-165, AND the sibling `<div className="moving-band" aria-hidden="true">...</div>` block (lines 167-172) — return both inside a fragment so this component owns the moving-band. Import `services` from `@/lib/site-content` and `Arrow` from `@/components/arrow`; render the `services.map(...)` service-row list exactly as today (each `<a className="service-row" ... data-reveal data-cursor="Cotizar">`). Add a scoped reveal `useGSAP({ scope: root })` wrapping the unchanged IntersectionObserver `[data-reveal]` setup on a `useRef` attached to the `<section>`. No new GSAP animation; the moving-band stays pure CSS (Phase 4 / POLISH-03 territory — do not touch its animation).
  </action>
  <verify>
    <automated>cd f:/ClaudeCode/Pagina_Web_Mayra/local-2 && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `components/sections/capabilities-section.tsx` starts with `"use client"`, exports `CapabilitiesSection`, contains `id="capacidades"`, `services.map`, `moving-band`, and `useGSAP`.
    - Imports `services` from `@/lib/site-content` and `Arrow` from `@/components/arrow`.
    - `grep -n "gsap.from\|gsap.to\|ScrollTrigger.create" components/sections/capabilities-section.tsx` returns zero matches.
    - `npm run typecheck` and `npm run lint` exit 0.
  </acceptance_criteria>
  <done>Capabilities section + moving-band extracted verbatim, consuming shared services/Arrow, with a scoped behavior-identical reveal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Static section markup extraction + browser reveal observers; no untrusted-input boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Tampering (behavior drift) | scoped `useGSAP` reveal wrappers | mitigate | Each section wraps the EXISTING IntersectionObserver verbatim (same threshold/rootMargin/class), not a new gsap animation — enforced by the "zero gsap.from/ScrollTrigger" grep gate — preserving the zero-visual-change contract. |
| T-01-06 | Tampering | Hero video DOM position | accept | video-toggle rendered as a JSX sibling (not nested) to preserve the `position: absolute` containing block; residual visual risk (Assumption A1) is caught by plan 07's manual visual QA. |
</threat_model>

<verification>
Per D-02 no formal gate here; per-task `npm run typecheck`/`npm run lint`. Full gate + visual regression at phase end (plan 07), including explicit re-check of the video-toggle on-screen position.
</verification>

<success_criteria>
- Hero, Manifesto, Capabilities are independent `"use client"` section components with verbatim markup (ARCH-02).
- Each has a scoped `useGSAP` wrapping the unchanged reveal observer (no GSAP-native animation added).
- Hero video read via ref (no querySelector); video-toggle preserved as a sibling.
</success_criteria>

<output>
Create `.planning/phases/01-motion-foundation-architecture-cleanup/01-04-SUMMARY.md` when done.
</output>
