---
phase: 01-motion-foundation-architecture-cleanup
plan: 06
type: execute
wave: 3
depends_on: [01-03, 01-04, 01-05]
files_modified:
  - app/layout.tsx
  - app/page.tsx
  - components/experience.tsx
autonomous: true
requirements: [FOUND-01, FOUND-02, FOUND-03, ARCH-02]
must_haves:
  truths:
    - "SmoothScrollProvider is mounted exactly once at the root client boundary and owns the only Lenis instance"
    - "The old Experience component and its global reveal/parallax/cursor/menu listeners no longer exist, so no competing requestAnimationFrame loop remains"
    - "app/page.tsx is a composition root for the extracted chrome and section components while preserving the footer and LOCAL 2 badge markup"
    - "The rendered section order and all existing IDs remain unchanged"
  artifacts:
    - path: "app/layout.tsx"
      provides: "Single root mount for SmoothScrollProvider"
      contains: "SmoothScrollProvider"
    - path: "app/page.tsx"
      provides: "Composition-only page using extracted global chrome and section components"
      contains: "HeroSection"
  key_links:
    - from: "app/layout.tsx"
      to: "components/providers/smooth-scroll-provider.tsx"
      via: "wraps children once"
      pattern: "<SmoothScrollProvider>"
    - from: "app/page.tsx"
      to: "components/sections/*"
      via: "ordered component composition"
      pattern: "HeroSection|ManifestoSection|CapabilitiesSection|TechnologySection|ProcessSection|ContactSection"
---

<objective>
Perform the coordinated Phase 1 switchover: mount the unified SmoothScrollProvider, replace the monolithic Experience + inline section tree with the extracted components, and remove components/experience.tsx in the same change.

Purpose: Activate the new single-loop motion foundation without ever leaving the app in a dual-rAF state, and complete ARCH-02 while preserving the exact visual DOM order required by the Phase 1 UI contract.

Output: app/layout.tsx mounts one SmoothScrollProvider; app/page.tsx composes global chrome and six section components; components/experience.tsx is removed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-motion-foundation-architecture-cleanup/01-CONTEXT.md
@.planning/phases/01-motion-foundation-architecture-cleanup/01-RESEARCH.md
@.planning/phases/01-motion-foundation-architecture-cleanup/01-PATTERNS.md
@.planning/phases/01-motion-foundation-architecture-cleanup/01-UI-SPEC.md
@app/layout.tsx
@app/page.tsx
@components/experience.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mount SmoothScrollProvider at the root client boundary</name>
  <files>app/layout.tsx</files>
  <read_first>
    - app/layout.tsx (current server layout and exact body markup)
    - components/providers/smooth-scroll-provider.tsx (provider created in plan 02)
    - .planning/phases/01-motion-foundation-architecture-cleanup/01-RESEARCH.md (single ticker and SSR/client-boundary constraints)
    - .planning/phases/01-motion-foundation-architecture-cleanup/01-UI-SPEC.md (locked reduced-motion and zero-visual-change contract)
  </read_first>
  <action>
    Import the official recommended Lenis stylesheet (`lenis/dist/lenis.css`) next to the existing global stylesheet, and import SmoothScrollProvider from @/components/providers/smooth-scroll-provider in app/layout.tsx. Keep app/layout.tsx as a server component. Wrap only the existing {children} inside <SmoothScrollProvider>{children}</SmoothScrollProvider> within the body. Do not add a second provider, browser API, use client directive, or visual wrapper element.
  </action>
  <verify>
    <automated>cd f:/ClaudeCode/Pagina_Web_Mayra/local-2 &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - app/layout.tsx imports SmoothScrollProvider and renders it exactly once.
    - app/layout.tsx imports lenis/dist/lenis.css exactly once.
    - app/layout.tsx does not contain a use client directive.
    - grep -rn "&lt;SmoothScrollProvider" app/ components/ returns exactly one mount.
    - npm run typecheck exits 0.
  </acceptance_criteria>
  <done>The unified provider is mounted once from the server layout through a valid client boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Switch app/page.tsx to extracted components and remove Experience</name>
  <files>app/page.tsx, components/experience.tsx</files>
  <read_first>
    - app/page.tsx (current exact section order, footer, badge, force-static export)
    - components/experience.tsx (legacy global listeners that must disappear with the switchover)
    - components/intro-sequence.tsx
    - components/menu-overlay.tsx
    - components/custom-cursor.tsx
    - components/sections/hero-section.tsx
    - components/sections/manifesto-section.tsx
    - components/sections/capabilities-section.tsx
    - components/sections/technology-section.tsx
    - components/sections/process-section.tsx
    - components/sections/contact-section.tsx
    - .planning/phases/01-motion-foundation-architecture-cleanup/01-PATTERNS.md (app/page.tsx composition pattern and single-instance systems)
  </read_first>
  <action>
    Replace the Experience import and all inline section markup in app/page.tsx with imports and one render of IntroSequence, MenuOverlay, CustomCursor, HeroSection, ManifestoSection, CapabilitiesSection, TechnologySection, ProcessSection, and ContactSection. Preserve export const dynamic = "force-static". Preserve the footer and environment-badge JSX byte-for-byte. Preserve the main wrapper and exact section order: hero, manifesto, capabilities (including its moving band), technology, process, contact. Render the three global chrome components exactly once outside main. Remove now-unused next/image, services, process, Arrow, ContactForm, and Experience definitions/imports from page.tsx. Then remove components/experience.tsx entirely. Do not alter app/globals.css in this switchover.
  </action>
  <verify>
    <automated>cd f:/ClaudeCode/Pagina_Web_Mayra/local-2 &amp;&amp; npm run typecheck &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - components/experience.tsx no longer exists and grep -rn "components/experience\|&lt;Experience" app/ components/ returns zero matches.
    - app/page.tsx contains each extracted global chrome and section component exactly once.
    - app/page.tsx still contains export const dynamic = "force-static", className="footer", and className="environment-badge".
    - grep -rn "requestAnimationFrame" app/ components/ hooks/ returns matches only in hooks/use-legacy-parallax.ts.
    - grep -rn "new Lenis\|ReactLenis" app/ components/ hooks/ lib/ shows a single provider-owned Lenis integration.
    - npm run typecheck and npm run lint exit 0.
  </acceptance_criteria>
  <done>The page renders exclusively through extracted components, the old Experience loop is gone, and the visual section order/footer/badge remain intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server layout to client provider | A server component renders one client boundary that initializes browser-only motion libraries after hydration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Denial of Service (self-inflicted) | Motion runtime switchover | mitigate | Mount the provider and delete Experience's loop in the same plan; grep proves requestAnimationFrame exists only in the shared legacy hook. |
| T-01-08 | Tampering (render drift) | app/page.tsx composition | mitigate | Preserve section ordering, IDs, footer, badge, and component markup; full visual regression gate follows immediately in plan 07. |
</threat_model>

<verification>
Run npm run typecheck and npm run lint. Confirm structurally that there is one provider, one cursor, one intro, one menu, no Experience import, and only the shared legacy parallax requestAnimationFrame loop.
</verification>

<success_criteria>
- One root SmoothScrollProvider activates the GSAP-ticker-driven Lenis engine (FOUND-01/FOUND-03).
- Reduced-motion selection remains owned by the provider contract from plan 02 (FOUND-02).
- Experience is deleted and app/page.tsx is a clean composition root (ARCH-02).
- Static render order, footer, badge, and section IDs are preserved.
</success_criteria>

<output>
Create .planning/phases/01-motion-foundation-architecture-cleanup/01-06-SUMMARY.md when done.
</output>
