---
phase: 04-header-sticky-y-carrusel-de-equipos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hooks/use-header-scroll-state.ts
  - components/menu-overlay.tsx
  - app/globals.css
autonomous: true
requirements: [HEAD-01, HEAD-02]

must_haves:
  truths:
    - "El header gana fondo sólido blanco + sombra sutil al pasar 80px de scroll (clase .is-scrolled)"
    - "El header revierte a transparente/sin sombra al volver al tope de la página"
    - "No existe un segundo window.addEventListener('scroll') nuevo — el toggle usa ScrollTrigger.create sobre el ticker único existente"
    - "El header conserva position: fixed (no se introduce position: sticky)"
  artifacts:
    - path: "hooks/use-header-scroll-state.ts"
      provides: "Hook useHeaderScrollState que registra un único ScrollTrigger.create({toggleClass})"
      exports: ["useHeaderScrollState"]
      min_lines: 12
    - path: "components/menu-overlay.tsx"
      provides: "Llama useHeaderScrollState() en el cuerpo del componente que renderiza .site-header"
      contains: "useHeaderScrollState"
    - path: "app/globals.css"
      provides: "Regla .site-header.is-scrolled (background + box-shadow) y transición del header"
      contains: ".is-scrolled"
  key_links:
    - from: "components/menu-overlay.tsx"
      to: "hooks/use-header-scroll-state.ts"
      via: "useHeaderScrollState() call in component body"
      pattern: "useHeaderScrollState\\(\\)"
    - from: "hooks/use-header-scroll-state.ts"
      to: ".site-header"
      via: "ScrollTrigger toggleClass targets"
      pattern: "toggleClass"
---

<objective>
Dar al header fijo existente (`.site-header`) un estado visual reactivo al scroll (HEAD-01): fondo blanco sólido + sombra sutil al bajar más de 80px, revirtiendo al tope. Se implementa con un único `ScrollTrigger.create({ toggleClass })` registrado sobre el ticker Lenis+GSAP ya existente — sin un segundo listener de scroll, sin cambiar `position: fixed` a `sticky`.

Purpose: HEAD-01 exige feedback visual de scroll sobre el ticker único; HEAD-02 exige probarlo junto con drawer y carrusel (el gate de estrés completo se ejecuta en el Plan 02, cuando el carrusel ya existe).
Output: `hooks/use-header-scroll-state.ts` nuevo, `menu-overlay.tsx` cableado, reglas CSS `.site-header.is-scrolled` en `app/globals.css`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-header-sticky-y-carrusel-de-equipos/04-RESEARCH.md
@.planning/phases/04-header-sticky-y-carrusel-de-equipos/04-UI-SPEC.md
@lib/gsap.ts
@components/menu-overlay.tsx
@components/providers/smooth-scroll-provider.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Crear el hook use-header-scroll-state.ts</name>
  <files>hooks/use-header-scroll-state.ts</files>
  <read_first>
    - 04-RESEARCH.md "Pattern 1: Header scroll-reactive class toggle via a dedicated hook" — contiene el código exacto verificado contra la API de ScrollTrigger 3.15.0.
    - lib/gsap.ts — confirma que `useGSAP`, `gsap` y `ScrollTrigger` se exportan desde `@/lib/gsap` (ya registrados con `gsap.registerPlugin`).
    - components/providers/smooth-scroll-provider.tsx — confirma que `useLenis(ScrollTrigger.update)` ya sincroniza ScrollTrigger con Lenis cada tick; por eso no hace falta ningún listener nuevo.
  </read_first>
  <action>
    Crear `hooks/use-header-scroll-state.ts` como client component (`"use client"` en la primera línea). Importar `useGSAP` y `ScrollTrigger` desde `@/lib/gsap` (no desde `gsap` directo — la instancia registrada vive en `@/lib/gsap`). Definir una constante módulo-scope `HEADER_SCROLL_THRESHOLD = 80`. Exportar la función nombrada `useHeaderScrollState()` que dentro de un `useGSAP(() => { ... }, [])` registra EXACTAMENTE un `ScrollTrigger.create()` con esta forma verificada por 04-RESEARCH.md: `trigger: document.body`, `start: \`top -${HEADER_SCROLL_THRESHOLD}px\``, `end: "max"`, y `toggleClass: { targets: ".site-header", className: "is-scrolled" }`. Retornar desde el callback de useGSAP una función de limpieza que llama `trigger.kill()`. Reglas duras (HEAD-01): NO usar `window.addEventListener("scroll", ...)` ni ningún listener manual; NO usar `scrub`; NO usar `pin`. El `end: "max"` es obligatorio — sin él el toggle se apaga prematuramente cerca del fondo de la página (Pitfall 2 del research). Respetar estilo del proyecto: sin punto y coma, indentación de 2 espacios, alias `@/*`, export nombrado.
  </action>
  <verify>
    <automated>cd F:/ClaudeCode/Pagina_Web_Mayra/local-2 && npm run typecheck && grep -c 'end: "max"' hooks/use-header-scroll-state.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` pasa sin errores.
    - `grep -n 'ScrollTrigger.create' hooks/use-header-scroll-state.ts` devuelve exactamente 1 ocurrencia.
    - `grep -c 'end: "max"' hooks/use-header-scroll-state.ts` devuelve >= 1.
    - `grep -c 'addEventListener' hooks/use-header-scroll-state.ts` devuelve 0.
    - `grep -c 'scrub' hooks/use-header-scroll-state.ts` devuelve 0.
    - El archivo exporta `useHeaderScrollState` (export nombrado) y define `HEADER_SCROLL_THRESHOLD = 80`.
  </acceptance_criteria>
  <done>El hook existe, tipa correctamente, registra un único ScrollTrigger con `end: "max"` y `toggleClass` sobre `.site-header`, sin ningún listener de scroll manual.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Cablear el hook en MenuOverlay y añadir el CSS del estado scrolled</name>
  <files>components/menu-overlay.tsx, app/globals.css</files>
  <read_first>
    - components/menu-overlay.tsx — es el componente que renderiza `<header className="site-header">`; ya es `"use client"` y ya llama `useScrollLock(menuOpen)` en el cuerpo. Añadir la llamada al nuevo hook junto a esa.
    - 04-RESEARCH.md "Registering the header scroll hook in MenuOverlay" y "CSS additions (append to app/globals.css)" — muestran el punto de inserción exacto y las dos reglas CSS.
    - 04-UI-SPEC.md "Header Scroll-Reactive Contract" — valores exactos del box-shadow y la transición de 200ms.
    - app/globals.css — localizar la regla base `.site-header` (≈línea 21, `position: fixed`) para confirmar que NO se toca; las reglas nuevas se APENDIZAN, no reemplazan.
  </read_first>
  <action>
    En `components/menu-overlay.tsx`: importar `useHeaderScrollState` desde `@/hooks/use-header-scroll-state` y llamar `useHeaderScrollState()` en el cuerpo del componente, inmediatamente después de la línea `useScrollLock(menuOpen)`. NO tocar el JSX: `className="site-header"` queda igual; la clase `.is-scrolled` la aplica/quita ScrollTrigger, no estado React.
    En `app/globals.css`: APENDIZAR (no reescribir la regla base `.site-header` de position: fixed) dos reglas nuevas — (1) una transición en `.site-header` de `background` y `box-shadow` usando `var(--motion-duration-fast)` y `var(--ease-moderate)` (200ms, tier micro-interacción); (2) `.site-header.is-scrolled` con `background: var(--bg-surface)` y `box-shadow: 0 1px 0 var(--border-subtle), 0 8px 24px color-mix(in srgb, var(--ink-primary) 6%, transparent)`. Reglas duras (04-UI-SPEC "never used for" + Motion Contract): el estado scrolled es blanco, NUNCA acento; el header NO cambia de altura/padding al hacer scroll (solo background/shadow); NO introducir `position: sticky` en `.site-header`; NO añadir ninguna regla `overflow-x` nueva en ningún sitio; usar solo tokens CSS existentes, cero literales de color.
  </action>
  <verify>
    <automated>cd F:/ClaudeCode/Pagina_Web_Mayra/local-2 && npm run lint && npm run typecheck && grep -rn 'addEventListener("scroll"' components/ hooks/ && grep -c 'overflow-x' app/globals.css</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint && npm run typecheck` pasan sin errores.
    - `grep -c 'useHeaderScrollState()' components/menu-overlay.tsx` devuelve >= 1.
    - `grep -c '.site-header.is-scrolled' app/globals.css` devuelve >= 1.
    - `grep -rn 'addEventListener("scroll"' components/ hooks/` devuelve ÚNICAMENTE el hit preexistente en `hooks/use-legacy-parallax.ts` (cero listeners nuevos).
    - `grep -c 'overflow-x' app/globals.css` sigue devolviendo 1 (la ocurrencia preexistente en `body`); no se añadió ninguna regla overflow-x nueva.
    - `grep -c 'position: sticky' app/globals.css` no aumentó respecto a antes del cambio (sigue siendo solo `.tech-sticky`).
  </acceptance_criteria>
  <done>MenuOverlay llama al hook; el CSS `.is-scrolled` (blanco + sombra sutil, transición 200ms) existe apendizado; ningún listener de scroll nuevo; header sigue `position: fixed`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client scroll input → GSAP ScrollTrigger | Posición de scroll (no confiable en el sentido de entrada de usuario, pero sin persistencia ni ejecución) impulsa un toggle de clase puramente visual |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-H1 | Denial of Service | `ScrollTrigger.create` sobre document.body | accept | Superficie mínima; un único trigger registrado y con `kill()` en cleanup de useGSAP; no crea rAF loop competidor (piggyback en el ticker único existente). ASVS L1 no exige control. |
| T-04-SC | Tampering | npm installs | accept | Este plan no instala ningún paquete (`gsap`/`@gsap/react` ya instalados y vetados en milestones previos). Sin nuevas dependencias, sin superficie supply-chain nueva. |
</threat_model>

<verification>
- `npm run lint && npm run typecheck` limpios tras ambas tareas.
- `npm run build` (gate de wave) — verifica que el path `force-static` sigue prerenderizando con el nuevo hook client.
- Grep gate HEAD-01: `grep -rn 'addEventListener("scroll"' components/ hooks/` → solo `use-legacy-parallax.ts`.
- Verificación manual del header (subconjunto de HEAD-02, gate completo en Plan 02): cargar la página, hacer scroll >80px → `.site-header` gana `.is-scrolled` (fondo blanco + sombra); volver al tope → revierte; flick-scroll rápido pasando 80px y de vuelta → sin flicker.
</verification>

<success_criteria>
- HEAD-01 cumplido: `.site-header` obtiene estado scrolled reactivo vía `ScrollTrigger.create({ toggleClass })`, cero listeners de scroll nuevos.
- Header conserva `position: fixed`; sin `overflow-x` nuevo; sin cambio de altura al hacer scroll.
- Estado scrolled es blanco + sombra sutil, nunca acento.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)

- **New file:** `hooks/use-header-scroll-state.ts` — export nombrado `useHeaderScrollState()`.
- **New symbol:** `useHeaderScrollState` (hook), constante `HEADER_SCROLL_THRESHOLD = 80` (módulo-scope).
- **New CSS class/state:** `.site-header.is-scrolled` (background `--bg-surface` + box-shadow), más una transición añadida a `.site-header`.
- **Modified:** `components/menu-overlay.tsx` (llamada al hook), `app/globals.css` (reglas apendizadas).
</artifacts_produced>

<output>
Create `.planning/phases/04-header-sticky-y-carrusel-de-equipos/04-01-SUMMARY.md` when done
</output>
