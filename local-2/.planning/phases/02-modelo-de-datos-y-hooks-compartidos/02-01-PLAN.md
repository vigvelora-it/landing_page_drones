---
phase: 02-modelo-de-datos-y-hooks-compartidos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/site-content.ts
autonomous: true
requirements: [ARCH-01]
must_haves:
  truths:
    - "lib/site-content.ts exports a typed services array with exactly the 5 real ejes de servicio (verbatim from BRAND-CONTENT.md)"
    - "lib/site-content.ts exports a typed team array with the 4 real geologos fundadores (verbatim names, roles, bios)"
    - "lib/site-content.ts exports a typed projects array with the 3 real projects (GESAC, Lezard, Las Dunas)"
    - "lib/site-content.ts exports a typed brochure metadata constant"
    - "capabilities-section.tsx still compiles and renders because services keeps number/title/detail field names"
  artifacts:
    - path: "lib/site-content.ts"
      provides: "Typed brand content: Service/ServiceGroup/TeamMember/Project/Brochure interfaces + services/team/teamIntro/projects/brochure exports (process unchanged)"
      contains: "export const services"
    - path: "lib/site-content.ts"
      provides: "team export"
      contains: "export const team"
  key_links:
    - from: "components/sections/capabilities-section.tsx"
      to: "lib/site-content.ts"
      via: "import { services }, reads service.number/title/detail"
      pattern: "service\\.(number|title|detail)"
---

<objective>
Extend `lib/site-content.ts` into the typed, real brand-content data module for SkyTech (ARCH-01). Replace the 6 leftover generic drone-service rows with the 5 real ejes de servicio, and add the 4 geologos fundadores, the 3 real projects, and brochure metadata — all using the CANONICAL verbatim wording from `.planning/BRAND-CONTENT.md`.

Purpose: This module is consumed at build time (`force-static`) and by the client section components. Phase 3's service drawer and Phase 5's team/projects sections will read directly from these exports, so the shape and content must be right now to avoid a breaking change later.
Output: Typed interfaces + populated `services`, `team`, `teamIntro`, `projects`, `brochure` exports. `process` unchanged.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/BRAND-CONTENT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/site-content.ts
@components/sections/capabilities-section.tsx
</context>

<artifacts_this_phase_produces>
This plan creates the following new symbols in `lib/site-content.ts` (Phase 3/5 consume them):

- Interfaces: `ServiceGroup`, `Service`, `TeamMember`, `Project`, `Brochure`
- `Service` fields: `id`, `number` (PRESERVED), `title` (PRESERVED), `detail` (PRESERVED), `tagline`, `groups: ServiceGroup[]`, `note?`, `icon?`, `image?`
- `ServiceGroup` fields: `heading?`, `items: string[]`
- Exports: `services: Service[]` (re-typed, now 5 entries), `team: TeamMember[]`, `teamIntro: string`, `projects: Project[]`, `brochure: Brochure`
- `process` export is unchanged (still exported).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Type the Service model and replace services with the 5 real ejes (verbatim)</name>
  <files>lib/site-content.ts</files>
  <read_first>
    - lib/site-content.ts — current untyped `services` (6 generic rows) and `process` exports; note NO semicolons, 2-space indent, no default exports.
    - components/sections/capabilities-section.tsx — the ONLY consumer of `services`; destructures `service.number`, `service.title`, `service.detail` and uses `key={service.number}`. These three field names MUST be preserved or `npm run typecheck` breaks (Pitfall 1 in 02-RESEARCH.md).
    - .planning/BRAND-CONTENT.md section "Los 5 ejes de servicio (contenido completo)" — canonical verbatim source for every eje's title, tagline, bullets, sub-group headings, and cliente notes.
  </read_first>
  <action>
    Define TypeScript interfaces above the data (PascalCase, per CONVENTIONS.md):
    - `ServiceGroup` with `heading?: string` and `items: string[]`.
    - `Service` with `id: string`, `number: string`, `title: string`, `detail: string`, `tagline: string`, `groups: ServiceGroup[]`, `note?: string`, `icon?: string`, `image?: string`. Include `icon?`/`image?` as optional now per the orchestrator-resolved question (Phase 3's drawer may populate them; leave them unset in this phase).
    Replace the current 6-entry `services` literal with `export const services: Service[]` containing EXACTLY the 5 real ejes in order, using BRAND-CONTENT.md verbatim text:
    1. id `"topografia-drones"`, number `"01"`, title `"Topografía y Tecnología con Drones"`
    2. id `"geotecnia-riesgos"`, number `"02"`, title `"Geotecnia y Riesgos Geológicos"`
    3. id `"mineria-consultoria"`, number `"03"`, title `"Minería: Consultoría y Formalización"`
    4. id `"obras-civiles"`, number `"04"`, title `"Obras Civiles e Infraestructura Vial"`
    5. id `"servicios-complementarios"`, number `"05"`, title `"Servicios Complementarios"`
    For each eje: set `tagline` to the client's verbatim "Tagline" line, and set `detail` to that SAME verbatim tagline string (detail is the short line capabilities-section already renders — the tagline is the client's own concise summary, so no copy is fabricated). Put every bullet into `groups`: ejes 1 and 2 use a single group `{ items: [...] }` with NO heading (flat lists); ejes 3, 4, 5 use one group per client sub-heading (e.g. "Consultoría para pequeños productores mineros:", "Formalización de mineros artesanales (REINFO):", etc.) with `heading` set to that verbatim sub-heading and `items` the verbatim bullets under it. Add `note` only where the client wrote a "Nota del cliente" (eje 1 and eje 5) using the verbatim note text. Do NOT paraphrase, shorten, translate, or invent any bullet. Do NOT set `icon`/`image` values.
    Preserve the existing `process` export exactly as-is.
  </action>
  <verify>
    <automated>cd F:/ClaudeCode/Pagina_Web_Mayra/local-2 && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` passes (proves capabilities-section.tsx still compiles against preserved number/title/detail).
    - `grep -c "id:" lib/site-content.ts` shows the 5 service ids present; `grep -q "Servicios Complementarios" lib/site-content.ts` succeeds.
    - `services` has exactly 5 entries (no leftover generic rows like "Fotogrametría aérea" / "Nubes de puntos LiDAR": `grep -q "Nubes de puntos LiDAR" lib/site-content.ts` returns non-zero / no match).
    - Interfaces `Service` and `ServiceGroup` are declared and exported.
  </acceptance_criteria>
  <done>`services` is a typed `Service[]` of the 5 real ejes with verbatim taglines, grouped bullets, and client notes; typecheck green; capabilities-section untouched and still compiling.</done>
</task>

<task type="auto">
  <name>Task 2: Add typed team, projects, and brochure exports (verbatim)</name>
  <files>lib/site-content.ts</files>
  <read_first>
    - .planning/BRAND-CONTENT.md sections "Equipo técnico (4 geólogos fundadores)" (the lead quote + the 4 named members with role and bio) and "Proyectos reales" (the 3-row table) and "Brochure descargable" (note that no real PDF exists yet).
    - lib/site-content.ts — the file as left by Task 1 (append below `services`, keep `process` last or grouped consistently; match no-semicolon 2-space style).
  </read_first>
  <action>
    Define interfaces: `TeamMember` with `id: string`, `name: string`, `role: string`, `bio: string`, `photo?: string`; `Project` with `id: string`, `name: string`, `client: string`, `location: string`, `service: string`, `featured?: boolean`; `Brochure` with `title: string`, `href: string`, `fileSizeLabel?: string`.
    Add `export const teamIntro: string` set to the client's verbatim lead quote ("SKYTECH SOLUTIONS está conformada por un equipo fundador de cuatro ingenieros geólogos...").
    Add `export const team: TeamMember[]` with the 4 real members, verbatim, in BRAND-CONTENT.md order:
    - `paulo-rodriguez` — name "Paulo Alfredo Rodríguez Coronado", role "Ingeniero Geólogo Senior · Gerente General", bio the verbatim paragraph.
    - `harold-navarro` — name "Harold Jesús Navarro Saavedra", role "Ingeniero Geólogo Senior · Sub Gerente", bio verbatim.
    - `luis-alban` — name "Luis Alberto Alban Zapata", role "Ingeniero Geólogo Senior · Socio Fundador", bio verbatim.
    - `juan-ruiz` — name "Juan Carlos Ruiz Gonzales", role "Ingeniero Geólogo Project · Socio Fundador", bio verbatim.
    Do NOT set `photo` (the real asset arrives in Phase 5; leave undefined rather than point at a non-existent/remote path — CLAUDE.md forbids remote images and a fake local path would 404).
    Add `export const projects: Project[]` with the 3 real projects verbatim from the table, `featured: true` on GESAC only (for Phase 5's "proyecto destacado + lista" layout):
    - `gesac-huarmey` — name "Proyecto GESAC", client "German Engineering & Cie. S.A.C", location "Huarmey, Ancash", service "Levantamiento Aerofotogramétrico", featured true.
    - `lezard-huaral` — name "Proyecto Lezard", client "Black Swan Minerals S.A.C.", location "Huaral, Lima", service "Levantamiento Aerofotogramétrico".
    - `las-dunas-piura` — name "Proyecto Las Dunas", client "Asociación Las Dunas Ecological", location "Castilla, Piura", service "Levantamiento Aerofotogramétrico".
    Add `export const brochure: Brochure` with title "Brochure SkyTech Solutions" and href "/brochures/skytech-solutions-brochure.pdf" (placeholder path — the actual PDF is a Phase 5 / BROCH-01 deliverable; nothing renders this link in Phase 2, so the path harmlessly 404s until then). Leave `fileSizeLabel` unset.
  </action>
  <verify>
    <automated>cd F:/ClaudeCode/Pagina_Web_Mayra/local-2 && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` and `npm run lint` both pass.
    - All 4 real surnames present: `grep -q "Rodríguez Coronado" lib/site-content.ts` and `grep -q "Navarro Saavedra" lib/site-content.ts` and `grep -q "Alban Zapata" lib/site-content.ts` and `grep -q "Ruiz Gonzales" lib/site-content.ts` each succeed.
    - `grep -q "German Engineering" lib/site-content.ts` and `grep -q "Black Swan Minerals" lib/site-content.ts` and `grep -q "Las Dunas Ecological" lib/site-content.ts` each succeed.
    - `grep -q "export const team" lib/site-content.ts`, `grep -q "export const projects" lib/site-content.ts`, `grep -q "export const brochure" lib/site-content.ts` each succeed.
    - No `photo:` value points to `http` (no remote asset): `grep "photo:" lib/site-content.ts | grep -q "http"` returns no match.
  </acceptance_criteria>
  <done>team (4 verbatim members + teamIntro quote), projects (3 verbatim, GESAC featured), and brochure metadata are exported and typed; lint + typecheck green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan only adds static, developer-authored content constants. No user input, no network calls, no new rendering path in this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | lib/site-content.ts | accept | Content is public marketing copy intended for the site; no secrets/PII beyond already-public company contact info. |
| T-02-02 | Tampering (XSS via content) | future consumers (Phase 3/5) | accept | This phase renders nothing new. When Phase 3/5 render this data, standard JSX text interpolation (auto-escaping) is used — never `dangerouslySetInnerHTML`. No package installs in this plan (T-02-SC N/A). |
</threat_model>

<verification>
- `npm run typecheck` green (guards against the Pitfall 1 field-rename regression in capabilities-section.tsx).
- `npm run lint` green.
- Verbatim-content grep checks in each task's acceptance_criteria pass.
</verification>

<success_criteria>
`lib/site-content.ts` exports typed, verbatim, real brand content for the 5 ejes de servicio (with grouped detail bullets), the 4 geologos fundadores, the 3 real projects, and brochure metadata — satisfying ARCH-01 and Phase 2 success criterion #1 — while keeping capabilities-section.tsx compiling unmodified.
</success_criteria>

<output>
Create `.planning/phases/02-modelo-de-datos-y-hooks-compartidos/02-01-SUMMARY.md` when done.
</output>
