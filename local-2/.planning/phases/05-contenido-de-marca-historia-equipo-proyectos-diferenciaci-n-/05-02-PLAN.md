---
phase: 05-contenido-de-marca-historia-equipo-proyectos-diferenciaci-n
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - lib/site-content.ts
  - components/sections/projects-section.tsx
  - app/page.tsx
  - components/menu-overlay.tsx
  - components/sections/process-section.tsx
  - components/sections/contact-section.tsx
  - app/globals.css
autonomous: true
requirements: [PROJ-01, DIFF-01]
requirements_blocked: [BROCH-01]
user_setup: []

must_haves:
  truths:
    - "#proyectos presenta GESAC como proyecto destacado y Lezard/Las Dunas con igual peso de soporte"
    - "Cada proyecto muestra siempre Cliente, Ubicación y Servicio realizado; no muestra resultados, logos, fechas o imágenes sin procedencia"
    - "La diferenciación usa la ventaja y mensaje verbatim más cuatro celdas de evidencia derivadas solo de datos canónicos"
    - "Ningún nombre de competidor aparece en app, components, lib, DOM visible, accesibilidad, metadata o comentarios"
    - "El menú y kickers forman seis destinos coherentes: 01 Nosotros, 02 Capacidades, 03 Tecnología, 04 Proyectos, 05 Proceso, 06 Contacto"
    - "Mientras no exista el PDF real, no hay anchor, botón, href falso ni control de brochure en el DOM; BROCH-01 permanece bloqueado"
    - "La página completa conserva header, drawer, carrusel y reduced-motion sin regresiones ni overflow en desktop/tablet/mobile"
  artifacts:
    - path: "lib/site-content.ts"
      provides: "Objeto differentiation exacto; projects existentes siguen siendo la fuente única"
      exports: ["differentiation", "projects"]
      contains: "export const differentiation"
    - path: "components/sections/projects-section.tsx"
      provides: "Sección #proyectos con featured/supporting, facts, evidencia y diferenciación"
      exports: ["ProjectsSection"]
      min_lines: 90
    - path: "app/page.tsx"
      provides: "ProjectsSection dentro del InertBoundary entre Technology y Process"
      contains: "<ProjectsSection"
    - path: "components/menu-overlay.tsx"
      provides: "Seis enlaces de navegación numerados"
      contains: "#proyectos"
    - path: "app/globals.css"
      provides: "Layout responsive 7/5 de proyectos, evidence 4/2/1 y menú de seis filas"
      contains: ".projects-section"
  key_links:
    - from: "components/sections/projects-section.tsx"
      to: "lib/site-content.ts"
      via: "imports de projects, differentiation y team para evidencia derivada"
      pattern: "from \"@/lib/site-content\""
    - from: "app/page.tsx"
      to: "components/sections/projects-section.tsx"
      via: "render único entre TechnologySection y ProcessSection dentro de InertBoundary"
      pattern: "<ProjectsSection"
    - from: "components/menu-overlay.tsx"
      to: "components/sections/projects-section.tsx#proyectos"
      via: "anchor de navegación 04"
      pattern: "href=\"#proyectos\""
---

<objective>
Añadir la sección de proyectos y diferenciación basada exclusivamente en evidencia canónica, integrarla en la página y convertir la navegación a seis destinos. PROJ-01 y DIFF-01 deben quedar completos. BROCH-01 debe quedar visiblemente omitido y documentado como bloqueado: falta el PDF real y no se autoriza fabricar o exponer un control roto.

Purpose: cerrar todo el contenido ejecutable de Fase 5 sin falsear resultados, imágenes, competidores ni descargas, preservando la arquitectura interactiva construida en Fases 3–4.
Output: `ProjectsSection`, datos de diferenciación, CSS responsive, wiring dentro de InertBoundary y navegación/kickers 01–06 coherentes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/BRAND-CONTENT.md
@.planning/phases/05-contenido-de-marca-historia-equipo-proyectos-diferenciaci-n-/05-RESEARCH.md
@.planning/phases/05-contenido-de-marca-historia-equipo-proyectos-diferenciaci-n-/05-UI-SPEC.md
@.planning/phases/05-contenido-de-marca-historia-equipo-proyectos-diferenciaci-n-/05-VALIDATION.md
@.planning/phases/05-contenido-de-marca-historia-equipo-proyectos-diferenciaci-n-/05-01-SUMMARY.md
@lib/site-content.ts
@app/page.tsx
@components/menu-overlay.tsx
@components/sections/process-section.tsx
@components/sections/contact-section.tsx
@app/globals.css
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Modelar diferenciación y construir ProjectsSection con evidencia canónica</name>
  <files>lib/site-content.ts, components/sections/projects-section.tsx</files>
  <read_first>
    - `.planning/BRAND-CONTENT.md` "Proyectos reales" y "Posicionamiento competitivo" — copiar solo ventaja principal y mensaje principal verbatim; los nombres de competidores nunca entran al código de página.
    - `05-UI-SPEC.md` "Projects — Featured + List", "Differentiation Based on Evidence", accessibility y canonical appendix.
    - `05-RESEARCH.md` "ProjectsSection", "Recommended Data Model Additions" y Pitfalls 4–6.
    - `lib/site-content.ts` — reusar `projects`/`team`, no duplicar proyectos ni importar/renderizar `brochure`.
  </read_first>
  <action>
    En `lib/site-content.ts`, añadir `export const differentiation = { advantage, message }` con los dos textos exactos de BRAND-CONTENT. No añadir nombres de competidores, claim de superioridad, precios, resultados, años combinados ni cobertura nacional.
    Crear `components/sections/projects-section.tsx` como client component, export nombrado `ProjectsSection`, con patrón local de `useRef` + `useGSAP` + IntersectionObserver cleanup. Renderizar `<section id="proyectos" className="projects-section section-pad">`, kicker `04 / Proyectos` y `<h2>Proyectos</h2>`. Separar `featuredProject = projects.find(project.featured)` y `supportingProjects = projects.filter(!featured)`; fallar explícitamente en desarrollo si no hay exactamente un featured, sin depender de `projects[0]`. GESAC ocupa `<article className="project-card project-card--featured">`; Lezard/Las Dunas se mapean como dos articles iguales. Cada uno usa `<dl>` con labels visibles exactos `Cliente`, `Ubicación`, `Servicio realizado`; no renderizar resultado, entregables, testimonial, logo, fecha, tamaño ni imagen.
    Después, dentro de la misma sección, renderizar `<h3>Diferenciación</h3>`, `differentiation.advantage` como texto principal, cuatro celdas de evidencia: `team.length / Ingenieros Geólogos`, `projects.length / Proyectos reales`, `2024 / Fundación`, y ubicaciones canónicas agrupadas `Huarmey · Huaral · Castilla` / `Ancash · Lima · Piura`; cerrar con `differentiation.message`. No importar `brochure`, no renderizar CTA/título/estado de bloqueo y no usar `dangerouslySetInnerHTML`.
  </action>
  <verify>
    <automated>Set-Location 'F:\ClaudeCode\Pagina_Web_Mayra\local-2'; npm.cmd run lint; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm.cmd run typecheck; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; rg -n "export const differentiation|featuredProject|supportingProjects|project-facts|Cliente|Ubicación|Servicio realizado" lib/site-content.ts components/sections/projects-section.tsx; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; rg -n "ARQUIDRON|JE [^A-Za-z0-9] WJ|GeoXPert|Norte Urbano" app components lib; if ($LASTEXITCODE -eq 0) { exit 1 }; if ($LASTEXITCODE -gt 1) { exit $LASTEXITCODE }; rg -n "brochure|download|skytech-solutions-brochure\.pdf" app components; if ($LASTEXITCODE -eq 0) { exit 1 }; if ($LASTEXITCODE -gt 1) { exit $LASTEXITCODE }; rg -n "Resultado|Entregables" components/sections/projects-section.tsx; if ($LASTEXITCODE -eq 0) { exit 1 }; if ($LASTEXITCODE -gt 1) { exit $LASTEXITCODE }; exit 0</automated>
  </verify>
  <acceptance_criteria>
    - Lint/typecheck pasan.
    - `differentiation` contiene solo ventaja y mensaje verbatim.
    - ProjectsSection deriva exactamente un featured y dos supporting desde `projects`, con tres `<dl>` completos.
    - Evidencia contiene solo 4, 3, 2024 y ubicaciones canónicas; no resultados/claims añadidos.
    - Cero competidores en `app/`, `components/`, `lib/`; la única referencia permitida a brochure/ruta futura sigue siendo el dato preexistente en `lib/site-content.ts`, no el componente.
  </acceptance_criteria>
  <done>ProjectsSection presenta los tres casos y diferenciación exacta con evidencia canónica, sin imágenes/claims/competidores/descarga inventados.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Estilizar e insertar Proyectos dentro del boundary coordinado</name>
  <files>components/sections/projects-section.tsx, app/page.tsx, app/globals.css</files>
  <read_first>
    - `05-UI-SPEC.md` layouts Projects/Differentiation y tabla responsive — 7/5 desktop, stacked tablet/mobile, evidence 4/2/1.
    - `05-RESEARCH.md` "Composition layer", "ProjectsSection" y "CSS Composition" — debe entrar dentro del segundo `InertBoundary` entre Technology y Process.
    - `app/page.tsx` — preservar exclusión/inert del drawer; no crear un boundary adicional.
    - `app/globals.css` — usar solo tokens/escala existentes y conservar una única regla global overflow-x.
  </read_first>
  <action>
    Añadir CSS de proyectos usando solo tokens existentes: `.projects-layout` en 7/5 desktop; `.project-supporting` con dos filas iguales; cards con superficies tokenizadas, border sutil, 24/32px padding y `min-width:0`; facts siempre visibles; diferenciación como banda estática; `.evidence-grid` 4 columnas desktop, 2x2 tablet, 1 columna mobile. Tipografía/gaps exactos del UI-SPEC, párrafos máximo 68ch, sin colores literales, hover-only, scroll horizontal, nuevas animaciones o `overflow-x` global.
    En `app/page.tsx`, importar/renderizar `ProjectsSection` exactamente una vez después de `TechnologySection` y antes de `ProcessSection`, dentro del mismo `InertBoundary` que ya contiene Technology/Process/Contact. No alterar Capabilities/ServiceDrawer, header, carrusel ni su coordinación. Confirmar que con drawer abierto Projects queda inert como el resto del fondo.
  </action>
  <verify>
    <automated>Set-Location 'F:\ClaudeCode\Pagina_Web_Mayra\local-2'; npm.cmd run typecheck; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; rg -n "ProjectsSection" app/page.tsx; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; rg -n "\.projects-layout|\.project-card--featured|\.evidence-grid" app/globals.css; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; $overflowCount = (rg -n "overflow-x" app/globals.css | Measure-Object -Line).Lines; if ($LASTEXITCODE -gt 1 -or $overflowCount -ne 1) { exit 1 }; exit 0</automated>
  </verify>
  <acceptance_criteria>
    - Typecheck pasa; ProjectsSection tiene un import y un render único.
    - Orden de JSX dentro del boundary: TechnologySection → ProjectsSection → ProcessSection.
    - Desktop usa 7/5, tablet/mobile stack; evidence usa 4/2/1; copy no se trunca.
    - `rg -n "overflow-x" app/globals.css` mantiene una única ocurrencia en body.
    - Ningún control de brochure aparece en ProjectsSection o page.
  </acceptance_criteria>
  <done>Proyectos queda integrado en el flujo y en el boundary del drawer con composición responsive y sin alterar las interacciones existentes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Renumerar seis destinos y ejecutar el gate integral de Fase 5</name>
  <files>components/menu-overlay.tsx, components/sections/process-section.tsx, components/sections/contact-section.tsx, app/globals.css</files>
  <read_first>
    - `05-UI-SPEC.md` "Top-level numbered kickers" y responsive/menu acceptance criteria — secuencia exacta 01–06; nunca ocultar destinos.
    - `05-RESEARCH.md` "Navigation and numbering", Pitfall 7 y Browser Matrix.
    - `components/menu-overlay.tsx` — sustituir `Perspectiva` por `Nosotros`, insertar Proyectos 04, renumerar Proceso/Contacto.
    - `components/sections/process-section.tsx` y `contact-section.tsx` — solo cambiar kickers 04→05 y 05→06; no reescribir contenido ni formulario.
    - `app/globals.css` reglas `.menu-overlay nav` — añadir nth-child(6) y ajustar solo font-size/padding responsive si seis filas colisionan a 390x844/320x568.
    - `05-VALIDATION.md` — gates estructurales, cuatro viewports, menú, no-overflow, reduced-motion, brochure omitido y regresión header/drawer/carrusel.
  </read_first>
  <action>
    Actualizar el menú a exactamente seis anchors: `01 Nosotros #nosotros`, `02 Capacidades #capacidades`, `03 Tecnología #tecnologia`, `04 Proyectos #proyectos`, `05 Proceso #proceso`, `06 Contacto #contacto`. Reemplazar los delays actuales de 70ms y definir explícitamente `nth-child(2)` a 60ms, `nth-child(3)` a 120ms, `nth-child(4)` a 180ms, `nth-child(5)` a 240ms y `nth-child(6)` a 300ms, de modo que el stagger incremental efectivo nunca supere `--motion-stagger-max:60ms`; ajustar en media queries solo display size/padding/alineación del menú si hace falta para que seis filas y `.menu-meta` quepan a 390x844 y 320x568, sin ocultar enlaces ni crear scroll horizontal. Cambiar kicker de Process a 05 y Contact a 06, sin tocar `ContactForm` o `/api/contact`.
    Ejecutar secuencialmente `npm.cmd run lint`, `npm.cmd run typecheck`, `npm.cmd run build`, iniciar `npm.cmd run start` local y probar con Playwright/manual en 1440x900, 1000x800, 390x844 y 320x568: `scrollWidth===clientWidth`; un h1; h2/h3 ordenados; seis enlaces tabulables y cada uno aterriza en un anchor único; seis valores y cuatro bios completos; GESAC destacado y dos support; facts completos; cuatro placeholders y cero team img; cero `[download]`; cero competidores en DOM; sin errores de consola. Hacer stress smoke de header scroll, drawer open/close con fondo inert, `.tech-sticky` y carrusel teclado/buttons/dots/drag; bajo reduced-motion contenido visible y controles usables. BROCH-01 permanece unchecked/bloqueado por PDF faltante y el summary usa `requirements-completed: [PROJ-01, DIFF-01]`, `requirements-blocked: [BROCH-01]`.
  </action>
  <verify>
    <automated>Set-Location 'F:\ClaudeCode\Pagina_Web_Mayra\local-2'; npm.cmd run lint; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm.cmd run typecheck; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm.cmd run build; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; rg -n "href=\"#(nosotros|capacidades|tecnologia|proyectos|proceso|contacto)\"" components/menu-overlay.tsx; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; rg -n "download|brochure\.href|skytech-solutions-brochure\.pdf" app components; if ($LASTEXITCODE -eq 0) { exit 1 }; if ($LASTEXITCODE -gt 1) { exit $LASTEXITCODE }; exit 0</automated>
    <human-check>
      <test>Servir el build local y recorrer la matriz 1440x900, 1000x800, 390x844 y 320x568, incluyendo menú completo, anchors, contenido, header, drawer, sticky, carrusel y reduced-motion.</test>
      <expected>Seis destinos caben y funcionan; proyectos/evidencia/copy completos; cero overflow/errores/competidores/download; interacciones previas sin regresiones.</expected>
      <why_human>Jerarquía visual, fit del menú, navegación real y stress interactivo requieren navegador.</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - Lint, typecheck y build pasan en secuencia.
    - Menú tiene exactamente seis anchors/labels/números y cada destino tiene un único id.
    - Kicker Proyectos=04, Proceso=05, Contacto=06.
    - Cuatro viewports sin overflow; seis filas no chocan con menu-meta; consola sin errores.
    - PROJ-01 y DIFF-01 pasan gates DOM/copy/evidencia.
    - `app/` y `components/` tienen cero `download`, `brochure.href` y ruta PDF; no existe CTA falso.
    - BROCH-01 queda bloqueado/unckecked por falta de PDF real, sin marcarlo completo.
  </acceptance_criteria>
  <done>Fase 5 ejecutable queda integrada y verificada: proyectos/diferenciación completos, navegación 01–06 coherente, sin regresiones; brochure honestamente bloqueado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| datos de proyectos/diferenciación → DOM público | Hechos del cliente deben conservar integridad sin claims o resultados agregados |
| planning competitivo → bundle/DOM/accesibilidad | Los competidores existen solo como contexto interno y no deben filtrarse al sitio |
| ruta futura de brochure → control de descarga | Una ruta sin archivo podría engañar al usuario o devolver HTML/404 como si fuese PDF |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering / Integrity | ProjectsSection | mitigate | Renderizar únicamente `projects` tipados y labels canónicos; cero resultado/entregable/imagen no confirmada; browser QA comprueba tres `<dl>`. |
| T-05-05 | Information Disclosure | Copy competitivo | mitigate | No copiar nombres desde planning a `app/`, `components/` o `lib/`; grep por los cuatro nombres y auditoría de `document.body.innerText`/DOM accesible. |
| T-05-06 | Spoofing / Tampering | Brochure download | mitigate | Omitir por completo anchor/button/control mientras falta el PDF; BROCH-01 sigue unchecked. Solo un plan futuro con asset real podrá verificar status 200, Content-Type y magic bytes `%PDF-`. |
| T-05-07 | Injection | Datos estáticos de proyectos | accept | Constantes TypeScript developer-authored renderizadas como texto escapado por React; sin CMS/input y sin `dangerouslySetInnerHTML`. |
</threat_model>

<source_coverage>
## Multi-Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | historia/misión/visión/valores | 05-01 Tasks 1–3 |
| GOAL | 4 fundadores | 05-01 Task 2; foto queda bloqueada por insumo ausente, no omitida ni falseada |
| GOAL | 3 proyectos + diferenciación | 05-02 Tasks 1–2 |
| GOAL | brochure descargable | 05-02 Tasks 1/3 registran bloqueo legítimo por PDF inexistente y prohíben control falso; futuro plan solo tras recibir asset |
| REQ BRAND-01 | copy canónico completo | 05-01 |
| REQ TEAM-01 | foto/nombre/cargo/bio | nombre/cargo/bio + composición cubiertos en 05-01; cuatro fotos bloqueadas por información/asset ausente |
| REQ PROJ-01 | featured + lista con facts | 05-02 Tasks 1–2 |
| REQ DIFF-01 | evidencia sin competidores | 05-02 Tasks 1/3 |
| REQ BROCH-01 | PDF estático real | bloqueado por asset externo ausente; se mantiene unchecked y sin href falso |
| RESEARCH | no-JS, responsive 4 viewports, six-menu, reduced-motion, regressions | 05-01 Task 3; 05-02 Task 3 |
| CONTEXT | no existe CONTEXT.md para esta fase | N/A; UI-SPEC aprobado + BRAND-CONTENT son autoridad |

No hay items silenciosamente omitidos. TEAM-01 y BROCH-01 son dependencias externas explícitas por archivos faltantes, la única causa legítima de cobertura parcial según el contrato planner.
</source_coverage>

<verification>
- Gates secuenciales: lint, typecheck, build; sin ejecución concurrente sobre `.next`.
- Estructural: un anchor por seis destinos, cero controles brochure, cero competidores en código de página, una sola regla global overflow-x.
- DOM/browser: tres proyectos/facts, cuatro evidencias, jerarquía headings, fit/anchors de menú y no overflow en cuatro viewports.
- Regresión: header, drawer/inert, sticky, carrusel y reduced-motion continúan operativos; no se despliega.
</verification>

<success_criteria>
- PROJ-01 completo con GESAC featured y Lezard/Las Dunas support, todos con cliente/ubicación/servicio.
- DIFF-01 completo con datos canónicos y cero competidores en página/bundle accesible.
- Navegación/kickers coherentes 01–06 y layout íntegro 1440/1000/390/320.
- BROCH-01 sigue bloqueado hasta PDF real; ninguna simulación de descarga aparece en DOM.
</success_criteria>

<artifacts_produced>
- `components/sections/projects-section.tsx` exporta `ProjectsSection`.
- `lib/site-content.ts` exporta `differentiation`.
- `app/page.tsx` integra Proyectos dentro del InertBoundary.
- `components/menu-overlay.tsx` contiene seis destinos; Process/Contact quedan numerados 05/06.
- `app/globals.css` contiene proyectos/evidence responsive y ajuste seguro del menú de seis filas.
</artifacts_produced>

<output>
Create `.planning/phases/05-contenido-de-marca-historia-equipo-proyectos-diferenciaci-n-/05-02-SUMMARY.md` when done. El summary debe marcar solo `requirements-completed: [PROJ-01, DIFF-01]`, declarar `BROCH-01` bloqueado por PDF real ausente y preservar TEAM-01 parcial de 05-01. No marcar Phase 5 completamente aprobada mientras esos dos requisitos sigan pendientes.
</output>
