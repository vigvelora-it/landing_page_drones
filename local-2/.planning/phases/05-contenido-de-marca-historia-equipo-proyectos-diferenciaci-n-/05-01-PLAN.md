---
phase: 05-contenido-de-marca-historia-equipo-proyectos-diferenciaci-n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/site-content.ts
  - components/sections/brand-section.tsx
  - components/sections/manifesto-section.tsx
  - app/page.tsx
  - app/layout.tsx
  - app/globals.css
autonomous: true
requirements: [BRAND-01, TEAM-01]
user_setup: []

must_haves:
  truths:
    - "Historia, quiénes somos, misión, visión y los 6 valores aparecen completos y exactamente como BRAND-CONTENT.md"
    - "El manifiesto inventado y sus claims de semanas y ±2 cm dejan de renderizarse y el destino #nosotros conserva su lugar"
    - "Los 4 fundadores aparecen en orden canónico con nombre, cargo y bio completos, sin truncación ni contenido hover-only"
    - "Cada fundador sin foto real usa un placeholder neutral 4:5 de iniciales aria-hidden; ningún asset genérico se presenta como retrato"
    - "TEAM-01 permanece explícitamente parcial porque faltan los 4 retratos reales; los placeholders no cuentan como fotos"
    - "Con JavaScript desactivado, el contenido marcado data-reveal permanece visible mediante un fallback noscript"
  artifacts:
    - path: "lib/site-content.ts"
      provides: "Fuente única tipada para brandStory y corporateValues, además del team ya canónico"
      exports: ["CorporateValue", "brandStory", "corporateValues", "teamIntro", "team"]
      contains: "export const brandStory"
    - path: "components/sections/brand-section.tsx"
      provides: "Sección Nosotros completa: historia, propósito, misión, visión, valores y equipo"
      exports: ["BrandSection"]
      min_lines: 100
    - path: "app/page.tsx"
      provides: "BrandSection cableada en la posición histórica de #nosotros"
      contains: "<BrandSection"
    - path: "app/layout.tsx"
      provides: "Fallback no-JS global para data-reveal"
      contains: "<noscript>"
    - path: "app/globals.css"
      provides: "Layout editorial responsive de marca/equipo usando solo tokens existentes"
      contains: ".brand-section"
  key_links:
    - from: "components/sections/brand-section.tsx"
      to: "lib/site-content.ts"
      via: "imports de brandStory, corporateValues, teamIntro y team"
      pattern: "from \"@/lib/site-content\""
    - from: "app/page.tsx"
      to: "components/sections/brand-section.tsx"
      via: "import y render único dentro del primer InertBoundary"
      pattern: "<BrandSection"
    - from: "components/sections/brand-section.tsx"
      to: "team[].photo"
      via: "rama Image solo con photo real; fallback decorativo de iniciales en ausencia"
      pattern: "member.photo"
---

<objective>
Reemplazar el manifiesto ficticio actual por la narrativa corporativa canónica de SkyTech y construir la composición definitiva del equipo técnico. BRAND-01 debe quedar completo con texto verbatim; TEAM-01 debe entregar estructura, cuatro perfiles y un fallback visual honesto, pero permanecer parcial hasta que el cliente suministre cuatro retratos reales.

Purpose: el centro narrativo de la página debe reflejar la historia, misión, visión, valores y fundadores reales sin inventar claims ni atribuir imágenes genéricas a personas.
Output: datos de marca tipados en `lib/site-content.ts`, nueva `BrandSection`, estilos responsive, wiring en la página y fallback global para contenido sin JavaScript.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/BRAND-CONTENT.md
@.planning/phases/05-contenido-de-marca-historia-equipo-proyectos-diferenciaci-n-/05-RESEARCH.md
@.planning/phases/05-contenido-de-marca-historia-equipo-proyectos-diferenciaci-n-/05-UI-SPEC.md
@.planning/phases/05-contenido-de-marca-historia-equipo-proyectos-diferenciaci-n-/05-VALIDATION.md
@lib/site-content.ts
@components/sections/manifesto-section.tsx
@app/page.tsx
@app/layout.tsx
@app/globals.css
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extender la fuente única con historia, misión, visión y valores canónicos</name>
  <files>lib/site-content.ts</files>
  <read_first>
    - `.planning/BRAND-CONTENT.md` secciones Historia breve, ¿Quiénes somos?, Misión, Visión y Valores corporativos — autoridad literal; copiar puntuación, acentos, mayúsculas y em dash sin reescribir.
    - `05-RESEARCH.md` "Recommended Data Model Additions" — contratos `CorporateValue`, `brandStory`, `corporateValues` y regla de fuente única.
    - `lib/site-content.ts` — conservar estilo TypeScript actual, exports existentes y datos de team/projects/brochure sin cambios semánticos.
  </read_first>
  <action>
    En `lib/site-content.ts`, añadir `export interface CorporateValue { id: string; name: string; description: string }`, `export const brandStory` con claves `history`, `about`, `mission`, `vision`, y `export const corporateValues: CorporateValue[]` con exactamente seis registros en este orden: Precisión, Innovación, Integridad, Compromiso con la seguridad, Sostenibilidad, Trabajo en equipo. Cada valor y cada párrafo debe copiarse verbatim de `.planning/BRAND-CONTENT.md`; no resumir, corregir estilo, dividir, interpolar HTML ni añadir resultados/claims. Mantener `teamIntro` y `team` como fuente canónica ya existente; no llenar `photo` con assets genéricos. No tocar `brochure.href`: su ruta futura puede permanecer como dato no renderizado.
  </action>
  <verify>
    <automated>cd F:/ClaudeCode/Pagina_Web_Mayra/local-2 && npm.cmd run typecheck && rg -n "export interface CorporateValue|export const brandStory|export const corporateValues" lib/site-content.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm.cmd run typecheck` pasa.
    - Existe un único export de `CorporateValue`, `brandStory` y `corporateValues`.
    - `corporateValues` contiene exactamente seis objetos y preserva el orden/copy canónico.
    - Historia, about, misión y visión coinciden carácter por carácter con `.planning/BRAND-CONTENT.md`; no aparecen en JSX duplicados.
    - Los cuatro `team[].photo` siguen ausentes y no se modificó el contenido canónico existente de `teamIntro`/`team`.
  </acceptance_criteria>
  <done>La fuente única exporta toda la narrativa de BRAND-01 y los seis valores exactos sin introducir copy inventado ni falsos retratos.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Construir BrandSection y el layout definitivo de equipo con fallback honesto</name>
  <files>components/sections/brand-section.tsx, app/globals.css</files>
  <read_first>
    - `05-UI-SPEC.md` "Page Architecture", "Brand Story", "Mission, Vision and Values", "Team Technical Grid", responsive/accessibility/motion contracts.
    - `05-RESEARCH.md` "BrandSection", "Team portrait fallback" y "CSS Composition" — arquitectura, rama `member.photo`, derivación de iniciales y breakpoints.
    - `components/sections/manifesto-section.tsx` — reutilizar el patrón local `useRef` + `useGSAP` + `IntersectionObserver` con cleanup; no conservar su copy inventado.
    - `app/globals.css` — reutilizar exclusivamente tokens de color, spacing, tipografía y motion; existe una sola regla global `overflow-x` en body.
  </read_first>
  <action>
    Crear `components/sections/brand-section.tsx` como client component y export nombrado `BrandSection`. Mantener `<section ref={root} className="brand-section section-pad" id="nosotros">`, `.site-shell`, kicker `01 / Nosotros` y el observer one-shot existente. Renderizar un solo `<h2>Nosotros</h2>`; bloques separados de Historia breve y ¿Quiénes somos?; dos `<article>` para Misión/Visión; una región etiquetada Valores corporativos con seis articles mapeados desde `corporateValues`; y región Equipo técnico con `teamIntro` completo y cuatro `<article>` en orden canónico. Los headings subordinados son h3 y los nombres/valores pueden usar h4 según jerarquía. Implementar `getInitials(name)` con primera y última palabra solo para el placeholder: contenedor 4:5, iniciales `aria-hidden="true"`, nombre visible inmediatamente después. Si en el futuro `member.photo` existe, usar `next/image` con `fill`, `sizes="(max-width: 720px) 100vw, 50vw"` y alt `Retrato de ${member.name}`; hoy deben renderizarse cuatro placeholders y cero imágenes de equipo. No truncar bio/copy, no modal/acordeón/hover reveal, no `dangerouslySetInnerHTML`.
    En `app/globals.css`, añadir un bloque `Contenido de marca` con el contrato aprobado: historia editorial 12 columnas en desktop, propósito 2-up, valores 3x2, equipo 2x2; tablet valores 2x3/equipo 2x2; móvil todo 1 columna. Usar solo gaps/padding 4/8/16/24/32/48/64px, roles tipográficos del UI-SPEC, `max-width:68ch` para párrafos, `min-width:0` en hijos de grid y solo tokens CSS existentes. Placeholder 4:5 en `--bg-surface-deep`, sin reutilizar `.media-frame`. Reveal máximo 24px/450ms vía tokens existentes; sin nuevas animaciones, pinning, parallax o `overflow-x`.
  </action>
  <verify>
    <automated>cd F:/ClaudeCode/Pagina_Web_Mayra/local-2 && npm.cmd run lint && npm.cmd run typecheck && rg -n "brandStory|corporateValues|teamIntro|team\.map|member\.photo|aria-hidden" components/sections/brand-section.tsx && rg -n "\.brand-section|\.values-grid|\.team-grid|aspect-ratio:4/5" app/globals.css</automated>
  </verify>
  <acceptance_criteria>
    - Lint y typecheck pasan.
    - BrandSection importa/mapea la fuente única y no contiene los párrafos largos hardcodeados.
    - Hay exactamente seis value cards y cuatro team cards en runtime; cada bio/cargo/nombre está siempre visible.
    - La rama `member.photo` usa Image solo si existe; el estado actual produce cuatro placeholders decorativos de iniciales, no imágenes falsas.
    - CSS cumple 3x2/2x3/1x6 para valores y 2x2/2x2/1x4 para equipo, sin nuevos colores literales ni segunda regla global overflow-x.
  </acceptance_criteria>
  <done>BrandSection representa el contenido completo de Nosotros y los cuatro perfiles con layout responsive, semántica accesible y placeholders honestos.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Sustituir ManifestoSection, retirar claims inventados y garantizar fallback no-JS</name>
  <files>app/page.tsx, app/layout.tsx, components/sections/manifesto-section.tsx</files>
  <read_first>
    - `app/page.tsx` — BrandSection reemplaza a ManifestoSection dentro del primer `InertBoundary`, después de Hero y antes de Capabilities.
    - `components/sections/manifesto-section.tsx` — retirar el archivo al finalizar el cambio para no dejar copy inventado/código muerto.
    - `app/layout.tsx` y `05-RESEARCH.md` "Styling and motion" — añadir el fallback `<noscript><style>` para que `[data-reveal]` y title spans sean visibles sin JavaScript.
    - `05-VALIDATION.md` — matriz desktop/tablet/mobile, copy-integrity, reduced-motion y no-JS.
  </read_first>
  <action>
    Actualizar `app/page.tsx` para importar/renderizar `BrandSection` exactamente una vez en lugar de `ManifestoSection`, en la misma posición dentro del primer `InertBoundary`. Eliminar `components/sections/manifesto-section.tsx` después del wiring: no debe quedar ningún render ni archivo con “Donde otros ven superficie”, “Reducimos semanas” o “±2 cm precisión GNSS”. En `app/layout.tsx`, dentro de body y antes del provider, añadir un `<noscript>` con style estático que fuerce `[data-reveal]{opacity:1!important;transform:none!important}` y `.title-line[data-reveal]>span{transform:none!important}`; no añadir script inline ni una segunda autoridad de motion. Ejecutar build y una verificación Playwright/local en 1440x900, 1000x800, 390x844 y 320x568: copy visible/completo, 6 valores, 4 perfiles, 4 placeholders, cero overflow; bajo reduced-motion todo visible; con JS desactivado el HTML no queda oculto. TEAM-01 se documenta como parcial en `05-01-SUMMARY.md` (`requirements-completed: [BRAND-01]`, `requirements-partial: [TEAM-01]`) y nunca se marca completo en REQUIREMENTS.md.
  </action>
  <verify>
    <automated>cd F:/ClaudeCode/Pagina_Web_Mayra/local-2 && npm.cmd run build && rg -n "BrandSection" app/page.tsx && rg -n "noscript|\[data-reveal\]" app/layout.tsx && if (rg -n "Donde otros ven superficie|Reducimos semanas|±2 cm" app components lib) { exit 1 } else { exit 0 }</automated>
    <human-check>
      <test>Servir el build local y revisar #nosotros en 1440x900, 1000x800, 390x844 y 320x568; repetir con reduced-motion y JavaScript desactivado.</test>
      <expected>Historia/MV/6 valores/4 perfiles legibles y completos, placeholders neutrales, cero overflow/truncación; todos los reveals visibles con reduced-motion y sin JS.</expected>
      <why_human>Composición responsive, wrapping de copy canónico y fallback visual no pueden probarse solo con grep.</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - `npm.cmd run build` pasa y la homepage force-static prerenderiza.
    - `BrandSection` tiene un import y un render; no quedan referencias ni archivo de ManifestoSection.
    - Los tres claims inventados anteriores tienen cero hits en `app/`, `components/`, `lib/`.
    - El fallback noscript está presente y las pruebas sin JS/reduced-motion muestran todo el contenido.
    - Los cuatro viewports cumplen `scrollWidth === clientWidth`; contenido sin clamp/ellipsis.
    - BRAND-01 queda completo; TEAM-01 queda parcial y bloqueado por cuatro retratos reales.
  </acceptance_criteria>
  <done>Nosotros usa exclusivamente copy canónico, el manifiesto ficticio fue retirado, el contenido funciona con y sin JavaScript y la dependencia real de TEAM-01 queda honestamente documentada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contenido canónico de planning → constantes TypeScript → DOM | El wording y los hechos del cliente deben conservar integridad durante la transcripción y renderizarse como texto React |
| `team[].photo` → identidad visible | Una ruta errónea o genérica podría atribuir una imagen falsa a una persona real |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering / Integrity | `brandStory`, `corporateValues`, `team` | mitigate | Copiar verbatim desde `BRAND-CONTENT.md`, centralizar en `lib/site-content.ts`, no duplicar párrafos en JSX y comparar `textContent`/fuente durante browser QA. |
| T-05-02 | Spoofing / Integrity | Retratos del equipo | mitigate | Renderizar Image solo cuando `member.photo` exista y haya procedencia confirmada; mientras falten assets, usar iniciales decorativas 4:5. Prohibido usar drones, stock o rostros generados. |
| T-05-03 | Injection | Render de copy | accept | Los datos son constantes TypeScript developer-authored y React los escapa; no hay input de usuario/CMS y se prohíbe `dangerouslySetInnerHTML`. Reauditar si la fuente se vuelve dinámica. |
</threat_model>

<verification>
- Por tarea: typecheck o lint+typecheck; gate final de plan: build de producción.
- Integridad: fuente única y ausencia total de los claims inventados; navegador confirma texto completo y seis/cuatro registros.
- Responsive: 1440x900, 1000x800, 390x844 y 320x568 sin overflow/truncación.
- Accesibilidad/motion: jerarquía h2/h3/h4, placeholders aria-hidden, reduced-motion y no-JS dejan todo visible.
</verification>

<success_criteria>
- BRAND-01 completo: historia, quiénes somos, misión, visión y seis valores verbatim.
- TEAM-01 parcialmente implementado: cuatro nombres/cargos/bios y composición final presentes; permanece unchecked hasta recibir cuatro fotos reales.
- Cero copy ficticio anterior, imágenes de identidad falsas, colores/dependencias nuevos u overflow horizontal.
</success_criteria>

<artifacts_produced>
- `components/sections/brand-section.tsx` exporta `BrandSection`.
- `lib/site-content.ts` exporta `CorporateValue`, `brandStory` y `corporateValues`.
- `app/page.tsx` sustituye ManifestoSection por BrandSection; el archivo viejo se elimina.
- `app/layout.tsx` contiene el fallback noscript global de reveals.
- `app/globals.css` contiene estilos de historia, purpose, valores y equipo.
</artifacts_produced>

<output>
Create `.planning/phases/05-contenido-de-marca-historia-equipo-proyectos-diferenciaci-n-/05-01-SUMMARY.md` when done. El summary debe usar `requirements-completed: [BRAND-01]` y declarar TEAM-01 como parcial/bloqueado por cuatro retratos reales; no actualizar su checkbox.
</output>
