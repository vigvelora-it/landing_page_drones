---
phase: 01-fundaci-n-tema-claro-y-especificaci-n-de-movimiento-moderado
plan: 04
type: execute
wave: 4
depends_on: [01-03]
files_modified:
  - app/globals.css
autonomous: true
requirements: [THEME-01, THEME-02, THEME-03, THEME-04]

must_haves:
  truths:
    - "La sección Contacto ya no es oscura (--ink) — es la superficie blanca 'brightest' de la conversión"
    - ".form-status.is-success usa --success y .is-error usa --destructive"
    - "El .submit-button y el .custom-cursor usan texto blanco sobre acento (contraste verificado)"
    - "El footer usa --bg-surface-deep (gris-azul claro), no un fondo oscuro"
    - ".environment-badge ya no usa backdrop-filter"
    - "El grep audit completo de VALIDATION.md devuelve el conteo esperado en todo el archivo"
  artifacts:
    - path: "app/globals.css"
      provides: "Secciones Contacto/Footer/Cursor/Badge migradas + archivo completo auditado"
      contains: "var(--success)"
  key_links:
    - from: ".form-status.is-success"
      to: "--success token"
      via: "color token verificado ≥4.5:1 sobre --bg-surface"
      pattern: "is-success\\{color:var\\(--success\\)"
---

<objective>
Cerrar la migración: migrar las secciones finales (Contacto, Footer, Cursor, `.environment-badge`, `.video-toggle`) del tema oscuro al layer claro, y ejecutar el audit de fase completo que verifica los cuatro gates THEME-01/02/03/04 sobre todo `app/globals.css` y `lib/motion-preferences.ts`.

Incluye el flip de la sección oscura `.contact-section` (`--ink` → `--bg-surface`, la superficie más brillante para el momento de conversión), la aplicación del token `--success` (creado en el Plan 01) a `.form-status.is-success`, el fix de contraste Pitfall 1 en `.submit-button` y `.custom-cursor`, el flip del footer a `--bg-surface-deep` (Discretion Call #3: footer claro, no banda oscura), y la eliminación de `backdrop-filter` en `.environment-badge`.

Purpose: THEME-01/02/03 en las secciones finales + verificación THEME-04, y el gate de fase completo (todos los grep audits de 01-VALIDATION.md). Aplica D-01 (filtro grayscale removido) y confirma D-02 (PALETA-DE-MARCA.md intacto).

Output: Bloques Contacto/Footer/Cursor/Badge migrados + reporte de audit de fase.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fundaci-n-tema-claro-y-especificaci-n-de-movimiento-moderado/01-UI-SPEC.md
@.planning/phases/01-fundaci-n-tema-claro-y-especificaci-n-de-movimiento-moderado/01-RESEARCH.md
@.planning/phases/01-fundaci-n-tema-claro-y-especificaci-n-de-movimiento-moderado/01-VALIDATION.md
@.planning/phases/01-fundaci-n-tema-claro-y-especificaci-n-de-movimiento-moderado/01-03-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Este plan NO crea tokens nuevos — aplica el token `--success` (definido en el Plan 01) y consume el resto. No crea archivos nuevos. Solo migra selectores en `app/globals.css` y produce el reporte de audit en el SUMMARY.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Migrar Contacto (flip de sección oscura + form-status + Pitfall 1 en submit)</name>
  <files>app/globals.css</files>
  <read_first>
    - app/globals.css (línea 35 — bloque `/* Contacto */`)
    - 01-RESEARCH.md — "Migration Map — Color Tokens" (fila `--ink` sección `.contact-section`, `--orange`, `--hot`, catch-all `#ffffffXX`), "Pitfall 1" (`.submit-button`), "Open Question #3" (`.form-status.is-success` → `--success`), catch-all rows (`.contact-backdrop img` grayscale, `.contact-section:before` glow ring)
    - 01-UI-SPEC.md — Color, secuencia (Contact = `--bg-surface`, blanco "brightest"), Contrast Verification (`--destructive` 6.53:1), Copywriting Contract (error → `--destructive`)
    - 01-CONTEXT.md — D-01 (remover filtro grayscale)
  </read_first>
  <action>
    Flip de sección: `.contact-section{background:var(--ink);color:#fff}` → `background:var(--bg-surface);color:var(--ink-primary)` (superficie blanca más brillante, momento de conversión). Todo el texto light-on-dark invierte a dark-on-light.

    Backdrop (D-01, THEME-03 — quinto selector filtrado, no listado en el checklist original): `.contact-backdrop img{filter:grayscale(1)}` → ELIMINAR la propiedad `filter`. `.contact-backdrop{opacity:.12}` — la opacidad es composición; evaluar si la imagen de fondo aún aporta sobre superficie blanca; documentar la decisión.

    Glow ring decorativo (fresh-read finding — invisible sobre fondo blanco): `.contact-section:before{border:1px solid #ffffff14;box-shadow:0 0 0 9vw #ffffff05,0 0 0 18vw #ffffff04}` → blanco-sobre-blanco es invisible una vez la sección es `--bg-surface`. ELIMINAR el ring, o re-derivarlo desde `--border-subtle`/`--ink-primary` a muy baja opacidad si el efecto vale la pena bajo el tema claro; documentar la decisión.

    Texto: `.contact-copy>p{color:#ffffff94}` → `var(--ink-secondary)`. `.contact-copy>a{border-bottom:1px solid #ffffff59}` → `var(--border-subtle)`. `.contact-form label>span{color:#ffffff8c}` → `var(--ink-secondary)`.

    Formulario: `.contact-form input,select,textarea{border-bottom:1px solid #ffffff47;color:#fff}` → `border-bottom:1px solid var(--border-strong);color:var(--ink-primary)` (borde funcional que ES el único indicador del campo — usa `--border-strong`, que pasa 3:1). `.contact-form input::placeholder,textarea::placeholder{color:#ffffff4d}` → `var(--ink-secondary)`. `.contact-form input:focus,select:focus,textarea:focus{border-color:var(--orange)}` → `var(--accent)` (borde de foco funcional). `.contact-form select option{color:var(--ink)}` → `var(--ink-primary)`.

    Submit (Pitfall 1): `.submit-button{background:var(--orange);color:var(--ink)}` → `background:var(--accent);color:#FFFFFF` (par verificado 5.77:1). `.submit-button:hover{background:var(--hot)}` → `var(--accent-hover)`.

    Form status: `.form-status.is-success{color:#9ce2a9}` (Open Question #3) → `var(--success)` (token definido en el Plan 01, verificado ≥4.5:1 sobre `--bg-surface`). `.form-status.is-error{color:#ff9c85}` → `var(--destructive)` (verificado 6.53:1 sobre `--bg-surface`).
  </action>
  <acceptance_criteria>
    - `grep -n "var(--orange)\|var(--hot)\|grayscale(1)\|#ffffff94\|#ffffff59\|#ffffff8c\|#ffffff47\|#ffffff4d\|#9ce2a9\|#ff9c85\|#ffffff14\|#ffffff05" app/globals.css` → 0 hits en el bloque Contacto (línea 35).
    - `.contact-section` usa `background:var(--bg-surface)` (no `var(--ink)`).
    - `.submit-button` usa `color:#FFFFFF` con `background:var(--accent)` (Pitfall 1); `:hover` usa `var(--accent-hover)`.
    - `.form-status.is-success` usa `var(--success)`; `.is-error` usa `var(--destructive)`.
    - `npm run build` compila.
  </acceptance_criteria>
  <verify>
    <automated>grep -n "is-success{color:#9ce2a9\|grayscale(1)" app/globals.css; npm run build</automated>
    <human-check>Verificar el formulario de contacto: fondo blanco, campos legibles con borde visible, foco con borde acento; enviar el form y confirmar que el mensaje de éxito (verde --success) y de error (rojo --destructive) son legibles.</human-check>
  </verify>
  <done>`.contact-section` es la superficie blanca de conversión; el formulario usa bordes/foco/submit tokenizados con texto blanco sobre acento; `.form-status` usa `--success`/`--destructive`; filtro grayscale y glow ring resueltos; cero literales oscuros en Contacto.</done>
</task>

<task type="auto">
  <name>Task 2: Migrar Footer, Cursor, environment-badge y video-toggle</name>
  <files>app/globals.css</files>
  <read_first>
    - app/globals.css (línea 36 — `.footer`/`.environment-badge`; línea 39 — `/* Cursor */` `.custom-cursor`; línea 40 — `.video-toggle`)
    - 01-RESEARCH.md — "Migration Map — Color Tokens" (fila `--hot` `.custom-cursor`, `--light-line`, catch-all `#ffffffXX`, `.footer` #080a0d → `--bg-surface-deep`, `.environment-badge` backdrop-filter), "Pitfall 1" (`.custom-cursor`), Retirement Checklist (`backdrop-filter`)
    - 01-UI-SPEC.md — Color, Discretion Call #3 (footer `--bg-surface-deep`)
  </read_first>
  <action>
    Footer (Discretion Call #3 confirmada: footer claro, no banda oscura): `.footer{background:#080a0d;color:#fff}` → `background:var(--bg-surface-deep);color:var(--ink-primary)`. `.footer-mark{border-bottom:1px solid var(--light-line)}` → `var(--border-subtle)`. `.footer-bottom{color:#ffffff80}` → `var(--ink-secondary)`.

    Environment badge (Retirement Checklist — quitar el efecto glass): `.environment-badge{border:1px solid #ffffff66;background:#0c0e12d1;backdrop-filter:blur(10px);color:#fff}` → ELIMINAR `backdrop-filter:blur(10px)`, `background:var(--bg-surface-deep)`, `border:1px solid var(--border-subtle)`, `color:var(--ink-primary)`.

    Cursor (Pitfall 1): `.custom-cursor{background:var(--hot);color:var(--ink)}` → `background:var(--accent);color:#FFFFFF` (par verificado). El resto del cursor son transiciones/tamaños, sin color literal.

    Video-toggle: `.video-toggle{color:#fff}` → sobre el hero (ahora `--bg-surface-alt` claro) → `var(--ink-primary)`. `.video-toggle i{border:1px solid #ffffff73}` → `var(--border-strong)`. `.video-toggle .pause-icon:before,.pause-icon:after{background:#fff}` y `.play-icon:before{border-left:6px solid #fff}` → `var(--ink-primary)` (iconos dibujados, ahora sobre fondo claro).
  </action>
  <acceptance_criteria>
    - `grep -n "#080a0d" app/globals.css` → 0 hits en TODO el archivo (footer era el último; `.intro` ya migró en Plan 02).
    - `grep -n "backdrop-filter" app/globals.css` → 0 hits.
    - `grep -n "var(--hot)\|var(--light-line)\|#ffffff80\|#ffffff66\|#0c0e12d1\|#ffffff73" app/globals.css` → 0 hits en los bloques Footer/Cursor/Badge/video-toggle.
    - `.custom-cursor` usa `background:var(--accent);color:#FFFFFF`.
    - `npm run build` compila.
  </acceptance_criteria>
  <verify>
    <automated>grep -n "#080a0d\|backdrop-filter" app/globals.css; npm run build</automated>
    <human-check>Footer gris-azul claro legible; badge de entorno sólido sin glass; cursor personalizado con texto blanco sobre acento; video-toggle legible sobre el hero claro.</human-check>
  </verify>
  <done>Footer usa `--bg-surface-deep`; badge sin `backdrop-filter`; cursor con texto blanco sobre acento; video-toggle tokenizado; cero `#080a0d`/`backdrop-filter`/`--hot`/`--light-line` en el archivo.</done>
</task>

<task type="auto">
  <name>Task 3: Audit de fase completo — grep gates + build/lint/typecheck</name>
  <files>app/globals.css, lib/motion-preferences.ts</files>
  <read_first>
    - 01-VALIDATION.md — "Per-Task Verification Map" (comandos grep exactos por THEME) y "Manual-Only Verifications"
    - 01-RESEARCH.md — "Validation Architecture > Phase Requirements → Verification Map" (conteos esperados de `infinite`, comandos de blend/filter)
    - 01-01-SUMMARY.md, 01-02-SUMMARY.md, 01-03-SUMMARY.md (para confirmar decisiones documentadas de scrim/vignette/glow/infinite)
  </read_first>
  <action>
    Ejecutar el gate de fase completo sobre el archivo ya migrado. NO modificar código salvo para corregir cualquier hit residual que los greps revelen.

    THEME-01/THEME-03 (cero literales oscuros): correr `grep -nE "#0c0e12|#0c0f12|#0a0c0f|#080a0d|--ink:|--soft:|--paper:|--orange:|--hot:|--dark-line|--light-line" app/globals.css` → esperar cero hits fuera de comentarios. También `grep -nE "var\(--ink\)|var\(--soft\)|var\(--paper\)|var\(--orange\)|var\(--hot\)|var\(--dark-line\)|var\(--light-line\)" app/globals.css` → cero hits (ningún selector consume tokens legacy).

    THEME-03 (blend/filter): `grep -nE "mix-blend-mode|backdrop-filter|filter:" app/globals.css` → revisar línea por línea contra el catálogo; esperar cero `mix-blend-mode`, cero `backdrop-filter`, y cero `filter:` de oscurecimiento/desaturación (los 5 selectores de imagen: hero-media, tech-media, statement-visual, deliverable-image, contact-backdrop). Nota: la línea `@media(prefers-reduced-motion)` con `animation-iteration-count` NO es un `filter`.

    THEME-04 (movimiento): `grep -n "infinite" app/globals.css` → confirmar solo los survivors sancionados/documentados (`orbit` en `.orbit-one`/`.orbit-two`, `marquee` en `.moving-band-track`; y `pulse`/`scroll-line`/`spin` SOLO si se decidió flaggearlas como diferidas y está documentado en los SUMMARYs — de lo contrario deben estar acotadas). `grep -rnE "pin:|scrub:" app/globals.css components lib` → cero hits (no existe pinning/scrub y debe seguir así). Confirmar `grep -n "normal: 0.1" lib/motion-preferences.ts` → 1 hit. Confirmar `grep -c "clamp(-8px,var(--parallax,0),8px)" app/globals.css` → 2 hits.

    D-02: confirmar que `PALETA-DE-MARCA.md` NO fue modificado en esta fase (`git status` no lo lista).

    Correr `npm run lint`, `npm run typecheck` y `npm run build` — los tres deben pasar sin errores.

    Registrar en el SUMMARY el conteo real de cada grep audit vs. el esperado, y las decisiones discrecionales documentadas (scrim de hero, vignette de tech, glow ring de contacto, disposición de las animaciones infinite pulse/scroll/spin).
  </action>
  <acceptance_criteria>
    - `grep -nE "#0c0e12|#0c0f12|#0a0c0f|#080a0d|--ink:|--soft:|--paper:|--orange:|--hot:|--dark-line|--light-line" app/globals.css` → 0 hits fuera de comentarios.
    - `grep -nE "var\(--ink\)|var\(--soft\)|var\(--paper\)|var\(--orange\)|var\(--hot\)|var\(--dark-line\)|var\(--light-line\)" app/globals.css` → 0 hits.
    - `grep -nE "mix-blend-mode|backdrop-filter" app/globals.css` → 0 hits; `grep -n "filter:" app/globals.css` → 0 hits de filtros de imagen de oscurecimiento.
    - `grep -rnE "pin:|scrub:" app/globals.css components lib` → 0 hits.
    - `grep -n "normal: 0.1" lib/motion-preferences.ts` → 1 hit.
    - `grep -c "clamp(-8px,var(--parallax,0),8px)" app/globals.css` → 2.
    - `npm run lint && npm run typecheck && npm run build` → los tres pasan.
    - `git status` no lista `PALETA-DE-MARCA.md` (D-02).
  </acceptance_criteria>
  <verify>
    <automated>grep -nE "#0c0e12|#0c0f12|#0a0c0f|#080a0d|--orange:|--hot:" app/globals.css; grep -nE "mix-blend-mode|backdrop-filter" app/globals.css; grep -rnE "pin:|scrub:" app/globals.css components lib; grep -c "clamp(-8px,var(--parallax,0),8px)" app/globals.css; npm run lint && npm run typecheck && npm run build</automated>
    <human-check>Scroll-through completo del sitio a 1440px y 375px: cero fondos oscuros en cualquier sección (incluido el splash intro), texto legible en todos los pares, timing de reveal consistente (no varía por sección), sin overflow horizontal. Ejecutar axe/Lighthouse `color-contrast` sobre el build de producción.</human-check>
  </verify>
  <done>Los cuatro grep gates de THEME-01/02/03/04 devuelven el conteo esperado; `pin:`/`scrub:` ausentes en todo el codebase; `lerp` 0.1 confirmado; 2 clamps de parallax; lint/typecheck/build limpios; `PALETA-DE-MARCA.md` intacto (D-02); reporte de audit registrado en el SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Ninguna. Cambios presentacionales (CSS) en secciones existentes; el audit solo lee/verifica. Sin entrada de usuario, red ni persistencia.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-NONE | — | Migración CSS de Contacto/Footer/Cursor/Badge + audit de fase | accept | Sin superficie de seguridad (01-RESEARCH.md Security Domain). El formulario de contacto solo cambia tokens visuales (borde/foco/color); su validación Zod (`lib/contact-schema.ts`) y su ruta (`app/api/contact/route.ts`) NO se tocan. No aplica ninguna categoría ASVS L1. Sin instalación de paquetes (sin `T-01-SC`). |
</threat_model>

<verification>
- `npm run lint && npm run typecheck && npm run build` limpios.
- Los cuatro grep gates (THEME-01/02/03/04) devuelven el conteo esperado en todo el archivo.
- `pin:`/`scrub:` ausentes; `lerp` 0.1; 2 clamps de parallax.
- `PALETA-DE-MARCA.md` no modificado (D-02).
</verification>

<success_criteria>
- Contacto, Footer, Cursor, badge y video-toggle en tema claro sin fondos oscuros ni literales del tema anterior.
- `.form-status` usa `--success`/`--destructive`; hover/submit/cursor con texto blanco sobre acento (Pitfall 1).
- Audit de fase completo pasa: cero literales oscuros/blend/filter, cero pin/scrub, motion tokens consistentes.
</success_criteria>

<output>
Create `.planning/phases/01-fundaci-n-tema-claro-y-especificaci-n-de-movimiento-moderado/01-04-SUMMARY.md` when done
</output>
