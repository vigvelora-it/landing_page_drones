---
phase: 01-fundaci-n-tema-claro-y-especificaci-n-de-movimiento-moderado
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - app/globals.css
autonomous: true
requirements: [THEME-01, THEME-02, THEME-03, THEME-04]

must_haves:
  truths:
    - "La sección Capacidades ya no es una sección oscura (--ink) — usa una superficie clara y su texto invierte a oscuro"
    - "El hover de .service-row usa texto blanco sobre el relleno de acento (contraste verificado)"
    - "La sección Tecnología ya no tiene fondos near-black (#0a0c0f) ni filtro de oscurecimiento en tech-media"
    - "El .tech-vignette duplicado dentro del media query 720px también está migrado"
    - "El parallax de .tech-media está clampeado a ±8px"
    - "La sección Proceso usa tokens claros sin literales oscuros"
  artifacts:
    - path: "app/globals.css"
      provides: "Secciones Capacidades, Tecnología y Proceso migradas al tema claro"
      contains: "clamp(-8px,var(--parallax,0),8px)"
  key_links:
    - from: ".service-row:hover"
      to: "--accent fill"
      via: "texto blanco sobre acento (Pitfall 1)"
      pattern: "service-row:hover"
---

<objective>
Migrar las tres secciones centrales (Capacidades, Tecnología, Proceso) del tema oscuro al layer claro. Incluye el flip de la sección oscura `.capabilities` (`--ink` → superficie clara, con inversión de todo su texto light-on-dark a dark-on-light), el fix de contraste Pitfall 1 en `.service-row:hover`, la migración del color de `.moving-band` (no su animación de marquee), la eliminación del fondo near-black y filtro de `.technology`/`.tech-media`, el clamp de parallax ±8px en `.tech-media`, la migración del duplicado de `.tech-vignette` dentro del media query 720px, y la migración de Proceso incluido `--soft` y el filtro de `.deliverable-image img`.

Purpose: THEME-01/02/03 en las secciones centrales + THEME-04 (clamp parallax tech-media, flag de marquee infinite). Resuelve Open Question #4 (color de `.moving-band` migrado, animación diferida) y aplica D-01 (filtros removidos).

Output: Bloques Capacidades, Tecnología (base + media query) y Proceso de `app/globals.css` migrados.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fundaci-n-tema-claro-y-especificaci-n-de-movimiento-moderado/01-UI-SPEC.md
@.planning/phases/01-fundaci-n-tema-claro-y-especificaci-n-de-movimiento-moderado/01-RESEARCH.md
@.planning/phases/01-fundaci-n-tema-claro-y-especificaci-n-de-movimiento-moderado/01-VALIDATION.md
@.planning/phases/01-fundaci-n-tema-claro-y-especificaci-n-de-movimiento-moderado/01-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Este plan NO crea tokens nuevos — consume los definidos en el Plan 01. No crea archivos nuevos. Solo migra selectores consumidores en `app/globals.css`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Migrar Capacidades (flip de sección oscura + contraste hover + moving-band)</name>
  <files>app/globals.css</files>
  <read_first>
    - app/globals.css (línea 26 — bloque `/* Capacidades */`)
    - 01-RESEARCH.md — "Migration Map — Color Tokens" (fila `--ink` sección `.capabilities`, `--light-line`, `--orange`, catch-all `#ffffffXX` y `#0c0e12XX`), "Pitfall 1" (`.service-row:hover`), "Open Question #4" (`.moving-band` color), Retirement Checklist
    - 01-UI-SPEC.md — Color, secuencia de superficies ("gris arriba" — Capabilities = `--bg-surface-alt`)
  </read_first>
  <action>
    Flip de sección: `.capabilities{background:var(--ink);color:#fff}` → `background:var(--bg-surface-alt);color:var(--ink-primary)` (banda gris en la secuencia). Como el fondo invierte de oscuro a claro, TODO el texto light-on-dark de la sección invierte a dark-on-light: `.capabilities-heading p,.process-intro>p{color:#ffffff99}` → `var(--ink-secondary)` (nota: `.process-intro>p` también aparece aquí — coordinar, se migra su valor en este selector agrupado). `.service-number{color:#ffffff73}` → `var(--ink-secondary)`. `.service-detail{color:#ffffff85}` → `var(--ink-secondary)`. `.service-row>span:last-child{border:1px solid #ffffff59}` → `var(--border-strong)`.

    Bordes: `.service-list{border-top:1px solid var(--light-line)}` → `var(--border-subtle)`. `.service-row{border-bottom:1px solid var(--light-line)}` → `var(--border-subtle)`.

    Relleno de hover + Pitfall 1: `.service-row:before{background:var(--orange)}` → `var(--accent)`. `.service-row:hover{color:var(--ink)}` → `color:#FFFFFF` (texto blanco sobre el relleno de acento que aparece en hover — Pitfall 1, el par verificado). `.service-row:hover .service-number,.service-row:hover .service-detail{color:#0c0e12a6}` → `color:#FFFFFF` (o un blanco a alpha alta) para mantener legibilidad sobre el acento.

    Moving-band (Open Question #4: color sí, animación no): `.moving-band{background:var(--orange);color:var(--ink)}` → `background:var(--accent);color:#FFFFFF` (Pitfall 1). `.moving-band-track{animation:marquee 24s linear infinite}` — NO tocar la animación (diferida a Fase 3+ por CONTEXT.md; es uno de los dos survivors `infinite` sancionados por el UI-SPEC junto con `.hero-orbit`).
  </action>
  <acceptance_criteria>
    - `grep -n "var(--ink)\|var(--light-line)\|var(--orange)\|#ffffff99\|#ffffff73\|#ffffff85\|#ffffff59\|#0c0e12a6" app/globals.css` → 0 hits en el bloque Capacidades (línea 26). (`.process-intro>p{color:#0c0e1299}` de la sección Proceso se migra en Task 3.)
    - `.service-row:hover` usa `color:#FFFFFF` (no `var(--ink)`); `.moving-band` usa `background:var(--accent);color:#FFFFFF`.
    - `.capabilities` usa `background:var(--bg-surface-alt)` (no `var(--ink)`).
    - `grep -c "infinite" app/globals.css` — el `marquee` de `.moving-band-track` se conserva (diferido), no aumenta el conteo.
    - `npm run build` compila.
  </acceptance_criteria>
  <verify>
    <automated>grep -n "\.capabilities{background:var(--ink)\|\.moving-band{background:var(--orange)\|service-row:hover{color:var(--ink)" app/globals.css; npm run build</automated>
    <human-check>Scroll a Capacidades: fondo gris claro (no oscuro), filas legibles, hover de service-row muestra texto blanco legible sobre el acento, moving-band con texto blanco sobre acento.</human-check>
  </verify>
  <done>`.capabilities` es una banda clara con texto oscuro; `.service-row:hover` y `.moving-band` usan texto blanco sobre acento; bordes tokenizados; sin literales oscuros; el marquee infinite se conserva (diferido).</done>
</task>

<task type="auto">
  <name>Task 2: Migrar Tecnología (fondo, filtro tech-media, vignette + duplicado media query, parallax clamp)</name>
  <files>app/globals.css</files>
  <read_first>
    - app/globals.css (línea 29 — bloque `/* Tecnología */`; línea 43 — media query `@media(max-width:720px)` que re-declara `.tech-vignette` y `.tech-media`)
    - 01-RESEARCH.md — "Pitfall 2" (los 4 literales near-black, `.tech-vignette` duplicado en 720px), "Code Examples > Parallax clamp", Retirement Checklist (`filter` en `.tech-media img`), catch-all `#ffffffXX`
    - 01-UI-SPEC.md — Color, secuencia de superficies (Technology = `--bg-surface`, blanco)
    - 01-CONTEXT.md — D-01 (remover filtro de imagen)
  </read_first>
  <action>
    Fondo: `.technology{background:#0a0c0f;color:#fff}` → `background:var(--bg-surface);color:var(--ink-primary)` (banda blanca en la secuencia). `.tech-media{transform:translateY(var(--parallax,0))}` → `translateY(clamp(-8px,var(--parallax,0),8px))` (THEME-04 ±8px). `.tech-media img{filter:grayscale(.25) contrast(1.15) brightness(.6)}` (D-01) → ELIMINAR la propiedad `filter`.

    Vignette (Pitfall 2): `.tech-vignette{background:radial-gradient(circle at 72% 50%,transparent 0,#0a0c0f1a 25%,#0a0c0fe6 72%),linear-gradient(90deg,#0a0c0ff0,transparent 65%)}` está tuneado para oscurecer una foto sobre fondo negro. Bajo el tema claro (fondo blanco) este vignette oscuro no aplica — eliminarlo o re-derivarlo a un scrim claro muy sutil derivado de tokens si aún aporta legibilidad; documentar la decisión (análogo al `.hero-shade` del Plan 02).

    Texto: `.tech-specs span{color:#ffffff80}` → `var(--ink-secondary)`. `.tech-caption{color:#ffffff80}` → `var(--ink-secondary)`.

    Media query 720px (línea 43 — CRÍTICO, se pierde fácil): re-declara `.tech-vignette{background:linear-gradient(0deg,#0a0c0ff2 15%,#0a0c0f26 72%,#0a0c0fb8)}` con la misma familia de literal near-black — migrarlo EN LOCKSTEP con la regla base (mismo tratamiento: eliminar o scrim claro sutil). `.tech-media{...opacity:.72}` en 720px — la opacidad es de composición, no color; dejar salvo que el vignette removido cambie el resultado visual.
  </action>
  <acceptance_criteria>
    - `grep -n "#0a0c0f" app/globals.css` → 0 hits en TODO el archivo (base + media query 720px). Este es el chequeo clave de Pitfall 2.
    - `grep -n "grayscale(.25)\|#ffffff80" app/globals.css` → 0 hits en el bloque Tecnología.
    - `grep -c "clamp(-8px,var(--parallax,0),8px)" app/globals.css` → 2 hits (hero-media del Plan 02 + tech-media de este plan).
    - `.technology` usa `background:var(--bg-surface)` (no `#0a0c0f`).
    - `npm run build` compila.
  </acceptance_criteria>
  <verify>
    <automated>grep -n "#0a0c0f" app/globals.css; grep -c "clamp(-8px,var(--parallax,0),8px)" app/globals.css; npm run build</automated>
    <human-check>Scroll a Tecnología a 1440px y 375px: sin banda oscura en ninguno de los dos anchos (verifica que el duplicado del media query se migró), imagen cruda legible, parallax sutil ≤8px.</human-check>
  </verify>
  <done>`.technology` es una banda blanca; el filtro de `.tech-media` eliminado; el parallax clampeado; los DOS `.tech-vignette` (base + 720px) migrados; cero `#0a0c0f` en el archivo.</done>
</task>

<task type="auto">
  <name>Task 3: Migrar Proceso (paper, bordes, literales, --soft, filtro deliverable)</name>
  <files>app/globals.css</files>
  <read_first>
    - app/globals.css (línea 32 — bloque `/* Proceso */`)
    - 01-RESEARCH.md — "Migration Map — Color Tokens" (filas `--paper`, `--orange`, `--soft`, `--dark-line`, catch-all `#0c0e12XX`), Retirement Checklist (`filter` en `.deliverable-image img`), "Assumptions Log A1" (`--soft` → `--bg-surface-deep`)
    - 01-UI-SPEC.md — Color, secuencia (Process = `--bg-surface-alt`, gris)
    - 01-CONTEXT.md — D-01 (remover filtro)
  </read_first>
  <action>
    `.process-section{background:var(--paper)}` → `var(--bg-surface-alt)` (banda gris en la secuencia). `.process-intro>p{color:#0c0e1299}` → `var(--ink-secondary)`. `.process-list{border-top:1px solid var(--dark-line)}` → `var(--border-subtle)`. `.process-step{border-bottom:1px solid var(--dark-line)}` → `var(--border-subtle)`. `.process-step>span{color:#0c0e1280}` → `var(--ink-secondary)`. `.process-step p{color:#0c0e129e}` → `var(--ink-secondary)`. `.process-step i{background:var(--orange)}` → `var(--accent)`. `.deliverable-image{background:var(--soft)}` (A1) → `var(--bg-surface-deep)`. `.deliverable-image img{filter:saturate(.65) contrast(1.1)}` (D-01) → ELIMINAR la propiedad `filter`. `.deliverable-copy>p{color:#0c0e129e}` → `var(--ink-secondary)`. `.file-types span{border:1px solid var(--dark-line)}` → `var(--border-subtle)`.
  </action>
  <acceptance_criteria>
    - `grep -n "var(--paper)\|var(--orange)\|var(--soft)\|var(--dark-line)\|#0c0e1299\|#0c0e1280\|#0c0e129e\|saturate(.65)" app/globals.css` → 0 hits en el bloque Proceso (línea 32). (Contacto en Plan 04 aún tiene sus propios literales.)
    - `.deliverable-image` usa `var(--bg-surface-deep)`; `.deliverable-image img` no contiene `filter:`.
    - `npm run build` compila.
  </acceptance_criteria>
  <verify>
    <automated>grep -n "\.process-section{background:var(--paper)\|\.deliverable-image{background:var(--soft)\|saturate(.65)" app/globals.css; npm run build</automated>
  </verify>
  <done>La sección Proceso usa tokens claros (`--bg-surface-alt`, `--ink-secondary`, `--accent`, `--border-subtle`, `--bg-surface-deep`), sin `--soft`/`--paper`/`--orange`/`--dark-line`/`#0c0e12XX` ni filtro en deliverable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Ninguna. Cambios presentacionales (CSS) en secciones existentes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-NONE | — | Migración CSS de Capacidades/Tecnología/Proceso | accept | Sin superficie de seguridad (01-RESEARCH.md Security Domain). No aplica ninguna categoría ASVS L1. Sin instalación de paquetes. |
</threat_model>

<verification>
- `npm run build` limpio.
- `grep "#0a0c0f" app/globals.css` → 0 hits (Pitfall 2, incluye media query 720px).
- 2 hits de `clamp(-8px,var(--parallax,0),8px)` (hero + tech).
- Flip de `.capabilities` a superficie clara; Pitfall 1 en `.service-row:hover` y `.moving-band`.
</verification>

<success_criteria>
- Capacidades, Tecnología y Proceso renderizan en tema claro sin fondos oscuros ni literales del tema anterior.
- Filtros de imagen eliminados (D-01); vignette de tech re-derivado/eliminado en base y media query.
- Parallax de tech-media clampeado; contraste de hover corregido a texto blanco.
</success_criteria>

<output>
Create `.planning/phases/01-fundaci-n-tema-claro-y-especificaci-n-de-movimiento-moderado/01-03-SUMMARY.md` when done
</output>
