---
phase: 01-fundaci-n-tema-claro-y-especificaci-n-de-movimiento-moderado
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - app/globals.css
autonomous: true
requirements: [THEME-01, THEME-02, THEME-03, THEME-04]

must_haves:
  truths:
    - "El splash .intro ya no tiene fondo oscuro (#080a0d) ni color --orange — usa tokens claros"
    - "El header ya no usa mix-blend-mode:difference — renderiza con un fondo/color sólido tokenizado"
    - "El hero ya no tiene fondos near-black (#0c0f12), ni filtros de oscurecimiento de imagen, ni .hero-noise"
    - "El parallax de .hero-media está clampeado a ±8px"
    - "Las secciones Apertura/Navegación/Hero/Perspectiva no contienen literales --orange/--hot ni #ffffffXX/#0c0e12XX del tema oscuro"
  artifacts:
    - path: "app/globals.css"
      provides: "Secciones Apertura, Navegación, Hero y Perspectiva/Statement migradas al tema claro"
      contains: "clamp(-8px,var(--parallax,0),8px)"
  key_links:
    - from: ".hero-media / .tech-media"
      to: "--parallax custom property"
      via: "clamp() cap ±8px en el punto de consumo CSS"
      pattern: "clamp\\(-8px,var\\(--parallax,0\\),8px\\)"
---

<objective>
Migrar las cuatro secciones superiores del sitio (Apertura `.intro`, Navegación `.site-header`/`.menu-overlay`, Hero, y Perspectiva/Statement) del tema oscuro al layer de tokens claros creado en el Plan 01. Incluye retirar el splash oscuro, eliminar `mix-blend-mode:difference` del header, retirar los filtros de oscurecimiento y `.hero-noise` (D-01: imágenes crudas sin filtro puente), clampear el parallax a ±8px, y aplicar el fix de contraste de Pitfall 1 en los hover con fondo de acento.

Purpose: THEME-01/02/03 (paleta clara, contraste AA, cero literales oscuros) en las secciones superiores + THEME-04 (clamp de parallax ±8px). Resuelve las Open Questions #1 (`.intro` re-tematizado) y #2 (`.hero-shade` como scrim de legibilidad claro) y #4 (color de `.hero-orbit` migrado, no su animación).

Output: Bloques Apertura, Navegación, Hero y Perspectiva de `app/globals.css` migrados.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fundaci-n-tema-claro-y-especificaci-n-de-movimiento-moderado/01-UI-SPEC.md
@.planning/phases/01-fundaci-n-tema-claro-y-especificaci-n-de-movimiento-moderado/01-RESEARCH.md
@.planning/phases/01-fundaci-n-tema-claro-y-especificaci-n-de-movimiento-moderado/01-VALIDATION.md
@.planning/phases/01-fundaci-n-tema-claro-y-especificaci-n-de-movimiento-moderado/01-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Este plan NO crea tokens nuevos — consume los definidos en el Plan 01 (`01-01-SUMMARY.md` lista todos). No crea archivos nuevos. Solo migra selectores consumidores en `app/globals.css`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Migrar Apertura (.intro) y Navegación (header + menu-overlay)</name>
  <files>app/globals.css</files>
  <read_first>
    - app/globals.css (línea 12 — bloque `/* Apertura */`; líneas 15-16 — bloque `/* Navegación */`)
    - 01-RESEARCH.md — "Migration Map — Color Tokens" (filas `--orange`, `--hot`, `--dark-line`/`--light-line`), "Open Question #1" (`.intro` in-scope), y "Pitfall 4" (`.intro-brand span` spin infinite)
    - 01-UI-SPEC.md — "Retirement Checklist" (fila `mix-blend-mode:difference` en `.site-header`), sección Color (tokens de destino)
  </read_first>
  <action>
    Apertura (`.intro`, línea 12) — Open Question #1 (resuelta: in-scope para THEME-03): cambiar `.intro{background:#080a0d;color:#fff}` a un fondo claro tokenizado: `background:var(--bg-surface)` (o `--bg-surface-alt`) y `color:var(--ink-primary)`. Cambiar `.intro:after{background:var(--orange)}` → `var(--accent)`. `.intro-brand span{color:var(--orange)}` → `var(--accent)`. `.intro-line i{background:var(--orange)}` → `var(--accent)`. `.intro-line{background:#ffffff26}` → un divisor tokenizado sobre fondo claro: `var(--border-subtle)`. El literal `.intro-brand strong{font:600 ...}` mantiene su peso (es un peso de display, no el grupo mono-label consolidado). `.intro-brand span{animation:spin 1.4s linear infinite}` (Pitfall 4): dejar la animación pero documentar que está acotada en la práctica por el auto-hide de 1450ms de `components/intro-sequence.tsx`; no es un loop verdaderamente infinito visible. NO removerla (es afordancia de interacción del intro, marcada como aceptable).

    Navegación (líneas 15-16): `.site-header{color:#fff;mix-blend-mode:difference}` (Retirement Checklist — el truco de blend rompe en tema claro): ELIMINAR `mix-blend-mode:difference` y darle un fondo/color sólido tokenizado desde el inicio — `color:var(--ink-primary)` sobre el fondo claro de la página (el header es transparente hoy; en tema claro el texto oscuro sobre superficie clara es legible). NO introducir el toggle de scroll-reactivo (eso es Fase 4/HEAD-01) — solo dejar el baseline sólido. `.brand-symbol{color:var(--hot)}` → `var(--accent)`. `.menu-toggle i{background:currentColor}` — sin cambio (hereda). `.menu-overlay{background:var(--orange);color:var(--ink)}` → `background:var(--accent);color:#FFFFFF` (Pitfall 1: texto blanco sobre acento, no `--ink-primary`). `.menu-overlay nav a{border-bottom:1px solid #0c0e1247}` → `var(--border-subtle)`.
  </action>
  <acceptance_criteria>
    - `grep -n "#080a0d\|mix-blend-mode\|#ffffff26\|#0c0e1247" app/globals.css` → 0 hits en las líneas de Apertura/Navegación (`.intro`, `.site-header`, `.intro-line`, `.menu-overlay nav a`). (`.footer` #080a0d aún migra en Plan 04.)
    - `grep -n "var(--orange)\|var(--hot)" app/globals.css` → 0 hits en los bloques Apertura y Navegación.
    - La regla `.menu-overlay` usa `background:var(--accent)` y `color:#FFFFFF` (Pitfall 1).
    - `.site-header` no contiene `mix-blend-mode`.
    - `npm run build` compila sin errores.
  </acceptance_criteria>
  <verify>
    <automated>grep -n "mix-blend-mode\|#080a0d\|#ffffff26" app/globals.css; npm run build</automated>
    <human-check>Cargar el sitio (dispara el intro): el splash se ve claro (no negro), el header es legible sobre la superficie clara, el menu-overlay tiene texto blanco legible sobre el acento.</human-check>
  </verify>
  <done>`.intro` es un splash claro sin `--orange`; el header no usa `mix-blend-mode` y es sólido/legible; `.menu-overlay` usa acento con texto blanco; cero literales oscuros en Apertura/Navegación.</done>
</task>

<task type="auto">
  <name>Task 2: Migrar Hero (fondos, filtros, scrim, orbit, animaciones infinite, contraste)</name>
  <files>app/globals.css</files>
  <read_first>
    - app/globals.css (líneas 19-20 — bloque `/* Hero */`)
    - 01-RESEARCH.md — "Code Examples > Parallax clamp before/after", "Pitfall 1" (`.circle-link:hover`), "Pitfall 3" (`.hero-shade`), "Pitfall 4" (`.pulse-dot`, `.scroll-cue i:after`), "Open Question #2" (scrim) y "#4" (color de `.hero-orbit`), catch-all de literales `#ffffffXX`
    - 01-UI-SPEC.md — Retirement Checklist (filtros de imagen, `.hero-noise`), Motion Contract (±8px parallax), Color (tokens)
    - 01-CONTEXT.md — D-01 (imágenes crudas sin filtro puente)
  </read_first>
  <action>
    Fondos: `.hero{background:#0c0f12;color:#fff}` → `background:var(--bg-surface-alt);color:var(--ink-primary)` (el hero es la banda "gris arriba" de la secuencia de superficies). `.hero-media{transform:translateY(var(--parallax,0));background:#0c0f12}` → clampear parallax a `translateY(clamp(-8px,var(--parallax,0),8px))` (THEME-04, ±8px) y `background:var(--bg-surface-alt)`.

    Filtros (D-01, THEME-03): `.hero-media img,.hero-video{...filter:saturate(.62) contrast(1.18) brightness(.58)}` → ELIMINAR la propiedad `filter` por completo (imágenes crudas, sin filtro puente). Eliminar `.hero-noise` (grano SVG de turbulencia — "exceso de efectos" vetado): quitar la regla `.hero-noise{...}` y su nodo asociado queda sin estilo (aceptable, es decorativo). `.hero-gridlines` usa `#ffffff0d` (líneas de grid sobre fondo oscuro): sobre fondo claro estas líneas blancas son invisibles — re-derivar a un divisor sutil tokenizado (p.ej. `var(--border-subtle)` a baja opacidad) o eliminar si no aporta bajo el tema claro; documentar la decisión.

    Hero-shade (Open Question #2, resuelta: mantener como scrim de legibilidad claro, NO como oscurecimiento): reemplazar el gradiente near-black `.hero-shade{background:linear-gradient(90deg,#06080af2...),linear-gradient(0deg,#06080ad1,...)}` por un scrim mucho más ligero derivado de tokens, suficiente para proteger la legibilidad del texto del hero — p.ej. un gradiente de `--ink-primary` a baja opacidad. Esto NO es re-añadir un filtro de oscurecimiento (D-01 concierne la propiedad `filter:` de la imagen); es una ayuda de legibilidad. Documentar como llamada discrecional.

    Orbit (Open Question #4: migrar color, NO animación): `.hero-orbit{border:1px solid #ffffff1f}` → `var(--border-subtle)`. `.hero-orbit:after{background:var(--orange);box-shadow:0 0 18px var(--orange)}` → `background:var(--accent);box-shadow:0 0 18px var(--accent)`. NO tocar `animation:orbit 24s/18s linear infinite` (diferido a Fase 3+ por CONTEXT.md — solo el color migra ahora).

    Pulse-dot (Pitfall 4): `.pulse-dot{background:var(--hot);box-shadow:0 0 0 0 #ff7852cc;animation:pulse 2s infinite}` y `@keyframes pulse` con `#ff785200` → migrar el color a `var(--accent)` y las sombras a un rgba derivado del acento. Para la animación `infinite`: acotarla (p.ej. `animation-iteration-count` finito o un solo pulse al cargar) o añadirla explícitamente a la lista "flagged, diferido a Fase 3+" documentada. Aplicar el mismo criterio a `.scroll-cue i:after{animation:scroll-line 2s ease-in-out infinite}` — acotar o flag documentado.

    Texto del hero (catch-all `#ffffffXX` — Pitfall 1, invertir par): el hero pasa de fondo oscuro a claro (`--bg-surface-alt`), así que el texto blanco/alpha invierte a oscuro. `.hero-eyebrow{color:#ffffffb3}`, `.hero-bottom>p{color:#ffffffad}`, `.hero-index{color:#ffffff8c}`, `.scroll-cue{color:#ffffff8c}`, `.scroll-cue i{background:#ffffff40}` → re-derivar a `--ink-primary`/`--ink-secondary`/`--border-subtle` según rol (texto primario/secundario/divisor). `.title-line-accent{color:var(--hot)}` → `var(--accent)`. `.circle-link{border:1px solid #ffffff73}` → `var(--border-strong)`; `.circle-link:hover{color:var(--ink);background:var(--hot);border-color:var(--hot)}` (Pitfall 1) → `color:#FFFFFF;background:var(--accent);border-color:var(--accent)`.
  </action>
  <acceptance_criteria>
    - `grep -n "#0c0f12\|saturate(.62)\|hero-noise\|#06080a\|#ffffff1f\|#ff7852\|#ffffff73" app/globals.css` → 0 hits en el bloque Hero (líneas 19-20). (`.tech-media` filtro y otros aún migran en Plan 03.)
    - `grep -n "clamp(-8px,var(--parallax,0),8px)" app/globals.css` → ≥1 hit (parallax de `.hero-media` clampeado).
    - `grep -n "var(--orange)\|var(--hot)" app/globals.css` → 0 hits en el bloque Hero.
    - `.circle-link:hover` usa `color:#FFFFFF` con `background:var(--accent)` (Pitfall 1, no `var(--ink)`).
    - `grep -c "infinite" app/globals.css` no aumenta respecto al baseline; las animaciones `pulse`/`scroll-line` del hero están acotadas o documentadas como flagged. `.hero-orbit` `orbit` infinite se conserva (diferido).
    - `npm run build` compila.
  </acceptance_criteria>
  <verify>
    <automated>grep -n "#0c0f12\|saturate(.62)\|hero-noise\|#06080a\|#ff7852" app/globals.css; grep -n "clamp(-8px,var(--parallax,0),8px)" app/globals.css; npm run build</automated>
    <human-check>Scroll del hero: sin banda oscura, texto legible sobre la media cruda (con el scrim claro de legibilidad), parallax como deriva sutil (≤8px), hover de circle-link con texto blanco legible sobre acento.</human-check>
  </verify>
  <done>El hero no tiene fondos near-black, filtros de imagen, ni `.hero-noise`; el parallax está clampeado a ±8px; `.hero-shade` es un scrim claro de legibilidad; el color de orbit/pulse migró al acento; los hover con fondo de acento usan texto blanco; las animaciones infinite están acotadas o documentadas como flagged.</done>
</task>

<task type="auto">
  <name>Task 3: Migrar Perspectiva/Statement (kicker, em, filtros, blend, bordes, literales)</name>
  <files>app/globals.css</files>
  <read_first>
    - app/globals.css (línea 23 — bloque `/* Perspectiva */`)
    - 01-RESEARCH.md — "Migration Map — Color Tokens" (filas `--paper`, `--orange`, `--dark-line`, `--light-line`, catch-all `#0c0e12XX`), Retirement Checklist (`filter` en `.statement-visual img`, `mix-blend-mode` en `.frame-coordinates`)
    - 01-UI-SPEC.md — Color (tokens), Retirement Checklist
    - 01-CONTEXT.md — D-01 (remover filtro de `.statement-visual img`)
  </read_first>
  <action>
    `.statement{background:var(--paper)}` → `var(--bg-surface)` (banda blanca en la secuencia). `.section-kicker{border-top:1px solid var(--dark-line);color:#0c0e129e}` → `border-top:1px solid var(--border-subtle);color:var(--ink-secondary)`. `.section-kicker.light{color:#ffffff99;border-color:var(--light-line)}` (invertir: ya no hay sección clara-sobre-oscura) → `color:var(--ink-secondary);border-color:var(--border-subtle)`. `em{color:var(--orange)}` → `var(--accent)`. `.media-frame:after{border:1px solid #ffffff29}` → `var(--border-subtle)`. `.statement-visual img{filter:saturate(.75) contrast(1.08)}` (D-01, THEME-03) → ELIMINAR la propiedad `filter`. `.frame-coordinates{color:#fff;mix-blend-mode:difference}` (Retirement Checklist) → ELIMINAR `mix-blend-mode:difference` y `color:var(--ink-primary)`. `.statement-aside>p:nth-child(2){color:#0c0e12ad}` → `var(--ink-secondary)`. `.metric{border-top:1px solid var(--dark-line)}` → `var(--border-subtle)`. `.media-frame{background:#bbb}` es un placeholder gris neutro — dejar o re-derivar a `var(--bg-surface-deep)` para consistencia; documentar.
  </action>
  <acceptance_criteria>
    - `grep -n "var(--paper)\|var(--orange)\|var(--dark-line)\|var(--light-line)\|mix-blend-mode\|#0c0e12\|#ffffff29\|#ffffff99\|saturate(.75)" app/globals.css` → 0 hits en el bloque Perspectiva (línea 23). (Otras secciones aún migran en planes 03-04.)
    - `.frame-coordinates` no contiene `mix-blend-mode`.
    - `.statement-visual img` no contiene `filter:`.
    - `npm run build` compila.
  </acceptance_criteria>
  <verify>
    <automated>grep -n "var(--paper)\|var(--orange)\|mix-blend-mode\|saturate(.75)" app/globals.css; npm run build</automated>
  </verify>
  <done>La sección Perspectiva/Statement usa tokens claros, sin `mix-blend-mode`, sin filtros de imagen, sin literales `--orange`/`--dark-line`/`--light-line`/`#0c0e12XX`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Ninguna. Cambios puramente presentacionales (CSS) en secciones existentes; sin entrada de usuario, red ni persistencia.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-NONE | — | Migración CSS de secciones Apertura/Nav/Hero/Statement | accept | Sin superficie de seguridad (01-RESEARCH.md Security Domain). No aplica ninguna categoría ASVS L1. Sin instalación de paquetes. |
</threat_model>

<verification>
- `npm run build` limpio.
- Grep de literales oscuros en las cuatro secciones migradas → 0 hits.
- `.hero-media` parallax clampeado a ±8px.
- Sin `mix-blend-mode` en header ni frame-coordinates.
</verification>

<success_criteria>
- Apertura, Navegación, Hero y Perspectiva renderizan en tema claro sin fondos oscuros ni literales del tema anterior.
- Filtros de imagen y `.hero-noise` eliminados (D-01); scrim de hero re-derivado como ayuda de legibilidad clara.
- Parallax de hero clampeado; hover con fondo de acento usan texto blanco (Pitfall 1).
</success_criteria>

<output>
Create `.planning/phases/01-fundaci-n-tema-claro-y-especificaci-n-de-movimiento-moderado/01-02-SUMMARY.md` when done
</output>
