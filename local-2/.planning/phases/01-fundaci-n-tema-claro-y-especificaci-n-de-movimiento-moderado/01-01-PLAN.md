---
phase: 01-fundaci-n-tema-claro-y-especificaci-n-de-movimiento-moderado
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - lib/motion-preferences.ts
autonomous: true
requirements: [THEME-01, THEME-02, THEME-03, THEME-04]

must_haves:
  truths:
    - "El bloque :root de app/globals.css ya no contiene ningún token oscuro (--ink, --soft, --paper, --orange, --hot, --dark-line, --light-line)"
    - "Existen todos los tokens de color claros del UI-SPEC (--bg-surface*, --ink*, --accent*, --border*, --focus-ring, --destructive, --success)"
    - "Existen todos los tokens de movimiento del Motion Contract (--motion-distance-*, --motion-duration-*, --motion-stagger-max, --ease-moderate)"
    - "Las reglas base (html/body/::selection/button) y el sistema [data-reveal]/.title-line consumen los nuevos tokens en vez de literales"
    - "lib/motion-preferences.ts exporta LENIS_LERP.normal = 0.1"
  artifacts:
    - path: "app/globals.css"
      provides: "Nuevo layer de tokens de color y movimiento + reglas base migradas"
      contains: "--bg-surface"
    - path: "lib/motion-preferences.ts"
      provides: "Constante Lenis lerp retuneada a moderado"
      contains: "normal: 0.1"
  key_links:
    - from: "app/globals.css :root"
      to: "[data-reveal] / .title-line[data-reveal]>span"
      via: "var(--motion-*) references"
      pattern: "var\\(--motion-"
    - from: "lib/motion-preferences.ts"
      to: "components/providers/smooth-scroll-provider.tsx"
      via: "LENIS_LERP import (unchanged consumer)"
      pattern: "LENIS_LERP\\.normal"
---

<objective>
Establecer la fundación del tema claro: reemplazar por completo el bloque `:root` de tokens oscuros de `app/globals.css` por el layer de tokens claros WCAG AA del UI-SPEC, añadir los tokens numéricos de "movimiento moderado" del Motion Contract, migrar las reglas base globales (html/body/::selection/button) y el sistema de reveal compartido (`[data-reveal]`, `.title-line`) a esos tokens, y retunear la constante Lenis `lerp` de `0.07` a `0.1`.

Este es el cimiento del que dependen los tres planes de sección siguientes (02, 03, 04): ninguna sección puede migrar sus selectores consumidores hasta que los nuevos tokens existan en `:root`.

Purpose: THEME-01 (paleta clara), THEME-02 (tokens con contraste AA verificado), THEME-03 (retiro de los tokens oscuros base) y THEME-04 (spec literal de movimiento como tokens `:root` + retune de Lenis lerp).

Output: `:root` reescrito con todos los tokens claros y de movimiento; reglas base y sistema de reveal migrados; `lib/motion-preferences.ts` retuneado.

Nota de alcance (D-02): NO se actualiza `PALETA-DE-MARCA.md` en esta fase — es documentación de referencia, fuera del alcance de ejecución.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fundaci-n-tema-claro-y-especificaci-n-de-movimiento-moderado/01-UI-SPEC.md
@.planning/phases/01-fundaci-n-tema-claro-y-especificaci-n-de-movimiento-moderado/01-RESEARCH.md
@.planning/phases/01-fundaci-n-tema-claro-y-especificaci-n-de-movimiento-moderado/01-VALIDATION.md
</context>

<artifacts_this_phase_produces>
Nuevas CSS custom properties creadas en este plan (excluidas de cualquier verificación de drift — son símbolos nuevos, no preexistentes):

`--bg-surface`, `--bg-surface-alt`, `--bg-surface-deep`, `--ink-primary`, `--ink-secondary`, `--accent`, `--accent-hover`, `--accent-soft`, `--destructive`, `--success`, `--border-subtle`, `--border-strong`, `--focus-ring`, `--motion-distance-max`, `--motion-distance-min`, `--motion-duration-fast`, `--motion-duration-base`, `--motion-duration-slow`, `--motion-stagger-max`, `--ease-moderate`.

Tokens preexistentes conservados sin cambios: `--display`, `--body`, `--shell`.
Nuevos archivos: ninguno (solo modificaciones a archivos existentes).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Reescribir el bloque :root con el layer de tokens claros + tokens de movimiento</name>
  <files>app/globals.css</files>
  <read_first>
    - app/globals.css (línea 1-9 — bloque `:root` actual y el grupo `.mono-label,.section-kicker,...` con `font-weight:600`)
    - 01-UI-SPEC.md — sección "Color > Token Layer" (valores hex exactos), tabla "Contrast Verification", y sección "Motion & Interaction Contract > Reveal Animation Constants" (valores de tokens de movimiento)
    - 01-RESEARCH.md — "Code Examples > `:root` token block — before/after" (mapa exacto), y "Open Questions #3" (definición del token `--success`)
  </read_first>
  <action>
    Reemplazar íntegramente los tokens legacy del bloque `:root` (`--ink:#0c0e12`, `--soft:#14171d`, `--paper:#eeeae3`, `--orange:#e46d4b`, `--hot:#ff7852`, `--dark-line`, `--light-line`) por el layer completo de tokens claros del UI-SPEC, conservando `--display`, `--body` y `--shell` sin cambios.

    Definir exactamente estos valores (verbatim del UI-SPEC Color section): `--bg-surface:#FFFFFF`, `--bg-surface-alt:#EEF1F4`, `--bg-surface-deep:#E3E8ED`, `--ink-primary:#1C2530`, `--ink-secondary:#4A5568`, `--accent:#0B6E8F`, `--accent-hover:#0A5A78`, `--accent-soft:#E4F0F4`, `--destructive:#B3261E`, `--border-subtle:#DCE2E8`, `--border-strong:#7C899A`, `--focus-ring:var(--accent)`.

    Definir el token `--success` (Open Question #3, resuelta): `--success:#1E7A3E` — un verde legible análogo a `--destructive`, para `.form-status.is-success` (que se aplicará en el Plan 04). Debe verificarse ≥4.5:1 contra `--bg-surface` (#FFFFFF) con un contrast checker; #1E7A3E computa ≈4.77:1.

    Añadir en el mismo bloque `:root` los tokens de movimiento del Motion Contract (THEME-04), valores verbatim del UI-SPEC "Reveal Animation Constants": `--motion-distance-max:24px`, `--motion-distance-min:12px`, `--motion-duration-fast:200ms`, `--motion-duration-base:450ms`, `--motion-duration-slow:650ms`, `--ease-moderate:cubic-bezier(0.33,1,0.68,1)`.

    IMPORTANTE — valor de stagger: `--motion-stagger-max:60ms` (SESENTA, no 80). El UI-SPEC Motion Contract y el quality gate de esta fase fijan 60ms como el cap bloqueante; el bloque de código de ejemplo en 01-RESEARCH.md muestra `80ms` por error — usar 60ms, no 80ms.

    Consolidación tipográfica (UI-SPEC Typography, confirmada por usuario 2026-07-18): cambiar el `font-weight:600` del selector agrupado `.mono-label,.section-kicker,.hero-index,.scroll-cue,.frame-coordinates,.tech-caption` (línea 8) a `font-weight:500`. El sistema tipográfico usa exactamente 2 pesos: 500 y 400.
  </action>
  <acceptance_criteria>
    - `grep -nE "#0c0e12|#0c0f12|#0a0c0f|#080a0d|--ink:|--soft:|--paper:|--orange:|--hot:|--dark-line|--light-line" app/globals.css` → cero hits en el bloque `:root` (líneas 1-6). (Otras secciones aún migran en planes 02-04; este criterio aplica al `:root` únicamente.)
    - `grep -c "\-\-bg-surface\|--ink-primary\|--accent\|--motion-distance-max\|--motion-duration-base\|--ease-moderate\|--success" app/globals.css` → ≥1 (los nuevos tokens existen).
    - `grep -n "\-\-motion-stagger-max:60ms" app/globals.css` → 1 hit (60ms, no 80ms).
    - `grep -n "font-weight:600" app/globals.css` → 0 hits en el selector `.mono-label,.section-kicker,...` (consolidado a 500; nota: pueden existir 600 en otros selectores de sección aún no migrados).
    - Contrast check manual: `--success` (#1E7A3E) sobre `--bg-surface` (#FFFFFF) ≥ 4.5:1 (WebAIM u otro).
  </acceptance_criteria>
  <verify>
    <automated>grep -nE "^:root|--bg-surface|--motion-stagger-max:60ms|--ease-moderate|--success" app/globals.css</automated>
    <human-check>Verificar en WebAIM contrast checker que --success (#1E7A3E) sobre #FFFFFF ≥ 4.5:1.</human-check>
  </verify>
  <done>El bloque `:root` contiene los 20 tokens nuevos (color + movimiento) con los valores exactos del UI-SPEC, `--motion-stagger-max` es 60ms, el grupo `.mono-label` usa font-weight:500, y ningún token legacy sobrevive en `:root`.</done>
</task>

<task type="auto">
  <name>Task 2: Migrar reglas base globales + sistema de reveal a tokens + retunear Lenis lerp</name>
  <files>app/globals.css, lib/motion-preferences.ts</files>
  <read_first>
    - app/globals.css (línea 7 — `html`/`body`/`::selection`/`button`; línea 9 — `[data-reveal]`, `.title-line[data-reveal]>span`, y los `transition-delay` de `.title-line:nth-child(2/3/4)`)
    - lib/motion-preferences.ts (archivo completo — 10 líneas; `LENIS_LERP.normal` en la línea 2)
    - 01-RESEARCH.md — "Code Examples > [data-reveal] before/after" y "Lenis lerp retune before/after"; "Migration Map — Motion Constants" (filas de `[data-reveal]`, `.title-line`, stagger, Lenis lerp)
    - 01-UI-SPEC.md — "Motion & Interaction Contract > Scroll Engine" (lerp 0.1) y "Common Pitfalls" Pitfall 1 (para el fix de `::selection`)
  </read_first>
  <action>
    En `app/globals.css` línea 7, migrar las reglas base a los nuevos tokens: `html{background:var(--ink)}` → `background:var(--bg-surface)`; `body{background:var(--paper);color:var(--ink)}` → `background:var(--bg-surface);color:var(--ink-primary)`. Dejar `body.menu-open{overflow:hidden}` sin tocar (mecanismo de scroll-lock, lo reemplaza la Fase 2). Para `button{color:inherit}` no hay literal que migrar (hereda), dejar igual.

    `::selection` — aplicar el fix de Pitfall 1 (no un rename mecánico): actualmente `::selection{color:var(--ink);background:var(--hot)}` empareja texto oscuro sobre el acento, un par NO verificado por el UI-SPEC. Cambiar a `background:var(--accent);color:#FFFFFF` (texto blanco sobre acento — el único par acento-fondo verificado por el UI-SPEC, 5.77:1).

    En la línea 9, migrar los literales de movimiento del reveal a tokens (THEME-04):
    - `[data-reveal]{opacity:0;transform:translateY(44px);transition:opacity .9s cubic-bezier(.16,1,.3,1),transform .9s cubic-bezier(.16,1,.3,1)}` → `translateY(var(--motion-distance-max))`, y ambas transiciones a `var(--motion-duration-base) var(--ease-moderate)`.
    - `.title-line[data-reveal]>span{transition:transform 1.15s cubic-bezier(.16,1,.3,1)}` → `transition:transform var(--motion-duration-slow) var(--ease-moderate)`. NO cambiar `translateY(115%)` (es una distancia de máscara relativa a line-height, no cubierta por `--motion-distance-*`).
    - Retunear los `transition-delay` de stagger a una cadencia consistente de 60ms (cap del Motion Contract): `.title-line:nth-child(2)>span` → `.06s`, `nth-child(3)` → `.12s`, `nth-child(4)` → `.18s` (reemplazan los actuales `.08s`/`.16s`/`.23s`).

    En `lib/motion-preferences.ts` línea 2, cambiar `normal: 0.07` a `normal: 0.1` (lerp moderado documentado de Lenis). Dejar `reduced: 0.15` sin cambios. NO tocar `getLenisLerp()` ni `components/providers/smooth-scroll-provider.tsx` — ya consumen la constante correctamente.
  </action>
  <acceptance_criteria>
    - `grep -n "translateY(44px)\|\.9s cubic-bezier\|1.15s\|transition-delay:.08s\|transition-delay:.16s\|transition-delay:.23s" app/globals.css` → 0 hits.
    - `grep -n "var(--motion-distance-max)\|var(--motion-duration-base)\|var(--motion-duration-slow)\|var(--ease-moderate)" app/globals.css` → ≥1 hit cada uno (reveal consume tokens).
    - `grep -n "::selection" app/globals.css` → la regla usa `background:var(--accent)` y `color:#FFFFFF` (no `var(--hot)` ni `var(--ink)`).
    - `grep -n "normal: 0.1" lib/motion-preferences.ts` → 1 hit; `grep -n "0.07" lib/motion-preferences.ts` → 0 hits.
    - `npm run typecheck` pasa sin errores.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && grep -n "normal: 0.1" lib/motion-preferences.ts && grep -nE "var\(--motion-duration-base\)|var\(--ease-moderate\)" app/globals.css</automated>
  </verify>
  <done>`[data-reveal]` y `.title-line` consumen los tokens de movimiento; el stagger usa cadencia de 60ms; `::selection` usa blanco sobre acento; `LENIS_LERP.normal` es 0.1; typecheck limpio.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Ninguna. Esta fase modifica valores de CSS custom properties y dos constantes numéricas de movimiento (`lib/motion-preferences.ts`). No cruza ningún límite de confianza: sin entrada de usuario, sin red, sin persistencia.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-NONE | — | Cambios CSS + 2 constantes de movimiento | accept | Sin superficie de seguridad. Por 01-RESEARCH.md "Security Domain": no se tocan autenticación, sesión, control de acceso, validación de entrada ni criptografía. No aplica ninguna categoría ASVS L1. No se instalan paquetes (sin `T-01-SC`). |
</threat_model>

<verification>
- `npm run typecheck` limpio.
- `:root` contiene los 20 tokens nuevos con valores exactos del UI-SPEC; `--motion-stagger-max:60ms`.
- Reglas base y sistema de reveal consumen tokens; `::selection` con fix de Pitfall 1.
- `LENIS_LERP.normal = 0.1`.
</verification>

<success_criteria>
- El bloque `:root` no contiene tokens legacy; contiene todos los tokens de color y movimiento del UI-SPEC.
- `[data-reveal]`/`.title-line` referencian `var(--motion-*)`; stagger a 60ms.
- `lib/motion-preferences.ts` retuneado a `normal: 0.1`.
- Base para los planes 02-04 (todos los tokens consumibles existen).
</success_criteria>

<output>
Create `.planning/phases/01-fundaci-n-tema-claro-y-especificaci-n-de-movimiento-moderado/01-01-SUMMARY.md` when done
</output>
