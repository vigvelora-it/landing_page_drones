---
phase: 06-calidad-y-regresi-n-final
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/06-calidad-y-regresi-n-final/evidence/quality-gates.md
  - .planning/phases/06-calidad-y-regresi-n-final/evidence/browser-matrix.md
  - .planning/phases/06-calidad-y-regresi-n-final/evidence/media-inventory.md
  - .planning/phases/06-calidad-y-regresi-n-final/evidence/contact-regression.md
  - .planning/phases/06-calidad-y-regresi-n-final/06-VALIDATION.md
  - .planning/phases/06-calidad-y-regresi-n-final/06-VERIFICATION.md
  - components/menu-overlay.tsx
  - components/service-drawer.tsx
  - components/contact-form.tsx
  - components/equipment-carousel.tsx
  - components/sections/hero-section.tsx
  - app/globals.css
autonomous: true
requirements: [QA-01, QA-02, BRAND-02]
requirements_blocked: [QA-03]
user_setup: []

must_haves:
  truths:
    - "Lint, typecheck y build de producción pasan secuencialmente contra el código final"
    - "Los cuatro viewports no tienen overflow horizontal ni errores de consola y el sistema header/menu/drawer/sticky/carrusel funciona como una unidad"
    - "Reduced motion, teclado, touch, no-JavaScript y zoom conservan contenido y controles utilizables"
    - "El formulario conserva POST /api/contact, validación, estados pending/success/error y wiring Supabase sin crear filas ni mensajes externos durante QA"
    - "El inventario de medios registra sujeto, alt y provenance reales; QA-03 permanece abierto mientras falten geología/minería autorizadas y provenance de PNG/JPG"
    - "TEAM-01 y BROCH-01 continúan parciales/bloqueados; ninguna evidencia de Fase 6 los marca completos"
  artifacts:
    - path: ".planning/phases/06-calidad-y-regresi-n-final/evidence/quality-gates.md"
      provides: "Comandos finales, orden, timestamps y exit codes de lint/typecheck/build/start"
      contains: "npm.cmd run build"
    - path: ".planning/phases/06-calidad-y-regresi-n-final/evidence/browser-matrix.md"
      provides: "Matriz reproducible de cuatro viewports y modos runtime, con escenarios y resultados"
      contains: "1440x900"
    - path: ".planning/phases/06-calidad-y-regresi-n-final/evidence/media-inventory.md"
      provides: "Inventario semántico y de provenance que demuestra el gap real de QA-03"
      contains: "QA-03"
    - path: ".planning/phases/06-calidad-y-regresi-n-final/evidence/contact-regression.md"
      provides: "Evidencia API/browser no destructiva y auditoría del wiring del formulario"
      contains: "/api/contact"
    - path: ".planning/phases/06-calidad-y-regresi-n-final/06-VERIFICATION.md"
      provides: "Resultado agregado de QA-01, QA-02, BRAND-02 y gap QA-03"
      contains: "requirements verified"
  key_links:
    - from: "components/contact-form.tsx"
      to: "app/api/contact/route.ts"
      via: "fetch POST JSON a /api/contact"
      pattern: "fetch\\(\"/api/contact\""
    - from: "app/api/contact/route.ts"
      to: "lib/contact-schema.ts"
      via: "contactSchema.safeParse antes de persistencia"
      pattern: "contactSchema.safeParse"
    - from: "app/api/contact/route.ts"
      to: "lib/supabase-admin.ts"
      via: "createSupabaseAdmin y contact_requests.insert solo después de validar"
      pattern: "createSupabaseAdmin"
    - from: ".planning/phases/06-calidad-y-regresi-n-final/evidence/media-inventory.md"
      to: "public/video/CREDITS.md"
      via: "provenance comprobable del único medio con créditos locales"
      pattern: "drone-flight-close.mp4"
---

<objective>
Cerrar la regresión técnica e interactiva del milestone sobre un build local de producción, reparar únicamente defectos reproducidos y dejar evidencia auditable sin despliegue ni escrituras externas. Completar honestamente QA-01, QA-02 y BRAND-02; documentar QA-03 como bloqueado por assets/provenance faltantes.

Purpose: demostrar que las capas construidas en Fases 1–5 funcionan juntas en desktop, tablet y móvil, y que el formulario mantiene su contrato seguro después del rediseño.
Output: gates finales, matriz de navegador, inventario de medios, regresión del formulario y `06-VERIFICATION.md`; cualquier fix se limita al componente que falle.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-calidad-y-regresi-n-final/06-UI-SPEC.md
@.planning/phases/06-calidad-y-regresi-n-final/06-RESEARCH.md
@.planning/phases/06-calidad-y-regresi-n-final/06-VALIDATION.md
@.planning/phases/05-contenido-de-marca-historia-equipo-proyectos-diferenciaci-n-/05-VERIFICATION.md
@package.json
@app/layout.tsx
@app/page.tsx
@app/api/contact/route.ts
@components/contact-form.tsx
@components/menu-overlay.tsx
@components/service-drawer.tsx
@components/equipment-carousel.tsx
@components/sections/hero-section.tsx
@components/sections/technology-section.tsx
@hooks/use-scroll-lock.ts
@lib/contact-schema.ts
@lib/supabase-admin.ts
@lib/site-content.ts
@app/globals.css
@public/video/CREDITS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Ejecutar preflight seguro, gates secuenciales y producción local identificada</name>
  <files>.planning/phases/06-calidad-y-regresi-n-final/evidence/quality-gates.md</files>
  <read_first>
    - `06-RESEARCH.md` "Lifecycle seguro de build/start en PowerShell" — usar la ruta absoluta de local-2 y detener únicamente el listener verificado.
    - `package.json` — usar `npm.cmd`; ejecutar lint, typecheck y build en ese orden; nunca invocar deploy.
    - `.env.example` y `lib/supabase-admin.ts` — registrar solo booleanos de presencia, nunca valores.
    - `.planning/config.json` — `workflow.use_worktrees:false`; toda la ejecución es secuencial.
  </read_first>
  <action>
    Crear el directorio `evidence/` dentro de la carpeta de Fase 6. Resolver `F:\ClaudeCode\Pagina_Web_Mayra\local-2` con `Resolve-Path`. Consultar el listener de `127.0.0.1:4173`; si existe, obtener su PID y `Win32_Process.CommandLine`, exigir que pertenezca a esa ruta y a `next start -p 4173`, y detener solo ese PID exacto. Si el puerto pertenece a otro proceso, abortar sin matar nada. No usar búsquedas globales ni `Stop-Process` sobre todos los Node.

    Registrar en `quality-gates.md` fecha/hora, ruta, estado del listener y únicamente booleanos para archivos `.env*` distintos de `.env.example`, `NEXT_PUBLIC_SUPABASE_URL` y `SUPABASE_SERVICE_ROLE_KEY`; no copiar command lines completas si contienen argumentos sensibles. Ejecutar, en PowerShell 5.1 y sin `&&`, `npm.cmd run lint`, comprobar `$LASTEXITCODE`, después `npm.cmd run typecheck`, comprobar, y finalmente `npm.cmd run build`, comprobar. Capturar comando, orden, exit code y resumen no sensible. Si un gate falla, corregir solo la causa concreta en una tarea posterior y reiniciar la secuencia completa; nunca borrar `.next` sin un error demostrado.

    Iniciar `npm.cmd run start` mediante `Start-Process -WindowStyle Hidden -PassThru`, con stdout/stderr temporales dentro de `evidence/`. Esperar como máximo 20 segundos en intervalos cortos, resolver de nuevo el listener 4173 y confirmar que la command line final pertenece a `local-2` y `next start -p 4173`; luego verificar HTTP 200 en `http://127.0.0.1:4173/`. Revisar logs por secretos antes de conservar cualquier resumen; no versionar logs crudos. No ejecutar Vercel, `npm run deploy`, una URL externa ni un servicio ajeno a localhost.
  </action>
  <verify>
    <automated>Set-Location 'F:\ClaudeCode\Pagina_Web_Mayra\local-2'; $quality = '.planning/phases/06-calidad-y-regresi-n-final/evidence/quality-gates.md'; if (-not (Test-Path -LiteralPath $quality)) { exit 1 }; rg -n "npm\.cmd run lint|npm\.cmd run typecheck|npm\.cmd run build|Exit code: 0|127\.0\.0\.1:4173" $quality; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; $listener = Get-NetTCPConnection -LocalPort 4173 -State Listen -ErrorAction SilentlyContinue; if (-not $listener) { exit 1 }; $runtime = Get-CimInstance Win32_Process -Filter "ProcessId = $($listener.OwningProcess)"; if (-not $runtime -or $runtime.CommandLine -notlike '*F:\ClaudeCode\Pagina_Web_Mayra\local-2*next*start*-p 4173*') { exit 1 }; $response = Invoke-WebRequest -UseBasicParsing 'http://127.0.0.1:4173/'; if ($response.StatusCode -ne 200) { exit 1 }; exit 0</automated>
  </verify>
  <acceptance_criteria>
    - Solo se detuvo el PID cuyo listener/command line fue verificado como `local-2 next start -p 4173`.
    - Lint, typecheck y build registran exit 0 y se ejecutaron secuencialmente.
    - El servidor nuevo responde 200 en localhost y su PID/ruta están verificados.
    - La evidencia contiene booleanos de configuración, no valores, tokens, payloads reales ni logs crudos.
    - No hubo deploy, acceso a servicios externos ni modificaciones fuera de `local-2`.
  </acceptance_criteria>
  <done>Existe un build final limpio servido por un proceso local identificado, con QA-01 reproducible y sin afectar procesos o servicios fuera del alcance.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Ejecutar la matriz browser combinada y reparar solo regresiones reproducidas</name>
  <files>.planning/phases/06-calidad-y-regresi-n-final/evidence/browser-matrix.md, components/menu-overlay.tsx, components/service-drawer.tsx, components/contact-form.tsx, components/equipment-carousel.tsx, components/sections/hero-section.tsx, app/globals.css</files>
  <read_first>
    - `06-UI-SPEC.md` "QA Matrix and Evidence Contract", "Combined Interaction Stress Test", "Accessibility, Motion and Resilience Gates" — ejecutar todos los modos y estados sin reinterpretarlos.
    - `06-RESEARCH.md` "QA-02: Browser Matrix and Stress Test" y "Likely Gaps and Minimal Repairs" — medir overflow y arreglar el selector culpable, nunca ocultarlo globalmente.
    - `05-VERIFICATION.md` — usar como baseline únicamente; repetir todo contra el build nuevo.
    - `menu-overlay.tsx`, `service-drawer.tsx`, `equipment-carousel.tsx`, `hero-section.tsx`, `use-scroll-lock.ts` y `app/globals.css` — preservar autoridades existentes de foco, Lenis, Embla y reduced-motion.
  </read_first>
  <action>
    Usar `playwright-cli.cmd` contra `http://127.0.0.1:4173` y crear `browser-matrix.md` con viewport, modo, pasos, resultados, `scrollWidth-clientWidth`, un solo h1, errores/warnings de consola y screenshot asociado. Ejecutar el recorrido vertical completo en 1440x900, 1000x800, 390x844 y 320x568. En cada uno exigir overflow 0, contenido sin truncar y consola sin errores. Si aparece overflow, enumerar por bounding boxes el elemento cuyo `left<0` o `right>clientWidth`, corregir su width/min-width/gap localmente y conservar como única supresión global la ocurrencia ya existente en body; no añadir `overflow-x` a html, body o ancestros grandes.

    En 1440x900 ejecutar en orden el stress test: carga top sin `.site-header.is-scrolled`; flick que cruce 80px y retorno, sin cambio de altura/flicker; menú abierto desde scroll con Lenis bloqueado, seis destinos visibles/teclado, exclusión con drawer, Escape/cierre/navegación liberan scroll y restauran foco; drawer con fondo fuera del boundary inert, menú excluido, body interno desplazable, Escape/backdrop/cierre y foco al servicio; CTA `Cotizar este servicio` cierra y llega a `#contacto`; `.tech-sticky` pin/release y EquipmentShowcase posterior sin stacking incorrecto; carrusel por viewport enfocado con ArrowRight/Left, botones extremos, dots/`aria-selected`/live region y `data-lenis-prevent`; drag horizontal real mediante pointer y touch emulado mueve el snap mientras un gesto vertical sigue desplazando la página. Esperar al menos 10 segundos en snap 1 y confirmar que no cambia; comprobar en fuente que no hay interval/timer/autoplay para progresión.

    Repetir los controles críticos con `prefers-reduced-motion: reduce` en 1440x900 y 390x844: video pausado, reveals visibles, transiciones decorativas efectivamente anuladas, navegación del carrusel por controles instantánea y drag conservado. Ejecutar keyboard-only en desktop y móvil principal: foco visible, orden lógico, overlays cerrables y foco restaurado. Ejecutar JavaScript desactivado en 390x844: intro oculta, todos los reveals esenciales visibles, Nosotros/Proyectos/Contacto/email accesibles y overflow 0. Comprobar 200% zoom real si el harness lo permite; si no, usar y documentar un proxy CSS de zoom 2 en desktop, sin scroll bidimensional ni controles inaccesibles.

    Si una aserción falla, reproducirla antes de editar y aplicar el fix más pequeño al componente responsable, reutilizando tokens/copy/librerías existentes. No rediseñar, no añadir dependencia, sección, color, claim ni animación. Detener el `next start` solo mediante la verificación de PID de Task 1 antes de reconstruir; volver a ejecutar lint, typecheck y build secuencialmente, reiniciar producción local, repetir el escenario fallido y después el smoke completo. Si no hay defecto, no tocar código. Guardar screenshots estables de desktop/mobile principal, menú/drawer/carrusel y reduced motion; no versionar temporales del runner.
  </action>
  <verify>
    <automated>Set-Location 'F:\ClaudeCode\Pagina_Web_Mayra\local-2'; $matrix = '.planning/phases/06-calidad-y-regresi-n-final/evidence/browser-matrix.md'; if (-not (Test-Path -LiteralPath $matrix)) { exit 1 }; rg -n "1440x900|1000x800|390x844|320x568|reduced motion|JavaScript disabled|keyboard|touch|200%|No autoplay|overflow.*0|console errors.*0" $matrix; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; $overflowCount = (rg -n "overflow-x" app/globals.css | Measure-Object -Line).Lines; if ($LASTEXITCODE -gt 1 -or $overflowCount -ne 1) { exit 1 }; rg -n "data-lenis-prevent|loop: false|onKeyDown|aria-live=\"polite\"" components/equipment-carousel.tsx; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; rg -ni "autoplay|setInterval|setTimeout" components/equipment-carousel.tsx; if ($LASTEXITCODE -eq 0) { exit 1 }; if ($LASTEXITCODE -gt 1) { exit $LASTEXITCODE }; npm.cmd run lint; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm.cmd run typecheck; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm.cmd run build; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; exit 0</automated>
    <human-check>
      <test>Revisar los screenshots y el resumen Playwright de los cuatro viewports, overlays, carrusel, reduced-motion y no-JS.</test>
      <expected>Jerarquía visual estable, cero overflow/errores, foco/scroll restaurados y ningún cambio de diseño fuera de un fix reproducido.</expected>
      <why_human>Composición visual, stacking y calidad de interacción requieren evidencia visual además de aserciones DOM.</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - Los cuatro viewports registran overflow 0 y cero errores de consola durante el recorrido completo.
    - Header, menú, drawer, CTA a contacto, sticky y carrusel pasan juntos en el orden contratado.
    - Carrusel pasa teclado, botones, dots, touch/drag, extremos, live region y espera de 10s sin autoplay.
    - Reduced motion desktop/móvil, no-JS móvil, keyboard-only y zoom 200% quedan documentados y utilizables.
    - Cualquier reparación está ligada a un fallo reproducido, mantiene tokens/copy/dependencias y fue revalidada con gates finales.
  </acceptance_criteria>
  <done>QA-02 cuenta con evidencia browser completa contra producción local y cualquier regresión reproducida quedó reparada sin workaround global ni rediseño.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Auditar medios y verificar el formulario sin escrituras externas</name>
  <files>.planning/phases/06-calidad-y-regresi-n-final/evidence/media-inventory.md, .planning/phases/06-calidad-y-regresi-n-final/evidence/contact-regression.md, .planning/phases/06-calidad-y-regresi-n-final/06-VALIDATION.md, .planning/phases/06-calidad-y-regresi-n-final/06-VERIFICATION.md, components/contact-form.tsx</files>
  <read_first>
    - `06-UI-SPEC.md` "Image Subject Balance" y "Contact Form Regression" — QA-03 es semántica/provenance, BRAND-02 es no destructiva.
    - `06-RESEARCH.md` "Rendered Media and Provenance Audit" y "Non-Destructive Contact Verification" — inventario baseline y casos seguros.
    - `app/api/contact/route.ts`, `contact-form.tsx`, `contact-schema.ts`, `supabase-admin.ts` — contrato real y mensajes canónicos.
    - `public/video/CREDITS.md`, `public/IMAGENES_PAGINA_WEB/` y `.env.example` — provenance disponible y configuración esperada.
    - `05-VERIFICATION.md` — TEAM-01/BROCH-01 son gaps heredados y no pertenecen a requirements de este plan.
  </read_first>
  <action>
    Crear `media-inventory.md` enumerando cada `img`/video efectivamente renderizado en producción local: sección, ruta/currentSrc, alt o condición decorativa, sujeto observado visualmente, provenance local verificable y eje defendible. Diferenciar el video con `CREDITS.md` de los PNG/JPG sin registro. Registrar que drone/topografía, ingeniería/geomatica de campo e infraestructura genérica tienen evidencia parcial, pero geología y minería no están respaldaldadas simultáneamente por assets autorizados y la mayoría de PNG/JPG carece de provenance. No inferir por filename, no reclasificar equipo/montañas como mina o campaña, no asociar imágenes a clientes/personas, no copiar/generar/renombrar assets. Mantener QA-03 unchecked y en `requirements_blocked`; esta auditoría no modifica código visual.

    Verificar BRAND-02 únicamente contra localhost. Confirmar de nuevo, como booleanos, que no hay `.env*` real ni las dos variables Supabase. Enviar directamente a `/api/contact`: JSON malformado → 400/copy canónico; objeto schema-invalid → 400/primer mensaje canónico; honeypot `website` no vacío → 400 `Solicitud no válida.`; solo si ambas credenciales están ausentes, payload sintético válido con `qa@example.invalid` → 503/fallback canónico, demostrando que falla antes de Supabase. Si aparece cualquier credencial, no enviar payload válido al endpoint y registrar la omisión segura; nunca imprimir valores. No probar 201 real, no crear fila, no enviar correo/WhatsApp y no llamar hosts externos.

    Con Playwright, interceptar same-origin `**/api/contact` antes de red y usar únicamente `Persona QA`, `qa@example.invalid` y texto sintético. Probar mock 201 con demora 250ms: pending visible, submit disabled, una sola request frente a doble activación, luego reset/enabled/success canónico. Probar mock 400 y 500: valores conservados, button reactivado y mensaje español mockeado en `#form-status`. Retirar cada route al terminar. Si la doble activación produce más de una request, añadir una guardia local de pending en `ContactForm` sin cambiar API/copy y repetir; cualquier otro fix debe ser mínimo y reproducido.

    En `contact-regression.md` registrar status/copy, contador de requests, booleanos env y grep estructural: `ContactForm` → POST `/api/contact` → `contactSchema.safeParse` → `createSupabaseAdmin` → `contact_requests.insert`; opciones derivan de `services`, honeypot queda fuera de tab, y `aria-live="polite"` permanece. No incluir payloads reales, URLs/keys, headers sensibles ni logs. Actualizar `06-VALIDATION.md` con resultados reales y crear `06-VERIFICATION.md` verificando hacia atrás: QA-01, QA-02 y BRAND-02 completos solo si su evidencia pasa; QA-03 gap/blocked; TEAM-01 parcial y BROCH-01 bloqueado como gaps heredados, sin cambiar sus checkboxes.
  </action>
  <verify>
    <automated>Set-Location 'F:\ClaudeCode\Pagina_Web_Mayra\local-2'; $media = '.planning/phases/06-calidad-y-regresi-n-final/evidence/media-inventory.md'; $contact = '.planning/phases/06-calidad-y-regresi-n-final/evidence/contact-regression.md'; $verification = '.planning/phases/06-calidad-y-regresi-n-final/06-VERIFICATION.md'; foreach ($file in @($media,$contact,$verification)) { if (-not (Test-Path -LiteralPath $file)) { exit 1 } }; rg -n "drone/topograf|ingenier|infraestructura|geolog|miner|provenance|QA-03.*(blocked|bloqueado|gap)" $media; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; rg -n "400|503|201|pending|success|error|request|sin escritura|no external" $contact; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; rg -n "fetch\(\"/api/contact\"|aria-live=\"polite\"|services\.map" components/contact-form.tsx; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; rg -n "contactSchema.safeParse|createSupabaseAdmin|contact_requests" app/api/contact/route.ts; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; rg -n "QA-01|QA-02|QA-03|BRAND-02|TEAM-01|BROCH-01" $verification; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; exit 0</automated>
  </verify>
  <acceptance_criteria>
    - Inventario cubre cada medio visible con sujeto, alt/decorativo, provenance y eje honesto.
    - QA-03 permanece unchecked/bloqueado; no se añadió ni reinterpretó ningún asset.
    - API devuelve los 400 esperados y, con configuración ausente, 503; ninguna petición puede alcanzar Supabase.
    - Mocks browser prueban pending, doble envío bloqueado, 201 success/reset y 400/500 error/preservación sin salir de localhost.
    - Wiring estructural, servicios canónicos, honeypot y live region quedan demostrados.
    - `06-VERIFICATION.md` no fuerza milestone completo ni altera TEAM-01/BROCH-01.
  </acceptance_criteria>
  <done>BRAND-02 queda verificado sin persistencia externa, QA-03 queda documentado como gap real y el reporte final refleja exactamente los requisitos probados y bloqueados.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| listener/PID del sistema → lifecycle de `.next` | Solo el `next start` confirmado como local-2 puede detenerse antes de reconstruir |
| navegador/API local → Supabase | Los tests deben terminar por validación/configuración o interception antes de cualquier insert externo |
| variables/archivos env → evidencia versionada | La evidencia registra presencia booleana, nunca secretos ni command lines sensibles |
| assets locales → claims públicos | Un archivo solo prueba el sujeto y provenance observados; no autoriza identidad, cliente, mina o campaña |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Denial of Service | Process lifecycle | mitigate | Resolver listener 4173, PID y command line; abortar si no coincide con la ruta absoluta de local-2; detener solo el PID exacto. |
| T-06-02 | Tampering | Build concurrente | mitigate | Detener el servidor verificado antes de build y ejecutar lint/typecheck/build secuencialmente; no borrar `.next` salvo error demostrado. |
| T-06-03 | Information Disclosure | Env/log evidence | mitigate | Registrar booleanos, revisar salidas y excluir logs crudos/keys/URLs/payloads sensibles de commits y screenshots. |
| T-06-04 | Tampering / Repudiation | Contact persistence | mitigate | 400 antes de Supabase, 503 solo con credenciales ausentes y route interception para 201/error; cero insert, email, WhatsApp o host externo. |
| T-06-05 | Spoofing / Integrity | Media provenance | mitigate | Inventario visual + créditos locales; no inferir por filename ni fabricar/copy/generar assets; QA-03 queda blocked. |
| T-06-06 | Tampering | QA repair | mitigate | Reproducir primero, parche local mínimo, un solo motor por interacción y rerun completo de gates/matriz afectada. |
</threat_model>

<source_coverage>
## Multi-Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | build limpio | Task 1 y gate final de Task 2 |
| GOAL | no overflow + sistema interactivo completo | Task 2, cuatro viewports y stress test ordenado |
| GOAL | balance geología/ingeniería/minería/infraestructura | Task 3 audita; QA-03 queda bloqueado por información/assets autorizados ausentes, causa legítima documentada |
| GOAL | formulario intacto | Task 3, API local + mocks browser + wiring |
| REQ QA-01 | lint/typecheck/build | Tasks 1–2; requirement ejecutable incluido |
| REQ QA-02 | revisión desktop/móvil combinada | Task 2; requirement ejecutable incluido |
| REQ QA-03 | medios no centrados solo en drones | Task 3; `requirements_blocked` porque faltan evidencia autorizada de geología/minería y provenance PNG/JPG |
| REQ BRAND-02 | /api/contact + Supabase sin regresión | Task 3; requirement ejecutable incluido sin escritura externa |
| RESEARCH | PID seguro, PowerShell 5.1, secuencia build/start | Task 1 |
| RESEARCH | 1440/1000/390/320, reduced/no-JS/keyboard/touch/zoom/no-autoplay | Task 2 |
| RESEARCH | API 400/503, mocks 201/error, inventario/provenance | Task 3 |
| UI-SPEC | QA repara sin rediseñar | Todas las tasks limitan fixes al fallo reproducido |
| CONTEXT | No existe 06-CONTEXT.md | N/A; UI-SPEC aprobado y RESEARCH son autoridad |
| Phase 5 gaps | TEAM-01 y BROCH-01 | Excluidos del requirements array y preservados como gaps heredados |

No hay ítems silenciosamente omitidos. QA-03 está cubierto por auditoría y explícitamente bloqueado por missing information/assets; no es completado de forma falsa.
</source_coverage>

<verification>
- Calidad: PID verificado; lint, typecheck y build secuenciales contra código final; HTTP 200 en producción local.
- Browser: cuatro viewports, recorrido completo, overflow/consola, stress header/menu/drawer/sticky/carrusel, no autoplay, reduced motion, no-JS, teclado, touch y zoom.
- Medios: inventario DOM + inspección visual + provenance; QA-03 gap documentado sin cambios de assets.
- Contacto: 400/503 locales seguros, interception 201/400/500, pending/re-submit/success/error, wiring estructural y cero writes externas.
- Release: 06-VERIFICATION refleja 3 requisitos verificables y QA-03 bloqueado; TEAM-01/BROCH-01 permanecen sin marcar.
</verification>

<success_criteria>
- QA-01, QA-02 y BRAND-02 quedan completos solo con evidencia reproducible del código final.
- QA-03 queda abierto con inventario/provenance y dependencia externa concreta, sin asset o claim fabricado.
- Todo fix es mínimo, local, sin nueva dependencia ni alteración del contrato visual/copy.
- No se despliega, no se escribe a Supabase, no se envían mensajes y no se toca ningún entorno hermano.
</success_criteria>

<output>
Create `.planning/phases/06-calidad-y-regresi-n-final/06-01-SUMMARY.md` when done. El summary debe usar `requirements-completed: [QA-01, QA-02, BRAND-02]` solo si pasan; `requirements-blocked: [QA-03]`; TEAM-01/BROCH-01 se mencionan como gaps heredados sin marcarlos completos.
</output>
