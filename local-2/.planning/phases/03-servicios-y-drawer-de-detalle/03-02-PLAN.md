---
phase: 03-servicios-y-drawer-de-detalle
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - components/sections/capabilities-section.tsx
  - app/globals.css
autonomous: true
requirements: [SERV-01, SERV-02, SERV-03]
tags: [services, cards, drawer, css, accessibility]

must_haves:
  truths:
    - "The 5 service ejes render as distinct, individually clickable <button> units (not flowing prose and not anchors)"
    - "Clicking a service row opens the ServiceDrawer populated with that exact service's groups/note"
    - "The custom-cursor label on a service row reads 'Ver detalle' (not 'Cotizar')"
    - "The row whose drawer is open shows a persistent accent selected indicator"
    - "While the menu overlay is open, every service row is visually disabled, carries aria-disabled=true, and its click is a no-op"
  artifacts:
    - path: "components/sections/capabilities-section.tsx"
      provides: "service-row buttons + activeService state + ServiceDrawer render + drawer-side overlay coordination"
      contains: "ServiceDrawer"
    - path: "app/globals.css"
      provides: ".service-row button reset + :focus-visible + aria-disabled treatment + selected-indicator styling"
      contains: "service-row"
  key_links:
    - from: "components/sections/capabilities-section.tsx"
      to: "components/service-drawer.tsx"
      via: "renders <ServiceDrawer service={activeService} isOpen={!!activeService} onClose={...} />"
      pattern: "<ServiceDrawer"
    - from: "components/sections/capabilities-section.tsx"
      to: "hooks/use-overlay-coordination.ts"
      via: "useOverlayCoordination(\"drawer\", !!activeService)"
      pattern: "useOverlayCoordination\\(\"drawer\""
---

<objective>
Convert the existing capabilities service list from anchor rows into accessible `<button>` triggers that open the `ServiceDrawer` from Plan 01, and add the row-level CSS (button reset, focus-visible, selected indicator, disabled treatment). This is where SERV-01 (navigable card units) and the trigger half of SERV-02 (click → drawer) become real, plus the drawer-side half of SERV-03 (rows disabled while menu is open).

Purpose: Wire the already-built drawer to its triggers and reflect UI-SPEC Decision #1 (row-list kept as-is, only behavior changes) and Decision #4 (cursor copy "Ver detalle").
Output: rewritten trigger markup in `components/sections/capabilities-section.tsx` and appended `.service-row` CSS in `app/globals.css`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-servicios-y-drawer-de-detalle/03-UI-SPEC.md
@.planning/phases/03-servicios-y-drawer-de-detalle/03-RESEARCH.md
@.planning/phases/03-servicios-y-drawer-de-detalle/03-01-SUMMARY.md
@F:\ClaudeCode\Pagina_Web_Mayra\local-2\components\sections\capabilities-section.tsx
@F:\ClaudeCode\Pagina_Web_Mayra\local-2\lib\site-content.ts
@F:\ClaudeCode\Pagina_Web_Mayra\local-2\CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert service rows to drawer-trigger buttons</name>
  <files>components/sections/capabilities-section.tsx</files>
  <read_first>
    - components/sections/capabilities-section.tsx — the CURRENT file: rows are `<a className="service-row" href="#contacto" key={service.number} data-reveal data-cursor="Cotizar">`; the `useGSAP` reveal observer on `root` must keep working (buttons still carry `data-reveal`).
    - 03-RESEARCH.md "Code Examples → capabilities-section.tsx trigger conversion sketch" (lines ~339-390) — authoritative reference for the exact JSX shape.
    - 03-RESEARCH.md "Open Questions #2" and the orchestrator-resolved decision: opening a DIFFERENT service while a drawer is open is an instant in-place content swap (single `activeService` state, dialog stays open) — do NOT add close/reopen special-casing.
    - 03-UI-SPEC.md "Service row/card trigger" states table (default/hover unchanged; focus-visible new; selected indicator; disabled treatment) and "Copywriting Contract" (cursor label "Ver detalle").
    - 03-01-SUMMARY.md — confirm the `ServiceDrawer` prop signature and `useOverlayCoordination` export names.
  </read_first>
  <action>
    Rewrite the `services.map(...)` block in `CapabilitiesSection`. Add `useState<Service | null>` named `activeService` (import `type Service` alongside `services` from `@/lib/site-content`). Add `const menuIsOpen = useOverlayCoordination("drawer", !!activeService)` (import from `@/hooks/use-overlay-coordination`) — this both broadcasts the drawer's open state into the shared store (which Plan 03's inert boundary and menu-toggle read) AND returns whether the menu is currently open (SERV-03).
    Change each row from `<a ... href="#contacto">` to `<button type="button" className="service-row" key={service.id} data-reveal data-cursor="Ver detalle" aria-disabled={menuIsOpen} onClick={() => { if (menuIsOpen) return; setActiveService(service) }}>`. Use `key={service.id}` (semantic id now used) not `service.number`. Keep the inner spans (`service-number`, `service-title`, `service-detail`) and the `<Arrow />` glyph exactly as-is (Arrow stays `aria-hidden`, unchanged per UI-SPEC). Append, inside the button when `activeService?.id === service.id`, a `<span className="service-row-selected-indicator" aria-hidden="true" />` (the pre-reserved selected-trigger state). The `data-cursor="Ver detalle"` change works with zero edits to `custom-cursor.tsx` (event delegation, ARCH-03).
    After the `.service-list` div, render the drawer as a sibling inside the component's returned fragment: `<ServiceDrawer service={activeService} isOpen={!!activeService} onClose={() => setActiveService(null)} />` (import from `@/components/service-drawer`). Rendering it here (inside `<main>` via this component) is correct — the dialog escapes ancestor inertness by spec, so it does not need to be lifted to `page.tsx` (03-RESEARCH.md Pattern 1). Do NOT rename the file or component (kept `CapabilitiesSection` per Assumption A2). Preserve the existing `<section>`/`site-shell`/`moving-band` markup and the `useGSAP` reveal observer verbatim.
  </action>
  <verify>
    <automated>from local-2/: npm run lint && npm run typecheck</automated>
    <human-check>Deferred to phase gate (03-03): 5 rows visible and individually clickable; clicking each opens the drawer with that service's content; clicking a different row while open swaps content in place (no close/reopen flash); cursor shows "Ver detalle" on hover.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -nE "<a[^>]*service-row|href=\"#contacto\"" components/sections/capabilities-section.tsx` returns ZERO hits (no anchor rows remain).
    - `grep -n "service-row" components/sections/capabilities-section.tsx` shows `<button type="button"` usage.
    - `grep -n "data-cursor=\"Ver detalle\"" components/sections/capabilities-section.tsx` returns a hit and `grep -n "data-cursor=\"Cotizar\"" ...` returns zero.
    - `grep -n "key={service.id}" components/sections/capabilities-section.tsx` returns a hit.
    - `grep -n "useOverlayCoordination(\"drawer\"" components/sections/capabilities-section.tsx` and `grep -n "<ServiceDrawer" components/sections/capabilities-section.tsx` both return hits.
    - `grep -n "aria-disabled={menuIsOpen}" components/sections/capabilities-section.tsx` returns a hit and the onClick guards with `if (menuIsOpen) return`.
    - `npm run typecheck` passes.
  </acceptance_criteria>
  <done>The 5 ejes render as `<button>` drawer triggers with `data-cursor="Ver detalle"`, keyed by `service.id`, that open `ServiceDrawer`, mark the selected row, no-op while the menu is open, and preserve the existing reveal animation.</done>
</task>

<task type="auto">
  <name>Task 2: Add service-row button-reset, focus-visible, disabled and selected CSS</name>
  <files>app/globals.css</files>
  <read_first>
    - 03-RESEARCH.md Pitfall 3 (button UA-chrome reset) and "Code Examples → CSS additions" (`.service-row` reset + `[aria-disabled]` + `:focus-visible` sketch, lines ~395-400).
    - app/globals.css — the CURRENT `.service-row` rule and its `:hover` accent-fill sweep (`.service-row:before{transform:scaleY(1)}`) and the existing `:hover` `padding-inline` override; also the `.submit-button:disabled` convention (opacity/pointer-events) to mirror.
    - 03-UI-SPEC.md "Service row/card trigger" states table: focus-visible inset `outline:2px solid var(--focus-ring);outline-offset:-2px`; disabled `opacity:.6;cursor:not-allowed;pointer-events:none`; selected = persistent accent left-edge reusing the existing `:before` accent fill pinned open.
  </read_first>
  <action>
    Append `.service-row` CSS to `app/globals.css`. Add the button reset alongside the existing rules so the `<a>`→`<button>` swap keeps identical layout (Pitfall 3): `width:100%;text-align:left;background:transparent;border:0;padding:0;font:inherit;cursor:pointer` — set `padding:0` explicitly (the existing `:hover` `padding-inline` rule still overrides on hover; the base must not inherit UA button padding). Add `.service-row[aria-disabled="true"]{opacity:.6;cursor:not-allowed;pointer-events:none;transition:opacity var(--motion-duration-fast)}` (mirrors `.submit-button:disabled`; the 200ms transition matches UI-SPEC Motion for the mutual-exclusivity opacity change). Add the codebase's first row `:focus-visible` rule: `.service-row:focus-visible{outline:2px solid var(--focus-ring);outline-offset:-2px}` (inset, since the row spans full width). Style `.service-row-selected-indicator` as a persistent accent left-edge marker (reuse the accent-fill visual language already on `.service-row:before` — a pinned-open accent bar, not a new hue); ensure it does not fight the existing hover sweep. Do NOT introduce new `--space-*` custom properties — use literal values consistent with the rest of the stylesheet.
  </action>
  <verify>
    <automated>from local-2/: npm run lint && npm run typecheck && npm run build</automated>
    <human-check>Deferred to phase gate (03-03): rows look identical to before (no extra padding/border shift) after the button swap; focus ring appears on keyboard focus; the open row shows the accent selected indicator; rows dim when the menu is open.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -n "service-row:focus-visible" app/globals.css` returns a hit (first row focus-visible rule).
    - `grep -n "service-row\[aria-disabled" app/globals.css` returns a hit with `pointer-events:none` and `opacity:.6`.
    - `grep -n "service-row-selected-indicator" app/globals.css` returns a hit.
    - `grep -nE "background:transparent|border:0|padding:0" app/globals.css` confirms the button reset is present on `.service-row`.
    - `npm run build` succeeds.
  </acceptance_criteria>
  <done>The converted rows render pixel-identical to the prior anchors, gain a keyboard focus ring, show the accent selected indicator when their drawer is open, and dim/disable when the menu is open; build is clean.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan creates/changes:
- `components/sections/capabilities-section.tsx` — now renders `<button className="service-row">` triggers, holds `activeService: Service | null` state, calls `useOverlayCoordination("drawer", ...)`, and renders `<ServiceDrawer>`; adds the `service-row-selected-indicator` element.
- `app/globals.css` additions — selectors: `.service-row` (button reset), `.service-row[aria-disabled="true"]`, `.service-row:focus-visible`, `.service-row-selected-indicator`.
No new exported symbols (consumes `ServiceDrawer` and `useOverlayCoordination` from Plan 01).
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Rows render static `service` fields; the trigger only sets local React state and opens a client-side dialog. No input, network, or auth surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Tampering | `.service-row` disabled state (SERV-03) | mitigate | Disabled state is enforced in JS (onClick `if (menuIsOpen) return`) AND `aria-disabled`, not `pointer-events:none` alone — so keyboard-Enter activation on a focused row is also blocked (per 03-RESEARCH.md Pattern 3 rationale). |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase. |
</threat_model>

<verification>
- `npm run lint && npm run typecheck` after Task 1; add `npm run build` after Task 2 (wave-2 merge gate).
- Grep gates confirm the anchor→button conversion is complete (no `href="#contacto"` rows), the cursor copy changed, and the coordination call + drawer render are wired.
</verification>

<success_criteria>
- 5 ejes render as `<button>` drawer triggers (SERV-01) that open `ServiceDrawer` with the correct service (SERV-02).
- Cursor label is "Ver detalle"; selected row shows accent indicator.
- Rows disable (visual + `aria-disabled` + no-op click) while the menu is open (SERV-03 drawer side).
- Build/lint/typecheck pass.
</success_criteria>

<output>
Create `.planning/phases/03-servicios-y-drawer-de-detalle/03-02-SUMMARY.md` when done.
</output>
