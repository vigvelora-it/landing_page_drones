---
phase: 03-servicios-y-drawer-de-detalle
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - components/menu-overlay.tsx
  - components/inert-boundary.tsx
  - app/page.tsx
  - app/globals.css
autonomous: true
requirements: [SERV-02, SERV-03]
tags: [overlay, mutual-exclusivity, inert, accessibility, css]

must_haves:
  truths:
    - "While the drawer is open, the hamburger menu-toggle is visually disabled, carries aria-disabled=true, and its click is a no-op"
    - "Neither overlay force-closes the other — the user must close whichever is open before the other becomes available"
    - "While the drawer is open, the header and every background section (Hero, Manifesto, Technology, Process, Contact) are marked inert in the rendered DOM"
    - "CapabilitiesSection (the drawer's own trigger + the drawer itself) is NOT inert-wrapped, so the drawer stays interactive"
  artifacts:
    - path: "components/menu-overlay.tsx"
      provides: "menu-toggle disabled treatment driven by useOverlayCoordination(\"menu\", menuOpen)"
      contains: "useOverlayCoordination"
    - path: "components/inert-boundary.tsx"
      provides: "InertBoundary client wrapper toggling the inert attribute from useOverlayOpen(\"drawer\")"
      exports: ["InertBoundary"]
      contains: "inert"
    - path: "app/page.tsx"
      provides: "InertBoundary wrapping the header + all non-capabilities sections"
      contains: "InertBoundary"
    - path: "app/globals.css"
      provides: ".menu-toggle[aria-disabled] disabled treatment"
      contains: "menu-toggle"
  key_links:
    - from: "components/menu-overlay.tsx"
      to: "hooks/use-overlay-coordination.ts"
      via: "useOverlayCoordination(\"menu\", menuOpen)"
      pattern: "useOverlayCoordination\\(\"menu\""
    - from: "components/inert-boundary.tsx"
      to: "hooks/use-overlay-coordination.ts"
      via: "useOverlayOpen(\"drawer\")"
      pattern: "useOverlayOpen\\(\"drawer\"\\)"
    - from: "app/page.tsx"
      to: "components/inert-boundary.tsx"
      via: "wraps header + background sections"
      pattern: "<InertBoundary"
---

<objective>
Complete SERV-03 by wiring the menu side of the mutual-exclusivity contract and adding the explicit `inert` background boundary (orchestrator-resolved option d). When the drawer is open the hamburger is disabled and the background is marked inert; when the menu is open the rows are already disabled (Plan 02). Neither force-closes the other.

Purpose: Close SERV-03 (mutual exclusivity + shared scroll lock, already via `useScrollLock` in both overlays) and satisfy SERV-02's literal "inert en el fondo" wording with a grep-able attribute in rendered markup — belt-and-suspenders over `showModal()`'s native modality (03-RESEARCH.md Pattern 1).
Output: menu-overlay coordination + disabled CSS, new `InertBoundary` client wrapper, and `page.tsx` restructured to wrap the header and all non-capabilities sections.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-servicios-y-drawer-de-detalle/03-UI-SPEC.md
@.planning/phases/03-servicios-y-drawer-de-detalle/03-RESEARCH.md
@.planning/phases/03-servicios-y-drawer-de-detalle/03-01-SUMMARY.md
@F:\ClaudeCode\Pagina_Web_Mayra\local-2\components\menu-overlay.tsx
@F:\ClaudeCode\Pagina_Web_Mayra\local-2\app\page.tsx
@F:\ClaudeCode\Pagina_Web_Mayra\local-2\CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire menu-overlay mutual exclusivity + disabled CSS</name>
  <files>components/menu-overlay.tsx, app/globals.css</files>
  <read_first>
    - components/menu-overlay.tsx — CURRENT file: `menuOpen` useState, `useScrollLock(menuOpen)` already present, the `.menu-toggle` button with `onClick={() => setMenuOpen((open) => !open)}`.
    - 03-RESEARCH.md Pattern 3 "Usage in MenuOverlay" line (`const drawerOpen = useOverlayCoordination("menu", menuOpen)` → spread aria-disabled + guard onClick).
    - 03-UI-SPEC.md "Mutual exclusivity (SERV-03)": disable, don't auto-close; menu-toggle gets `opacity:.6;pointer-events:none;aria-disabled="true"` when drawer open, mirroring the service-row treatment.
    - app/globals.css `.submit-button:disabled` and the `.service-row[aria-disabled]` rule added in Plan 02 — reuse the same visual convention.
  </read_first>
  <action>
    In `components/menu-overlay.tsx` add `const drawerOpen = useOverlayCoordination("menu", menuOpen)` (import from `@/hooks/use-overlay-coordination`). This broadcasts the menu's open state into the shared store (so CapabilitiesSection's rows disable — Plan 02 already reads it) AND returns whether the drawer is open. On the `.menu-toggle` button add `aria-disabled={drawerOpen}` and guard its onClick: `onClick={() => { if (drawerOpen) return; setMenuOpen((open) => !open) }}`. Do NOT force-close the drawer from here — disable only (UI-SPEC decision #5). Leave `useScrollLock(menuOpen)` and all navigation markup unchanged.
    In `app/globals.css` append `.menu-toggle[aria-disabled="true"]{opacity:.6;cursor:not-allowed;pointer-events:none;transition:opacity var(--motion-duration-fast)}` — the same convention as `.service-row[aria-disabled]`, giving both triggers a symmetric disabled signal.
  </action>
  <verify>
    <automated>from local-2/: npm run lint && npm run typecheck</automated>
    <human-check>Deferred to phase gate: with a drawer open, the hamburger is dimmed and clicking it does nothing; closing the drawer re-enables it; the drawer is never auto-closed by menu interaction.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -n "useOverlayCoordination(\"menu\"" components/menu-overlay.tsx` returns a hit.
    - `grep -n "aria-disabled={drawerOpen}" components/menu-overlay.tsx` returns a hit and the onClick guards with `if (drawerOpen) return`.
    - `grep -n "setMenuOpen(false)\|setActiveService" components/menu-overlay.tsx` shows NO force-close of the drawer (menu-overlay must not reach into drawer state).
    - `grep -n "menu-toggle\[aria-disabled" app/globals.css` returns a hit.
    - `npm run typecheck` passes.
  </acceptance_criteria>
  <done>The hamburger disables (visual + `aria-disabled` + no-op click) while the drawer is open, sharing the same store-driven signal as the rows, and never force-closes the drawer.</done>
</task>

<task type="auto">
  <name>Task 2: Create InertBoundary and apply it in page.tsx</name>
  <files>components/inert-boundary.tsx, app/page.tsx</files>
  <read_first>
    - 03-RESEARCH.md Pattern 1 (the `inert`-across-Server/Client-boundary open question) and the orchestrator resolution: option (d) — a small client wrapper wraps every section EXCEPT `CapabilitiesSection` (Hero, Manifesto, Technology, Process, Contact, plus the header), driven by the `drawer` boolean from `useOverlayOpen`.
    - app/page.tsx — CURRENT structure: `<IntroSequence />`, `<MenuOverlay />`, `<CustomCursor />`, then `<main>` with Hero/Manifesto/Capabilities/Technology/Process/Contact, then footer + environment-badge. `page.tsx` is a Server Component (`force-static`) and cannot hold client state — the boundary reads the store instead.
    - 03-01-SUMMARY.md — confirm `useOverlayOpen(key)` export name/signature.
    - CLAUDE.md — `"use client"` header, named export, kebab-case filename.
  </read_first>
  <action>
    Create `components/inert-boundary.tsx` (`"use client"`) exporting `InertBoundary({ children }: { children: React.ReactNode })`. It calls `const drawerOpen = useOverlayOpen("drawer")` (read-only selector from Plan 01 — do NOT use `useOverlayCoordination` here, which would clobber store state) and renders `<div style={{ display: "contents" }} inert={drawerOpen || undefined}>{children}</div>`. Use `display:contents` so the wrapper generates no box and layout is unchanged; pass `inert={drawerOpen || undefined}` so the attribute is absent (not `inert="false"`) when the drawer is closed. `inert` applies to descendants regardless of `display:contents`.
    In `app/page.tsx`, import `InertBoundary` and restructure so the header and all NON-capabilities sections sit inside `InertBoundary` wrappers while `CapabilitiesSection` stays OUTSIDE any inert wrapper: wrap `<MenuOverlay />` in its own `InertBoundary`; inside `<main>`, wrap `<HeroSection /><ManifestoSection />` in one `InertBoundary`, leave `<CapabilitiesSection />` unwrapped between them, and wrap `<TechnologySection /><ProcessSection /><ContactSection />` in a second `InertBoundary`. Leave `<IntroSequence />`, `<CustomCursor />`, footer, and environment-badge as-is (decorative/aria-hidden; native `showModal()` already makes them unreachable). Keep `<main>` a single element and preserve section order and `force-static`. The drawer itself is rendered inside `CapabilitiesSection` (Plan 02), which is intentionally NOT inert-wrapped, and escapes ancestor inertness by spec anyway (Pattern 1) — double-safe.
  </action>
  <verify>
    <automated>from local-2/: npm run lint && npm run typecheck && npm run build</automated>
    <human-check>Deferred to phase gate: with the drawer open, DevTools shows `inert` on the header wrapper and the two background-section wrappers but NOT on the CapabilitiesSection subtree; Tab and mouse clicks cannot reach any background link while the drawer is open; the drawer's own controls remain fully interactive.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -n "useOverlayOpen(\"drawer\")" components/inert-boundary.tsx` returns a hit and `grep -n "useOverlayCoordination" components/inert-boundary.tsx` returns ZERO (read-only, no clobber).
    - `grep -nE "inert=\{drawerOpen" components/inert-boundary.tsx` returns a hit and `grep -n "display: \"contents\"\|display:contents" components/inert-boundary.tsx` returns a hit.
    - `grep -c "<InertBoundary" app/page.tsx` returns 3 (header wrapper + two section-group wrappers).
    - `grep -n "<CapabilitiesSection" app/page.tsx` shows it is NOT nested inside an `<InertBoundary>` (sits directly under `<main>` between the two wrapped groups).
    - `npm run build` succeeds.
  </acceptance_criteria>
  <done>`InertBoundary` toggles the `inert` attribute on the header and all background sections from the shared drawer boolean, leaving `CapabilitiesSection` and the drawer interactive; build is clean.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan creates/changes:
- `components/inert-boundary.tsx` — exports `InertBoundary` (prop: `children`); reads `useOverlayOpen("drawer")`.
- `components/menu-overlay.tsx` — now calls `useOverlayCoordination("menu", menuOpen)` and disables `.menu-toggle` when the drawer is open.
- `app/page.tsx` — wraps the header and non-capabilities sections in three `InertBoundary` instances.
- `app/globals.css` addition — selector `.menu-toggle[aria-disabled="true"]`.
No new exported symbols beyond `InertBoundary` (consumes the Plan 01 hook).
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Coordination is client-only, in-memory module state; `inert` is a declarative accessibility attribute. No input, network, auth, or persistence introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Elevation of Privilege (accessibility integrity) | Background reachability while drawer open | mitigate | Native `showModal()` makes the document inert automatically; the explicit `InertBoundary` `inert` attribute is an independently grep-able, DevTools-verifiable reinforcement of SERV-02's "inert en el fondo" wording. Menu-toggle disabling is enforced in JS (guarded onClick) plus `aria-disabled`, not `pointer-events` alone (SERV-03). |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase. |
</threat_model>

<verification>
- `npm run lint && npm run typecheck` after Task 1; `npm run build` after Task 2 (final wave merge).
- Phase gate (end-of-phase human verify, per config `human_verify_mode: end-of-phase`) walks the three manual rows from 03-VALIDATION.md: (1) drawer focus trap + focus-return; (2) background truly unreachable (inert + modality); (3) drawer↔menu mutual exclusivity + scroll-lock consistency across both overlays. Re-verify in `npm run build` output, not only `npm run dev`.
</verification>

<success_criteria>
- Hamburger disables (visual + `aria-disabled` + no-op click) while the drawer is open; rows disable while the menu is open (Plan 02); neither force-closes the other (SERV-03).
- Header + all background sections carry `inert` in rendered DOM while the drawer is open; `CapabilitiesSection`/drawer stay interactive (SERV-02 "inert en el fondo").
- Both overlays share `useScrollLock` — background does not scroll while either is open.
- Build/lint/typecheck pass.
</success_criteria>

<output>
Create `.planning/phases/03-servicios-y-drawer-de-detalle/03-03-SUMMARY.md` when done.
</output>
