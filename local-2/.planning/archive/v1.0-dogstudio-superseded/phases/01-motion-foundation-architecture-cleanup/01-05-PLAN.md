---
phase: 01-motion-foundation-architecture-cleanup
plan: 05
type: execute
wave: 2
depends_on: [01-01, 01-02]
files_modified:
  - components/sections/technology-section.tsx
  - components/sections/process-section.tsx
  - components/sections/contact-section.tsx
autonomous: true
requirements: [ARCH-02]
must_haves:
  truths:
    - "Technology (#tecnologia), Process (#proceso), and Contact (#contacto) are each their own \"use client\" section component with markup lifted verbatim from app/page.tsx"
    - "Each section owns a scoped useGSAP({ scope }) call wrapping the existing IntersectionObserver [data-reveal] setup — no new gsap.from()/ScrollTrigger animation"
    - "TechnologySection's tech-media [data-parallax='0.1'] element is served by the single shared use-legacy-parallax hook (from the provider), not a new local parallax loop"
    - "ContactSection renders <ContactForm /> (from plan 01) rather than duplicating the form markup"
  artifacts:
    - path: "components/sections/technology-section.tsx"
      provides: "Technology (#tecnologia) markup incl. tech-media parallax element, scoped reveal"
      exports: ["TechnologySection"]
    - path: "components/sections/process-section.tsx"
      provides: "Process (#proceso) markup + process.map steps + deliverable, scoped reveal"
      exports: ["ProcessSection"]
    - path: "components/sections/contact-section.tsx"
      provides: "Contact (#contacto) copy + renders <ContactForm />, scoped reveal"
      exports: ["ContactSection"]
  key_links:
    - from: "components/sections/contact-section.tsx"
      to: "components/contact-form.tsx"
      via: "renders <ContactForm />"
      pattern: "ContactForm"
    - from: "components/sections/process-section.tsx"
      to: "lib/site-content process"
      via: "process.map for process-step list"
      pattern: "process.map"
    - from: "components/sections/technology-section.tsx"
      to: "[data-parallax] shared hook"
      via: "tech-media keeps data-parallax='0.1' (no local rAF loop)"
      pattern: "data-parallax"
---

<objective>
Extract the remaining three page sections — `TechnologySection`, `ProcessSection`, `ContactSection` — from `app/page.tsx` into per-section `"use client"` components, each with its own scoped `useGSAP` reveal wrapper. Completes the section extraction half of ARCH-02.

Purpose: Finish decomposing the inline section markup. Two codebase-specific constraints from 01-RESEARCH.md: TechnologySection's `tech-media` `[data-parallax]` element must remain served by the ONE shared `useLegacyParallax` hook (in the provider) — no local parallax loop; ContactSection renders `<ContactForm />` (created in plan 01), not a copy of the form. Reveal stays vanilla/byte-identical (Phase 2 does GSAP-native migration).

Output: `components/sections/{technology,process,contact}-section.tsx`. Not mounted yet — plan 06 composes them.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-motion-foundation-architecture-cleanup/01-RESEARCH.md
@.planning/phases/01-motion-foundation-architecture-cleanup/01-PATTERNS.md
@.planning/phases/01-motion-foundation-architecture-cleanup/01-UI-SPEC.md
@app/page.tsx
@components/experience.tsx
</context>

<artifacts_this_phase_produces>
This plan creates the following new symbols/files:
- `components/sections/technology-section.tsx` — exports `TechnologySection`
- `components/sections/process-section.tsx` — exports `ProcessSection`
- `components/sections/contact-section.tsx` — exports `ContactSection`
Consumes (from earlier waves): `process` (`@/lib/site-content`), `Arrow` (`@/components/arrow`), `ContactForm` (`@/components/contact-form`), `useGSAP` (`@/lib/gsap`).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Extract TechnologySection (#tecnologia) with shared parallax element</name>
  <files>components/sections/technology-section.tsx</files>
  <read_first>
    - app/page.tsx (technology/#tecnologia markup lines 174-196, including tech-media parallax-media with data-parallax="0.1", tech-specs, tech-caption)
    - components/experience.tsx (reveal observer core lines 20-32)
    - .planning/phases/01-motion-foundation-architecture-cleanup/01-RESEARCH.md (tech-media served by the single shared use-legacy-parallax, not a local loop)
    - .planning/phases/01-motion-foundation-architecture-cleanup/01-PATTERNS.md (sections analog)
  </read_first>
  <action>
    Create `components/sections/technology-section.tsx` as a `"use client"` component `TechnologySection`. Copy the `<section className="technology" id="tecnologia">...</section>` markup verbatim from `app/page.tsx` lines 174-196, keeping the `<div className="tech-media parallax-media" data-parallax="0.1">` element and its `data-parallax` attribute exactly (the shared `useLegacyParallax` hook in the provider iterates all `[data-parallax]` elements globally — do NOT add a local parallax loop here). Add a scoped reveal `useGSAP({ scope: root })` on a `useRef` attached to the `<section>`, wrapping the unchanged IntersectionObserver `[data-reveal]` setup. No new GSAP animation.
  </action>
  <verify>
    <automated>cd f:/ClaudeCode/Pagina_Web_Mayra/local-2 && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `components/sections/technology-section.tsx` starts with `"use client"`, exports `TechnologySection`, contains `id="tecnologia"`, `data-parallax="0.1"`, and `useGSAP`.
    - `grep -n "requestAnimationFrame\|data-parallax.*forEach\|useLegacyParallax" components/sections/technology-section.tsx` returns zero matches for a local parallax loop (the attribute stays; no loop is added).
    - `grep -n "gsap.from\|gsap.to\|ScrollTrigger.create" components/sections/technology-section.tsx` returns zero matches.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>Technology section extracted verbatim; parallax element served by the shared hook; scoped behavior-identical reveal.</done>
</task>

<task type="auto">
  <name>Task 2: Extract ProcessSection (#proceso)</name>
  <files>components/sections/process-section.tsx</files>
  <read_first>
    - app/page.tsx (process/#proceso markup lines 198-230, process.map steps lines 205-211, deliverable block lines 213-228)
    - lib/site-content.ts (process array, created in plan 01)
    - components/experience.tsx (reveal observer core lines 20-32)
    - .planning/phases/01-motion-foundation-architecture-cleanup/01-PATTERNS.md (sections analog)
  </read_first>
  <action>
    Create `components/sections/process-section.tsx` as a `"use client"` component `ProcessSection`. Copy the `<section className="process-section section-pad" id="proceso">...</section>` markup verbatim from `app/page.tsx` lines 198-230, including the `process.map(([number, title, copy]) => ...)` step list and the `deliverable` block. Import `process` from `@/lib/site-content`. Add a scoped reveal `useGSAP({ scope: root })` on a `useRef` attached to the `<section>`, wrapping the unchanged IntersectionObserver `[data-reveal]` setup. No new GSAP animation.
  </action>
  <verify>
    <automated>cd f:/ClaudeCode/Pagina_Web_Mayra/local-2 && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `components/sections/process-section.tsx` starts with `"use client"`, exports `ProcessSection`, contains `id="proceso"`, `process.map`, and `useGSAP`.
    - Imports `process` from `@/lib/site-content`.
    - `grep -n "gsap.from\|gsap.to\|ScrollTrigger.create" components/sections/process-section.tsx` returns zero matches.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>Process section extracted verbatim consuming shared process data, with a scoped behavior-identical reveal.</done>
</task>

<task type="auto">
  <name>Task 3: Extract ContactSection (#contacto) rendering ContactForm</name>
  <files>components/sections/contact-section.tsx</files>
  <read_first>
    - app/page.tsx (contact copy lines 232-247 — the contact-copy column; note the form block lines 249-268 was already extracted to ContactForm in plan 01)
    - components/contact-form.tsx (ContactForm, created in plan 01)
    - components/arrow.tsx (Arrow, created in plan 01)
    - components/experience.tsx (reveal observer core lines 20-32)
    - .planning/phases/01-motion-foundation-architecture-cleanup/01-PATTERNS.md (contact-section renders <ContactForm /> in place of raw form)
  </read_first>
  <action>
    Create `components/sections/contact-section.tsx` as a `"use client"` component `ContactSection`. Copy the `<section className="contact-section" id="contacto">...` structure verbatim from `app/page.tsx` lines 232-247 (the contact-backdrop image + the `contact-grid` with the `contact-copy` column). Where the raw `<form className="contact-form" id="contact-form">` used to sit inside `contact-grid`, render `<ContactForm />` (imported from `@/components/contact-form`) instead. Import `Arrow` from `@/components/arrow` for the mailto link in the copy column. Add a scoped reveal `useGSAP({ scope: root })` on a `useRef` attached to the `<section>`, wrapping the unchanged IntersectionObserver `[data-reveal]` setup (this observes the `data-reveal` elements in the copy column; ContactForm carries its own `data-reveal` on its root `<form>` and is picked up by its own section scope). No new GSAP animation.
  </action>
  <verify>
    <automated>cd f:/ClaudeCode/Pagina_Web_Mayra/local-2 && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `components/sections/contact-section.tsx` starts with `"use client"`, exports `ContactSection`, contains `id="contacto"`, renders `<ContactForm />`, and uses `useGSAP`.
    - Imports `ContactForm` from `@/components/contact-form` and `Arrow` from `@/components/arrow`; does NOT contain a raw `<form className="contact-form"` (that lives in ContactForm).
    - `grep -n "gsap.from\|gsap.to\|ScrollTrigger.create" components/sections/contact-section.tsx` returns zero matches.
    - `npm run typecheck` and `npm run lint` exit 0.
  </acceptance_criteria>
  <done>Contact section extracted verbatim, rendering the colocated ContactForm, with a scoped behavior-identical reveal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser form → `/api/contact` | ContactSection renders ContactForm, which owns the submit path into the server API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering / Information Disclosure | ContactSection → ContactForm | mitigate | ContactSection renders the plan-01 `ContactForm` unchanged (honeypot + payload + server-authoritative Zod validation preserved). No form logic is re-implemented or weakened here. |
| T-01-05 | Tampering (behavior drift) | scoped `useGSAP` reveal wrappers | mitigate | Each section wraps the existing IntersectionObserver verbatim, enforced by the "zero gsap.from/ScrollTrigger" grep gate; parallax attribute kept without a local loop (single shared hook). |
</threat_model>

<verification>
Per D-02 no formal gate here; per-task `npm run typecheck`/`npm run lint`. Full gate + visual regression at phase end (plan 07).
</verification>

<success_criteria>
- Technology, Process, Contact are independent `"use client"` section components with verbatim markup (ARCH-02).
- tech-media parallax served by the single shared hook (no local loop); ContactSection renders ContactForm.
- Each has a scoped `useGSAP` wrapping the unchanged reveal observer (no GSAP-native animation added).
</success_criteria>

<output>
Create `.planning/phases/01-motion-foundation-architecture-cleanup/01-05-SUMMARY.md` when done.
</output>
