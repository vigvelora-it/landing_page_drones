---
phase: 01-motion-foundation-architecture-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - components/intro-sequence.tsx
  - components/menu-overlay.tsx
  - components/custom-cursor.tsx
autonomous: true
requirements: [ARCH-02]
must_haves:
  truths:
    - "The intro overlay, header+menu overlay, and custom cursor are each their own \"use client\" component, extracted verbatim from experience.tsx with identical markup, state, timers, and listeners"
    - "The custom cursor is a single global-chrome component (one pointermove listener, [data-cursor] delegation) — not duplicated per section"
    - "Menu open/close still toggles the body 'menu-open' class exactly as today (no new Lenis scroll-lock added this phase)"
  artifacts:
    - path: "components/intro-sequence.tsx"
      provides: "Intro overlay + introDone timer (1450ms / 100ms reduced-motion)"
      exports: ["IntroSequence"]
    - path: "components/menu-overlay.tsx"
      provides: "Header brand+toggle and overlay nav sharing menuOpen state"
      exports: ["MenuOverlay"]
    - path: "components/custom-cursor.tsx"
      provides: "Single custom-cursor element + global pointer listeners"
      exports: ["CustomCursor"]
  key_links:
    - from: "components/menu-overlay.tsx"
      to: "document.body 'menu-open' class"
      via: "useEffect toggling class on menuOpen"
      pattern: "menu-open"
    - from: "components/custom-cursor.tsx"
      to: "[data-cursor] targets"
      via: "pointerenter/pointerleave delegation"
      pattern: "data-cursor|pointerenter"
---

<objective>
Extract the three global-chrome concerns from the `experience.tsx` monolith into their own `"use client"` components — `IntroSequence`, `MenuOverlay`, and `CustomCursor` — with zero behavior or markup change. Part of ARCH-02.

Purpose: These are page-level fixed chrome (intro overlay, header+menu, cursor), independent of section DOM nesting and of the GSAP/Lenis engine. Extracting them here (in parallel with the motion-engine plan) shrinks what remains of `experience.tsx` to just the section markup wiring, which plan 06 finishes. Per 01-RESEARCH.md the cursor must stay a single instance (not per-section) to avoid duplicate `pointermove` listeners.

Output: `components/intro-sequence.tsx`, `components/menu-overlay.tsx`, `components/custom-cursor.tsx`. None are mounted yet — plan 06 renders them from `app/page.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-motion-foundation-architecture-cleanup/01-PATTERNS.md
@.planning/phases/01-motion-foundation-architecture-cleanup/01-UI-SPEC.md
@components/experience.tsx
</context>

<artifacts_this_phase_produces>
This plan creates the following new symbols/files:
- `components/intro-sequence.tsx` — exports `IntroSequence`
- `components/menu-overlay.tsx` — exports `MenuOverlay`
- `components/custom-cursor.tsx` — exports `CustomCursor`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Extract IntroSequence</name>
  <files>components/intro-sequence.tsx</files>
  <read_first>
    - components/experience.tsx (introDone state line 7; intro timer + reduced-motion branch lines 10-18; intro JSX lines 150-154; cleanup lines 76-77)
    - .planning/phases/01-motion-foundation-architecture-cleanup/01-PATTERNS.md (intro-sequence.tsx analog section, verbatim state/timer/JSX)
  </read_first>
  <action>
    Create `components/intro-sequence.tsx` as a `"use client"` component `IntroSequence`. Move: the `introDone` `useState(false)`, the `useEffect` that reads `window.matchMedia("(prefers-reduced-motion: reduce)").matches` and sets a `setTimeout(() => setIntroDone(true), reducedMotion ? 100 : 1450)` with a `clearTimeout` cleanup, and the intro JSX (`<div className={`intro ${introDone ? "intro-done" : ""}`} aria-hidden="true">...` from lines 150-154, verbatim including the brand span, `intro-line`, and `intro-coordinate`). Also carry over the hero-video reduced-motion pause behavior ONLY if it can be done without `document.querySelector("#hero-video")` — otherwise leave the video pause-on-reduced-motion to `HeroSection` (plan 04), which owns the video ref; do NOT reintroduce a `document.querySelector` here. Keep timing values (100ms / 1450ms) and easing untouched (Visual Regression Contract).
  </action>
  <verify>
    <automated>cd f:/ClaudeCode/Pagina_Web_Mayra/local-2 && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `components/intro-sequence.tsx` starts with `"use client"`, exports `IntroSequence`, and contains the `intro-done` class toggle and the `1450` / `100` timer values.
    - `grep -n "document.querySelector" components/intro-sequence.tsx` returns zero matches.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>Intro overlay + timer live in their own component, byte-identical behavior, no querySelector coupling.</done>
</task>

<task type="auto">
  <name>Task 2: Extract MenuOverlay (header + overlay, shared menuOpen state)</name>
  <files>components/menu-overlay.tsx</files>
  <read_first>
    - components/experience.tsx (menuOpen state line 6; body-class effect + navigate lines 89-94; header JSX lines 156-170; overlay JSX lines 172-185)
    - .planning/phases/01-motion-foundation-architecture-cleanup/01-PATTERNS.md (menu-overlay.tsx analog section — rationale for keeping header + overlay in one component)
    - .planning/phases/01-motion-foundation-architecture-cleanup/01-UI-SPEC.md (Visual Regression Contract; menu overlay is CSS-only this phase — Phase 2 handles the GSAP menu timeline)
  </read_first>
  <action>
    Create `components/menu-overlay.tsx` as a `"use client"` component `MenuOverlay` holding `const [menuOpen, setMenuOpen] = useState(false)`. Move verbatim: the `useEffect` that toggles `document.body.classList.toggle("menu-open", menuOpen)` with the `menu-open` removal cleanup, the `const navigate = () => setMenuOpen(false)` helper, the `<header className="site-header">...` block (brand link with `onClick={navigate}`, `header-center`, the `menu-toggle` button with its `is-open`/aria/onClick toggle), and the `<div className={`menu-overlay ...`}>` block (menu-index, `<nav>` with the five `#nosotros`/`#capacidades`/`#tecnologia`/`#proceso`/`#contacto` links each `onClick={navigate}`, and `menu-meta`). Keep it as ONE component (header toggle and overlay nav share `menuOpen`). Do NOT add Lenis `.stop()/.start()` scroll-lock — that is new behavior deferred beyond this phase; the current CSS-only `body.menu-open` behavior must be preserved exactly.
  </action>
  <verify>
    <automated>cd f:/ClaudeCode/Pagina_Web_Mayra/local-2 && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `components/menu-overlay.tsx` starts with `"use client"`, exports `MenuOverlay`, contains `classList.toggle("menu-open"`, and the five nav anchors (`#nosotros`, `#capacidades`, `#tecnologia`, `#proceso`, `#contacto`).
    - `grep -n "lenis\|Lenis\|\.stop()\|\.start()" components/menu-overlay.tsx` returns zero matches (no new scroll-lock).
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>Header + menu overlay share menuOpen state in one component with unchanged CSS-only open/close behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Extract CustomCursor (single global instance)</name>
  <files>components/custom-cursor.tsx</files>
  <read_first>
    - components/experience.tsx (cursor listener setup lines 54-73; cursor JSX line 187; cleanup lines 80, 82-85)
    - .planning/phases/01-motion-foundation-architecture-cleanup/01-PATTERNS.md (custom-cursor.tsx analog + Shared Patterns: single-instance systems must not be duplicated per-section)
  </read_first>
  <action>
    Create `components/custom-cursor.tsx` as a `"use client"` component `CustomCursor`. Move verbatim into a single `useEffect`: the `supportsPointer = window.matchMedia("(pointer: fine)").matches` guard, the `onPointerMove` handler writing `translate3d(...)` and adding `cursor-ready`, the `[data-cursor]` target collection with `enterCursor`/`leaveCursor` (setting the label text and toggling `cursor-active`), the passive `pointermove` window listener, and per-target `pointerenter`/`pointerleave` listeners — with the exact matching cleanup (remove pointermove + per-target listeners). Instead of `document.querySelector(".custom-cursor")` to find the element, render the cursor JSX from this component (`<div className="custom-cursor" aria-hidden="true"><span>Explorar</span></div>`, line 187) and read it via a `useRef` on that element (avoids the querySelector dependency while keeping identical DOM/markup). This component is instantiated exactly once as global chrome — never inside a per-section component.
  </action>
  <verify>
    <automated>cd f:/ClaudeCode/Pagina_Web_Mayra/local-2 && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `components/custom-cursor.tsx` starts with `"use client"`, exports `CustomCursor`, renders `className="custom-cursor"`, and contains `pointer: fine`, `pointermove`, and `data-cursor` handling.
    - The `pointermove` window listener and per-target `pointerenter`/`pointerleave` listeners each have a matching removal in the effect cleanup.
    - `npm run typecheck` and `npm run lint` exit 0.
  </acceptance_criteria>
  <done>Custom cursor is a single global component with StrictMode-safe listener cleanup and identical markup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan extracts static chrome markup + browser event listeners; no untrusted-input boundary is crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Denial of Service (self-inflicted) | `components/custom-cursor.tsx` global listeners | mitigate | Cursor extracted as a single instance with exact listener cleanup — prevents the duplicate-`pointermove`/N-listener failure mode flagged in 01-RESEARCH.md (must not be duplicated per-section). |
| T-01-04 | Tampering | Chrome markup extraction | accept | Verbatim markup move, zero-visual-change gate; no new trust boundary and no runtime state change (Runtime State Inventory: none). |
</threat_model>

<verification>
Per D-02 no formal gate here; `npm run typecheck`/`npm run lint` per-task localize errors. Full gate + visual regression at phase end (plan 07).
</verification>

<success_criteria>
- Intro, menu, and cursor are independent `"use client"` components with byte-identical markup/behavior (ARCH-02).
- Cursor is a single global instance with complete listener cleanup (T-01-03).
- No new querySelector coupling and no new scroll-lock behavior introduced.
</success_criteria>

<output>
Create `.planning/phases/01-motion-foundation-architecture-cleanup/01-03-SUMMARY.md` when done.
</output>
