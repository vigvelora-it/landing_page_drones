---
phase: 01-motion-foundation-architecture-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/page.tsx
  - components/experience.tsx
  - app/globals.css
  - components/contact-form.tsx
  - components/arrow.tsx
  - lib/site-content.ts
autonomous: true
requirements: [ARCH-01, ARCH-03]
must_haves:
  truths:
    - "Submitting the contact form invokes an onSubmit handler declared in the same component that renders the <form> — no FormConnector, no document.querySelector('#contact-form') coupling"
    - "The honeypot input (name=\"website\") and the exact request payload posted to /api/contact are preserved unchanged; server-side Zod validation remains authoritative"
    - "landing-page-v4.html, lib/v4-template.ts, and components/v4-interactions.tsx no longer exist under local-2/ and are preserved under ../referencias/"
    - "The dead .form-event-bridge decoy form and its CSS rule are removed"
  artifacts:
    - path: "components/contact-form.tsx"
      provides: "ContactForm client component with colocated handleSubmit"
      exports: ["ContactForm"]
      contains: "onSubmit"
    - path: "lib/site-content.ts"
      provides: "Shared services and process data arrays extracted from page.tsx"
      exports: ["services", "process"]
    - path: "components/arrow.tsx"
      provides: "Shared decorative Arrow component"
      exports: ["Arrow"]
  key_links:
    - from: "components/contact-form.tsx"
      to: "/api/contact"
      via: "fetch POST in handleSubmit"
      pattern: "fetch\\(\"/api/contact\""
    - from: "app/page.tsx"
      to: "components/contact-form.tsx"
      via: "renders <ContactForm /> in place of the raw <form id=\"contact-form\">"
      pattern: "ContactForm"
---

<objective>
Extract the contact form into a self-owned `ContactForm` component (ARCH-01) and move the unused legacy v4 files out of the active codebase (ARCH-03). Both are isolated, GSAP-free architecture-cleanup changes that can land before the motion engine and section decomposition.

Purpose: Kill the fragile `FormConnector` DOM-query bridge and the dead `.form-event-bridge` decoy so the form has a single source of truth (ARCH-01), and remove dead legacy files so the decomposition work in later plans has a clean tree (ARCH-03). Per D-01 legacy files are moved to `../referencias/` (backup), not hard-deleted. Per D-03 verification is code-level only (no live Supabase submission).

Output: `components/contact-form.tsx`, `lib/site-content.ts`, `components/arrow.tsx`; `app/page.tsx` renders `<ContactForm />`; `components/experience.tsx` and `app/globals.css` cleaned of dead form artifacts; three legacy files relocated to `../referencias/`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-motion-foundation-architecture-cleanup/01-CONTEXT.md
@.planning/phases/01-motion-foundation-architecture-cleanup/01-RESEARCH.md
@.planning/phases/01-motion-foundation-architecture-cleanup/01-PATTERNS.md
@.planning/phases/01-motion-foundation-architecture-cleanup/01-UI-SPEC.md
@lib/contact-schema.ts
@app/api/contact/route.ts
</context>

<artifacts_this_phase_produces>
This plan creates the following new symbols/files (later plans and reviewers must treat these as newly-created, not drift):
- `components/contact-form.tsx` — exports `ContactForm`
- `lib/site-content.ts` — exports `services`, `process` (data arrays lifted verbatim from `app/page.tsx` lines 6-44)
- `components/arrow.tsx` — exports `Arrow` (the `↗` span helper lifted from `app/page.tsx` lines 46-48)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Extract ContactForm and shared content module (ARCH-01)</name>
  <files>components/contact-form.tsx, components/arrow.tsx, lib/site-content.ts, app/page.tsx, components/experience.tsx, app/globals.css</files>
  <read_first>
    - components/experience.tsx (source of submitForm, lines 112-146; FormConnector lines 207-216; dead .form-event-bridge form lines 199-201)
    - app/page.tsx (source of form markup lines 249-268; services array lines 6-37; process array lines 39-44; Arrow helper lines 46-48)
    - .planning/phases/01-motion-foundation-architecture-cleanup/01-PATTERNS.md (contact-form.tsx analog section — verbatim handleSubmit body)
    - lib/contact-schema.ts (Zod schema + honeypot field — unchanged, the authoritative validation contract)
    - app/api/contact/route.ts (server payload contract — must not change)
    - .planning/phases/01-motion-foundation-architecture-cleanup/01-UI-SPEC.md (Copywriting Contract + Visual Regression Contract)
  </read_first>
  <action>
    Create `lib/site-content.ts` exporting the `services` array (verbatim copy of `app/page.tsx` lines 6-37) and the `process` array (verbatim copy of lines 39-44) as named exports. Plain data module, no "use client" directive. Create `components/arrow.tsx` exporting `Arrow` (the exact `<span aria-hidden="true">↗</span>` function component from `app/page.tsx` lines 46-48); no directive needed (no hooks).

    Create `components/contact-form.tsx` as a "use client" component named `ContactForm` (named export). Port `experience.tsx`'s `submitForm` (lines 112-146) verbatim as a local `handleSubmit` — same fetch to `/api/contact`, same `Object.fromEntries(data.entries())` JSON payload, same `#submit-button` disable, same `#form-status` class/text transitions, same fallback string "No pudimos enviar tu solicitud." and "Ocurrió un error. Inténtalo nuevamente." Do NOT add client-side Zod validation (current code has none — server is authoritative; adding or removing validation would change behavior). Move the form markup from `app/page.tsx` lines 249-268 into this component, attaching `onSubmit={handleSubmit}` directly on the `<form>` element. Preserve verbatim: `className="contact-form"`, `data-reveal` attribute on the root `<form>`, `noValidate`, all field `name` attributes, the honeypot `<input className="honeypot" name="website" tabIndex={-1} aria-hidden="true" />`, `id="submit-button"`, `id="form-status"`, and the `services.map(...)` select options (import `services` from `@/lib/site-content`). Drop only the `id="contact-form"` attribute (no longer needed once there is no querySelector coupling). Import `Arrow` from `@/components/arrow` for the submit button.

    In `app/page.tsx`: replace the `<form className="contact-form" id="contact-form" ...>...</form>` block (lines 249-268) with `<ContactForm />`, and add `import { ContactForm } from "@/components/contact-form"`. Leave the rest of `app/page.tsx` (inline section markup, local `services`/`process`/`Arrow` definitions, `<Experience />`) untouched — those are removed later in plan 06's switchover. This is ARCH-01 per D-03: code-level verification only, no live submission test.

    In `components/experience.tsx`: delete the `.form-event-bridge` decoy `<form>` block (lines 199-201), delete the `FormConnector` function (lines 207-216), delete the `<FormConnector onSubmit={submitForm} />` render (line 202), and delete the now-unused `submitForm` function (lines 112-146). Remove the `FormEvent` import from line 3 if no longer referenced. In `app/globals.css`: delete the `.form-event-bridge { display: none }` rule.
  </action>
  <verify>
    <automated>cd f:/ClaudeCode/Pagina_Web_Mayra/local-2 && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `components/contact-form.tsx` exists, starts with `"use client"`, exports `ContactForm`, and contains `onSubmit={handleSubmit}` on the `<form>` element.
    - `grep -n "document.querySelector" components/contact-form.tsx` returns zero matches (no FormConnector-style coupling).
    - `grep -n "website" components/contact-form.tsx` matches the preserved honeypot input; `grep -n "data-reveal" components/contact-form.tsx` matches the root `<form>`.
    - `grep -rn "FormConnector\|form-event-bridge" components/ app/` returns zero matches (decoy + bridge fully removed).
    - `grep -n "form-event-bridge" app/globals.css` returns zero matches.
    - `app/page.tsx` contains `<ContactForm />` and `import { ContactForm }`; it no longer contains `id="contact-form"`.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>Contact form markup + submit handler are colocated in `components/contact-form.tsx`; FormConnector, decoy form, and its CSS are gone; payload/honeypot/schema contract to `/api/contact` unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Relocate legacy v4 files to ../referencias/ (ARCH-03, D-01)</name>
  <files>landing-page-v4.html, lib/v4-template.ts, components/v4-interactions.tsx</files>
  <read_first>
    - .planning/phases/01-motion-foundation-architecture-cleanup/01-RESEARCH.md (ARCH-03 verification: grep already confirmed these three files are unreferenced under app/ and components/)
    - .planning/phases/01-motion-foundation-architecture-cleanup/01-CONTEXT.md (D-01: move to ../referencias/, do not hard-delete)
  </read_first>
  <action>
    Per D-01, move (not delete) the three unused legacy files to the repo-root `../referencias/` folder (the designated home for prior HTML designs and original resources): `landing-page-v4.html`, `lib/v4-template.ts`, `components/v4-interactions.tsx`. First confirm none are imported anywhere under `app/` or `components/` (research pre-verified this; re-confirm with grep). Then copy each to `../referencias/` (preserving filename; place `v4-template.ts` and `v4-interactions.tsx` alongside `landing page.html` at the referencias root) and remove the originals from `local-2/`. Do not touch `PALETA-DE-MARCA.md` (its stale reference to `lib/v4-template.ts` is a documentation follow-up flagged out-of-scope in 01-UI-SPEC.md).
  </action>
  <verify>
    <automated>cd f:/ClaudeCode/Pagina_Web_Mayra/local-2 && grep -rn "v4-template\|v4-interactions\|landing-page-v4" app/ components/ lib/ | grep -v '^#' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `landing-page-v4.html`, `lib/v4-template.ts`, `components/v4-interactions.tsx` no longer exist under `local-2/` (`ls` on each path fails).
    - The three files exist under `../referencias/`.
    - `grep -rn "v4-template\|v4-interactions\|landing-page-v4" app/ components/ lib/` returns zero import/reference matches.
  </acceptance_criteria>
  <done>Legacy v4 files preserved in ../referencias/ and removed from local-2/, with zero remaining references in the active source tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser form → `/api/contact` | Untrusted user form input crosses into the server API on submit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering / Information Disclosure | `components/contact-form.tsx` submit path | mitigate | Preserve the exact current payload (`Object.fromEntries(FormData)` → JSON POST) and the honeypot input (`name="website"`); do not remove or bypass any validation. Server-side `contactSchema.safeParse` in `app/api/contact/route.ts` stays authoritative and unchanged (V5 Input Validation). |
| T-01-02 | Tampering | Legacy v4 file removal | accept | Files pre-verified unreferenced by any runtime import path (dev-time-only change); moved to `../referencias/` backup per D-01, zero runtime effect. |
</threat_model>

<verification>
Code-level only this plan (per D-02 the full lint/typecheck/build/visual gate runs once at phase end in plan 07). `npm run typecheck` must pass. The greps in the acceptance criteria confirm the ARCH-01/ARCH-03 structural assertions.
</verification>

<success_criteria>
- ContactForm renders and owns its submit handler with no `document.querySelector` coupling (ARCH-01).
- Honeypot + payload contract to `/api/contact` unchanged (T-01-01, D-03).
- Legacy v4 files relocated to `../referencias/`, unreferenced in source (ARCH-03, D-01).
- `npm run typecheck` exits 0.
</success_criteria>

<output>
Create `.planning/phases/01-motion-foundation-architecture-cleanup/01-01-SUMMARY.md` when done.
</output>
